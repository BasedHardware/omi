# AI Clone — Fill Gaps + Improve UI

## Goal
Fill remaining gaps and improve the AI Clone settings page UI for a polished manual-verify experience.

## Tasks

### 1. Auto-discovery banner in PluginURLCard
When plugin is auto-discovered (not manually configured), show a green "Plugin discovered automatically" banner instead of the "Configure" prompt.

### 2. Dev API key: skip requirement when discovery says dev_mode=true
When the plugin is in dev mode, the dev API key is optional. Update `isFullyConfigured` to account for this.

### 3. Connection status indicator
Add a health-check ping to the plugin URL on the PluginURLCard. Show a green dot when reachable, red when not.

### 4. Simplify the Connect flow for zero-config
When plugin URL + bearer are auto-discovered, the Telegram Connect sheet should only ask for the bot token — the other fields are pre-filled from config.

### 5. UI polish
- Use OmiColors tokens consistently
- Improve spacing, typography, and card styling
- Add a "What is AI Clone?" info section at the bottom
- Show the connection state (connected/disconnected/auto-reply on/off) more clearly

### 6. Uncommitted backend fixes
Commit the streaming fix (graph.py + integration.py + agentic.py) and the discovery file code (plugin_discovery.py + main.py lifespan).

### 7. Uncommitted desktop fixes
Commit PluginDiscovery.swift + AICloneConfig auto-discovery + OmiApp eager init trigger.
