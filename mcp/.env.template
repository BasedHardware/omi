# REST API base for this self-hosted stdio package — the package appends REST
# segments (memories, conversations, …), so the value MUST end with /v1/mcp/.
# This is distinct from the hosted MCP endpoint itself, https://api.omi.me/v1/mcp
# (no trailing slash), which is what remote MCP clients connect to.
OMI_API_BASE_URL=https://api.omi.me/v1/mcp/
OMI_API_KEY=
