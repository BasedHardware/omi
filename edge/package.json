{
  "name": "omi-edge",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "bun run --filter '*' test",
    "typecheck": "bun run --filter '*' typecheck",
    "dev": "bun run --filter @omi/api dev",
    "dev:node": "bun run packages/gateway/src/node-dev.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.3",
    "vitest": "^3.1.1",
    "hono": "^4.7.7",
    "wrangler": "4.12.0",
    "@cloudflare/workers-types": "^4.20250417.0"
  }
}
