{
  "name": "@omi-core/adapters-platform",
  "version": "0.0.0",
  "private": true,
  "description": "The contracts-native service's client half: makes that service reachable through the ports the surfaces already use. Carries the ratified memory, conversations, and folders READ paths and the tasks WRITE op-sender.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "echo adapters-platform:tested-via-testkit",
    "lint": "echo adapters-platform:no-lint-yet"
  },
  "dependencies": {
    "@omi-core/contracts": "workspace:*",
    "@omi-core/kernel": "workspace:*",
    "@omi-core/ratified-contracts": "workspace:*",
    "@omi-core/sync": "workspace:*",
    "@omi-core/wire-listen": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
