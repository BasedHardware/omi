pytest==8.4.1
pytest-asyncio==1.4.0
pytest-xdist==3.8.0
fake-firestore==0.13.1
fakeredis[lua]==2.36.2
pytest-httpserver==1.1.5
aioresponses==0.7.8
# Test-only Mongo fake (tests/store_fakes.py + people/users store unit tests). Kept OUT of
# requirements.txt so it does not land in pylock.runtime.toml (the prod/on-prem image); it stays in
# pylock.toml via this test-requirements input, and the offline test image installs it explicitly.
mongomock==4.3.0
