"""Local, high-fidelity listen-to-pusher stack test harness.

This package is intentionally an explicit developer test, not a unit-test
fixture or an always-on CI service.  See README.md for the trust boundary and
the command that starts the isolated stack.
"""
