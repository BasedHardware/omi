apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: {{ .Chart.Name }}-store
spec:
  provider:
    gcpsm:
      projectID: {{ required "gke.projectID is required" .Values.gke.projectID }}
      auth:
        workloadIdentity:
          clusterLocation: {{ required "gke.clusterLocation is required" .Values.gke.clusterLocation }}
          clusterName: {{ required "gke.clusterName is required" .Values.gke.clusterName }}
          serviceAccountRef:
            name: {{ .Values.serviceAccount.name }}
