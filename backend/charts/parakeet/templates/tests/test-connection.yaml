apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "parakeet.fullname" . }}-test-connection"
  labels:
    {{- include "parakeet.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "parakeet.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
