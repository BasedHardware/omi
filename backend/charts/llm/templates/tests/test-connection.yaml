apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "llm.fullname" . }}-test-connection"
  labels:
    {{- include "llm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "llm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
