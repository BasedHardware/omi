apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "backend-proactive.fullname" . }}-test-connection"
  labels:
    {{- include "backend-proactive.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "backend-proactive.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
