rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default rules - update according to your security requirements
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
