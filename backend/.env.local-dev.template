# Copy to backend/.env.local-dev — provider secrets ONLY. The harness injects everything else.
# Non-secret vars added here are ignored (see `make dev-status`).
PROVIDER_MODE=real
OPENAI_API_KEY=
DEEPGRAM_API_KEY=
GEMINI_API_KEY=
ANTHROPIC_API_KEY=
