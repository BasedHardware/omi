# GPU extras layered on top of the main backend uv.lock (see Dockerfile).
# Install with: uv sync --frozen --no-dev && uv pip install -r modal/requirements.txt
torch==2.10.0
torchaudio==2.10.0
pyannote.audio==3.3.1
speechbrain>=1.0.0,<2.0.0
pandas==2.2.2
matplotlib==3.9.1
