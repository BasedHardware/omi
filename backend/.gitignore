
# Local dev-only scratch files (never commit). Bare .venv name also matches a
# per-worktree .venv symlink, not just a real directory. dev_live_bridge.py is a
# local-only bridge that deliberately does blocking I/O in the async path.
.venv
dev_live_bridge.py
.env.harness.local
