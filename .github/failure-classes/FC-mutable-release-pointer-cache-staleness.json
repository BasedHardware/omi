{
  "schema_version": 1,
  "id": "FC-mutable-release-pointer-cache-staleness",
  "violated_contract": "A mutable release pointer must resolve to its authoritative current object generation; a cacheable public media response must never select an older release after the control plane has advanced.",
  "canonical_prevention": "Resolve the pointer through authenticated object metadata, pin the media read to that immutable generation, and write every mutable pointer path with Cache-Control no-store while retaining compare-and-swap generation guards.",
  "canonical_prevention_artifact": [
    "backend/tests/unit/test_agent_vm_reconciler.py",
    "backend/tests/unit/test_agent_vm_reconciler_deployment_scripts.py"
  ],
  "evidence_prs": [11221],
  "scope_hints": [
    "backend/jobs/agent_vm_reconciler.py",
    "backend/scripts/*agent-vm*release*.sh",
    ".github/workflows/desktop_backend_*.yml"
  ],
  "status": "open"
}
