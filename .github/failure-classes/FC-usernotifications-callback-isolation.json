{
  "schema_version": 1,
  "id": "FC-usernotifications-callback-isolation",
  "violated_contract": "UserNotifications completion handlers must not inherit MainActor isolation because the framework invokes them on a private XPC queue.",
  "canonical_prevention": "Register every completion-handler UserNotifications API through UserNotificationCallbackBridge, copy only Sendable callback values, then explicitly hand off to MainActor.",
  "evidence_prs": [10072],
  "scope_hints": ["desktop/macos/Desktop/Sources/**", "desktop/macos/scripts/**", "codemagic.yaml"],
  "status": "open"
}
