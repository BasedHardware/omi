{
  "schema_version": 1,
  "id": "FC-per-hop-timeout",
  "violated_contract": "A multi-hop request must consume one request-level deadline budget rather than receive a full timeout at each hop.",
  "canonical_prevention": "Create one monotonic deadline per Python chat execution, pass only its remaining budget to every retry, provider fallback, and LKG fallback, and fail before a downstream call once it is exhausted.",
  "canonical_prevention_artifact": ["backend/tests/unit/test_llm_gateway_executor.py"],
  "evidence_prs": [9533, 9644],
  "scope_hints": ["backend/**", "desktop/macos/**"],
  "status": "open"
}
