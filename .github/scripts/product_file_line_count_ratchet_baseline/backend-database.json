{
  "files": {
    "backend/database/conversations.py": 1728,
    "backend/database/memory_apply_store.py": 1932,
    "backend/database/users.py": 2131
  },
  "raise_justifications": {
    "backend/database/conversations.py": "Chat-first capture archive filtering and the #9809 orphaned in_progress selection belong beside the shared conversation query and cursor invariants; extraction would duplicate their ordered query contracts. Null-aware user-field preservation and folder_user_set marker so AI folder assignment does not clobber stub nulls or explicit user unfile.",
    "backend/database/memory_apply_store.py": "Canonical apply, source replacement, lineage, and projection-outbox writes must remain one atomic Firestore transaction boundary; splitting this owner would reintroduce split mutation authority (FC-split-mutation-authority). +11 routes stored lineage reactivation validation through the shared malformed-document read boundary while retaining atomic conflict semantics (#9494/#9696). +20 places the universal deployment-wide intake pause fence at both authoritative Firestore transaction entries so no caller can bypass incident rollback policy.",
    "backend/database/users.py": "Account-deletion marker fencing and recursive missing-root cleanup must remain beside the existing deletion transaction and collection traversal; extracting them would duplicate the durable lifecycle authority. +33 for the queue-before-auth recovery guard and +16 for the job-id-fenced repeat-dispatch transaction. +74 for the server-owned, expiring city-context consent record and revocation cache cleanup: both belong next to the existing user-document access boundary so Redis is never authorization authority (SCA-174). +16 keeps the uncached deletion-state normalization and atomic pending admission beside the transaction that owns that marker so auth and queue dispatch cannot consult competing lifecycle authority (SCA-280). +36 keeps injectable deletion reads and a durable late-Agent-VM cleanup record with the marker that drives retry dispatch. +21 persists and validates the provider numeric-ID fence on that deletion-owned late-cleanup record. +7 exposes the deletion-marker transaction that upgrades pre-fence retry records before provider cleanup. +11 keeps optional customer-Firestore targeting and no-provision quota reads beside the subscription document owner so desktop-backend cannot stamp Free onto a paying user in a second project."
  },
  "threshold": 1500
}
