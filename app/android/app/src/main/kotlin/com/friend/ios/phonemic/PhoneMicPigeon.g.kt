// Autogenerated from Pigeon (v26.1.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package com.friend.ios.phonemic

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMethodCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer
private object PhoneMicPigeonPigeonUtils {

  fun createConnectionError(channelName: String): PhoneMicPigeonError {
    return PhoneMicPigeonError("channel-error",  "Unable to establish connection on channel: '$channelName'.", "")  }

  fun wrapResult(result: Any?): List<Any?> {
    return listOf(result)
  }

  fun wrapError(exception: Throwable): List<Any?> {
    return if (exception is PhoneMicPigeonError) {
      listOf(
        exception.code,
        exception.message,
        exception.details
      )
    } else {
      listOf(
        exception.javaClass.simpleName,
        exception.toString(),
        "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
      )
    }
  }
}

/**
 * Error class for passing custom error details to Flutter via a thrown PlatformException.
 * @property code The error code.
 * @property message The error message.
 * @property details The error details. Must be a datatype supported by the api codec.
 */
class PhoneMicPigeonError (
  val code: String,
  override val message: String? = null,
  val details: Any? = null
) : Throwable()

/** Native capture state machine, mirrored to Dart on every transition. */
enum class PhoneMicCaptureState(val raw: Int) {
  IDLE(0),
  STARTING(1),
  RUNNING(2),
  INTERRUPTED(3),
  REBUILDING(4);

  companion object {
    fun ofRaw(raw: Int): PhoneMicCaptureState? {
      return values().firstOrNull { it.raw == raw }
    }
  }
}

/**
 * Capture sink selector, chosen once per session at start().
 *
 * - stream: converted PCM16 chunks are forwarded to Dart via onAudioFrame for
 *   realtime transcription (the existing path).
 * - batch: converted PCM16 chunks are opus-encoded natively and written to
 *   WAL-compatible .bin files on disk; nothing is sent to Dart except state and
 *   onBatchProgress. Used for "transcribe later" offline capture.
 */
enum class PhoneMicCaptureMode(val raw: Int) {
  STREAM(0),
  BATCH(1);

  companion object {
    fun ofRaw(raw: Int): PhoneMicCaptureMode? {
      return values().firstOrNull { it.raw == raw }
    }
  }
}
private open class PhoneMicPigeonPigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      129.toByte() -> {
        return (readValue(buffer) as Long?)?.let {
          PhoneMicCaptureState.ofRaw(it.toInt())
        }
      }
      130.toByte() -> {
        return (readValue(buffer) as Long?)?.let {
          PhoneMicCaptureMode.ofRaw(it.toInt())
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is PhoneMicCaptureState -> {
        stream.write(129)
        writeValue(stream, value.raw.toLong())
      }
      is PhoneMicCaptureMode -> {
        stream.write(130)
        writeValue(stream, value.raw.toLong())
      }
      else -> super.writeValue(stream, value)
    }
  }
}


/**
 * Dart -> native. start(mode) resolves once the engine is running, or throws a
 * PlatformException with one of: permission_denied, session_config_failed,
 * format_invalid, converter_init_failed, engine_start_failed. In batch mode it
 * may additionally throw opus_init_failed (the native opus encoder could not be
 * created) or batch_dir_unavailable (flutter.batchAudioDir is unset/empty).
 * stop() resolves only after teardown is fully drained — no frames or events
 * arrive after it, and in batch mode the current .bin file is finalized (fsynced
 * + atomically promoted, so it is ingestable) before stop() resolves.
 *
 * Generated interface from Pigeon that represents a handler of messages from Flutter.
 */
interface PhoneMicHostApi {
  fun start(mode: PhoneMicCaptureMode, callback: (Result<Unit>) -> Unit)
  fun stop(callback: (Result<Unit>) -> Unit)
  fun isRecording(): Boolean

  companion object {
    /** The codec used by PhoneMicHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      PhoneMicPigeonPigeonCodec()
    }
    /** Sets up an instance of `PhoneMicHostApi` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: PhoneMicHostApi?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.omi_phone_mic.PhoneMicHostApi.start$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val modeArg = args[0] as PhoneMicCaptureMode
            api.start(modeArg) { result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(PhoneMicPigeonPigeonUtils.wrapError(error))
              } else {
                reply.reply(PhoneMicPigeonPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.omi_phone_mic.PhoneMicHostApi.stop$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.stop{ result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(PhoneMicPigeonPigeonUtils.wrapError(error))
              } else {
                reply.reply(PhoneMicPigeonPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.omi_phone_mic.PhoneMicHostApi.isRecording$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            val wrapped: List<Any?> = try {
              listOf(api.isRecording())
            } catch (exception: Throwable) {
              PhoneMicPigeonPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
/**
 * Native -> Dart.
 *
 * Generated class from Pigeon that represents Flutter messages that can be called from Kotlin.
 */
class PhoneMicFlutterApi(private val binaryMessenger: BinaryMessenger, private val messageChannelSuffix: String = "") {
  companion object {
    /** The codec used by PhoneMicFlutterApi. */
    val codec: MessageCodec<Any?> by lazy {
      PhoneMicPigeonPigeonCodec()
    }
  }
  /**
   * PCM16 little-endian mono @16kHz. Chunk sizes vary with the input route.
   * Stream mode only; batch mode never emits frames.
   */
  fun onAudioFrame(pcm16leMono16kArg: ByteArray, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.omi_phone_mic.PhoneMicFlutterApi.onAudioFrame$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(pcm16leMono16kArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(PhoneMicPigeonError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(PhoneMicPigeonPigeonUtils.createConnectionError(channelName)))
      }
    }
  }
  fun onStateChanged(stateArg: PhoneMicCaptureState, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.omi_phone_mic.PhoneMicFlutterApi.onStateChanged$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(stateArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(PhoneMicPigeonError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(PhoneMicPigeonPigeonUtils.createConnectionError(channelName)))
      }
    }
  }
  /**
   * Non-fatal runtime failures (capture self-heals): converter_failed,
   * rebuild_failed, resume_failed, media_services_reset, batch_storage_full.
   */
  fun onCaptureError(codeArg: String, messageArg: String, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.omi_phone_mic.PhoneMicFlutterApi.onCaptureError$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(codeArg, messageArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(PhoneMicPigeonError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(PhoneMicPigeonPigeonUtils.createConnectionError(channelName)))
      }
    }
  }
  /**
   * Emitted at 1Hz while batch capture runs. Its arrival is the liveness signal
   * for the Dart batch watchdog; the value is derived from the number of frames
   * actually written to disk, so mutes and interruptions freeze it while the
   * event keeps arriving.
   */
  fun onBatchProgress(capturedSecondsArg: Double, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.omi_phone_mic.PhoneMicFlutterApi.onBatchProgress$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(capturedSecondsArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(PhoneMicPigeonError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(PhoneMicPigeonPigeonUtils.createConnectionError(channelName)))
      }
    }
  }
}
