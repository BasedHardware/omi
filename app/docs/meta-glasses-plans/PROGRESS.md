01 — done 2026-07-03 — thermal/hinge health state, warning UI, thermal photo-loop pause/retry/recovery, and silent stream-frame sampling replacing shuttered still capture — needs on-device check: yes, fold/thermal signals plus glasses gesture delivery on hardware.
02 — done 2026-07-03 — background frame loop now pauses the stream capability between samples, resumes for capture, and keeps live preview running when visible — needs on-device check: yes, verify native startStreamSession count stays one per capture run and frames still arrive every ~30s.
03 — done 2026-07-03 — Ray-Ban Display capture card now waits for DisplayState.started and shows throttled latest transcript snippets on-lens — needs on-device check: yes, verify live transcript updates on Ray-Ban Display and non-Display glasses do not start display sessions.
04 — done 2026-07-03 — opt-in continuous vision now subscribes to DAT videoFramesStream, downscales live frames to ≤512px JPEG, sends at ≤1/sec with no buffering or UI photo-strip spam, disables on overheat, and replaces the periodic snapshot cadence while enabled — needs on-device check: yes, verify the glasses stop playing shutter sounds and background frames reach the backend during capture.
05 — done 2026-07-03 — onboarding now shows a first-class Meta Glasses device choice, pushes the existing Meta registration page, and advances past the device step when registered linked glasses are present without requiring a BLE Omi device — needs on-device check: yes, verify fresh onboarding can complete with Meta Glasses only.

06 — done 2026-07-03 — Mock Device Kit integration now gates on kDebugMode + OMI_META_MOCK, pairs simulated Ray-Ban glasses, feeds a fixture frame, verifies queue-to-capture ingest, and covers linked/unlinked sanitizer states — needs on-device check: no, simulator/mock only.

07 — done 2026-07-03 — contract and behavioral tests now pin camera-permission states, update bridge layers, compatibility update routing, device-kind l10n labels, and English ARB keys — needs on-device check: no, unit coverage only.

08 — done 2026-07-03 — SwiftProtobuf now builds only for mcumgr compile-time use and is removed from Runner link/embed outputs, leaving MWDATCore as the sole runtime copy; simulator launch shows zero duplicate SwiftProtobuf objc warnings — needs on-device check: yes, verify firmware update still works on a real Omi device and Meta registration/capture still works on glasses.

09 — done 2026-07-03 — Android DAT Stage A wiring now uses FlutterFragmentActivity, minSdk 31/NDK 28.2, Meta GitHub Packages repos/docs, MWDAT manifest metadata, and omimeta callback scheme — needs on-device check: yes, Android SDK/GitHub Packages token plus glasses hardware required to verify registration, then capture/audio/gesture Stage B/C.

runtime — partial 2026-07-04 — honest-gesture candidate plus native main-thread gesture dispatch is installed on EddyPhone (`dev.moni11811.omi`), but latest install could not be launched because EddyPhone stayed locked through 30s and 90s retries. Source now removes fake tap/swipe remapping: only tap-equivalent media commands are registered, tap toggles capture, non-tap events are logged unsupported, swipe is treated as glasses volume, and the UI no longer exposes swipe/remap dropdowns. The Swift bridge now dispatches `invokeMethod("onGesture")` on `DispatchQueue.main.async`. Previous hardware proof before this native dispatch patch showed `stream-started=1`, `frame-event=1`, `frame-captured=1`, `MetaGlassGestureDiag listening-started=1`, `stream-start-failed=0`, `videoStreamingError=0`, but `MetaGlassGestureDiag received=0`. Verification after this patch: focused touched gate passes; `flutter analyze` has `error_lines=0` with 570 existing non-error issues; profile build succeeds; install succeeds; launch/proof blocked by locked device. Next action: unlock EddyPhone, launch `dev.moni11811.omi`, physically tap the glasses once, then run `./scripts/pull_meta_glasses_runtime_proof.sh && scripts/check_meta_glasses_runtime_proof.sh`.

launch-crash — fixed+verified 2026-07-05 — root cause from 11 pulled .ips crash reports: all faulted in SwiftFlutterContactsPlugin.register (EXC_BREAKPOINT) force-unwrapping UIApplication.shared.delegate!.window!! which is nil under the UIScene lifecycle; UIScene was only required because the build used Xcode-beta's iOS 27 SDK. Fix: removed UIApplicationSceneManifest from Info.plist (pinned by test "Runner keeps the legacy UIKit lifecycle"), rebuilt with default stable Xcode (sdk 26.5), also restored .dev.env API_BASE_URL=https://api.omi.me/ (dead trycloudflare tunnel was baked into dev_env.g.dart) and Flutter.podspec 17.0. Verified on EddyPhone: devicectl --console launch stayed attached >25s, full boot log (CallKit, BLE, keyboard, DAT startRegistration preflight OK → SDK returned without throwing), screenshot shows Omi Dev → Meta AI registration handoff. Open: glasses re-registration (fresh container wiped state), capture/gesture hardware proof, and the removed configureForBluetooth audio path (contract test red on purpose — product decision pending).

capture+gestures — hardware-proven 2026-07-05 — first successful on-device acceptance run (proof log 15:34–15:37): native captureLatestFrame produced real JPEGs (52–78KB) on the user interval; audio-stream-started route=bluetooth (glasses HFP mic restored, photos now ride the transcription socket via ingestCapturedImage because prod has no REST photo endpoint — v1/meta-wearables/photos/cache 404s on api.omi.me); FIRST hardware gesture delivery ever captured: OmiMetaGestures command=pause → MetaGlassGestureDiag received. Two bugs found from evidence: (1) phantom AVRCP pause ~400ms after listening-started toggled capture off — fixed with a 3s _gestureActivationGrace discard window (test-pinned); (2) the "restarts" the user saw were the agent's own --terminate-existing launch retry loop, not the app. One start-capture-failed reason=camera-stream-not-ready with successful retry remains a known flake. Still to verify by user: photos visible in history, transcript from glasses mic, real tap toggling capture after the grace window.
