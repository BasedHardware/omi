// The suite capture_test.dart runs. On current main it is registry.dart; app/scripts/visual_audit.sh
// points it at compat/<name>/registry.dart when it captures an older revision.
export 'registry.dart';
