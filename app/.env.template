Public app builds no longer read this file.

Use app/.client.env.example for client-safe config. Every value in
app/.client.env is compiled into public binaries and must be safe for users to
extract. Server-only secrets such as provider API keys, OAuth client secrets,
service accounts, private keys, admin tokens, and signing credentials must stay
out of app config.
