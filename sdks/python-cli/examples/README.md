# Example Recipes for the Python CLI SDK

This directory contains ready‑to‑run example scripts (recipes) that showcase
common tasks you might perform with the OMI Python CLI SDK.

## Available Recipes

- **audio_chunks_to_manifest** – Index a directory of raw audio chunk files into a
  validated SHA‑256 JSON manifest.

  