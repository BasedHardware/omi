# Python CLI Examples

These examples demonstrate common workflows and utilities for the
`omi` Python SDK.

## Available Recipes

| Recipe | Description |
|--------|-------------|
| `audio_chunks_to_manifest.py` | Convert a directory of raw audio chunk files into a SHA‑256 JSON manifest. |
| ... | ... |

> **Note**: The `audio_chunks_to_manifest.py` script is a new addition
> (see issue #15869).  It can be run directly from the command line
> or imported as a module in your own scripts.

## Running a Recipe

