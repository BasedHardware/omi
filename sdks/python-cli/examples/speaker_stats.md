# Speaker Stats Analyzer

The **Speaker Stats Analyzer** is a lightweight command‑line tool that
takes a transcript of speaker turns and produces a concise summary of
who talked when, how long, and how balanced the conversation was.

## Input Format

The input file must be a JSON array where each element represents a
speaker turn:

