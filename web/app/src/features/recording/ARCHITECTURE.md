# recording

Live capture. Used from `/home` composer and `/record` popout.

- `ui/` — context, controller, widget, transcript, header indicator
- `useRecording.ts` — capture + websocket; conversation finalize lives in `features/conversations`
