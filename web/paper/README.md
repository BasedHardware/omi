# PAPER

Standalone Next.js (App Router) marketing landing page for PAPER. No component libraries by design.

- Install: `npm ci`
- Develop: `npm run dev` — http://localhost:3005
- Build: `npm run build`
