# Optional: enables high-quality LLM flashcard extraction.
# Without it, a heuristic extractor is used as fallback.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional overrides
RECALL_DB_PATH=recall.db
MAX_CARDS_PER_CONVERSATION=5
