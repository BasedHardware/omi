# Apps, calls and settings

Generated by `spec/generate.py` from the artboards and `spec/content_*.py`. Read `spec/README.md` first for the vocabulary (presentation, transitions, gestures) and global rules.

## Apps — Apps

`Apps.dc.html` · plan IDs **A01** · stage **S11** · presented as **tab root (Apps)** · theme: dark + light

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/Apps.webp`, `specs/geometry/<device>/Apps.json`.

**Purpose.** Discover and manage apps that write summaries, chat, react in real time or send data elsewhere.

**Reached from.** `AppDetail`, `Chat`, `Conversations`, `FirstDay`, `Integrations`, `Main`, `Offline`, `Tasks`.

**Data (existing contracts only).** Grouped `GET v2/apps?offset&limit&include_reviews`; by capability `GET v2/apps/capability/{cap}/grouped`; search `GET v2/apps/search`; categories `GET v1/app-categories`; capabilities `GET v1/app-capabilities`; enabled `GET v1/apps/enabled`; enable `POST v1/apps/enable?app_id=`; disable `POST v1/apps/disable?app_id=`; detail `GET v1/apps/{id}`.

**States.**

- Explore / Installed
- search results
- catalog unavailable (≠ empty search)
- enable in progress: capsule shows spinner
- setup required: capsule says Set Up (never “Enabled”)

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | Search apps | always | Search apps (debounced; server search). | — | in place |
| 2 | Explore | always | Explore segment. | — | in place |
| 3 | Installed | always | Installed segment. | — | in place |
| 4 | Chat Talk to an expert | explore | Capability: Chat → capability list (push). | — | in place |
| 5 | Summaries Your format, every time | explore | Capability: Summaries. | — | in place |
| 6 | Real-time Nudges while you talk | explore | Capability: Real-time. | — | in place |
| 7 | Integrations Send it elsewhere | explore | Capability: Integrations. | — | in place |
| 8 | (icon) | explore, each row | Open app detail (push). | `AppDetail` | push |
| 9 | Enable | a.isGet | Enable (no setup needed): `POST v1/apps/enable` → capsule morphs to a check; read back enabled list. | — | in place |
| 10 | Set Up | a.isSetup | Set Up → app detail (setup steps). | `AppDetail` | push |
| 11 | Build your own app Describe it in a sentence — Omi drafts … | always | Build your own app: explain + open the web creator (creator flows are tracked separately). | — | in place |
| 12 | (icon) | installed, each row | Open app detail (Installed). | `AppDetail` | push |
| 13 | Enable | a.isGet | Enable (Installed row). | — | in place |
| 14 | Set Up | a.isSetup | Set Up (Installed row). | `AppDetail` | push |
| 15 | Filters | always | Filters: sheet (category, rating, free/paid). | — | in place |
| 16 | Create an app | always | Create an app (same as Build your own). | — | in place |
| 17 | Home | always | Home tab. If already on Home: pop Home's stack to root, then scroll to top. | `Main` | tab |
| 18 | Conversations | always | Conversations tab (keeps its own stack and scroll). Tap again when active: pop to root / scroll to top. | `Conversations` | tab |
| 19 | Tasks | always | Tasks tab. Same re-tap rule. | `Tasks` | tab |
| 20 | Apps | always | Apps tab. Same re-tap rule. | — | tab |
| 21 | Ask Omi | always | Ask Omi: push Chat onto the current tab's stack (Chat is not a tab). | `Chat` | push |


## AppDetail — App detail and setup

`AppDetail.dc.html` · plan IDs **A04** · stage **S11** · presented as **push** · theme: dark + light

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/AppDetail.webp`, `specs/geometry/<device>/AppDetail.json`.

**Purpose.** What an app does, what it needs, and its setup steps in order.

**Reached from.** `Apps`, `Privacy`.

**Data (existing contracts only).** `GET v1/apps/{id}`; reviews in the detail payload; enable/disable; integration OAuth `GET v1/integrations/{app}/oauth-url` then callback; paid apps `GET/DELETE v1/apps/{id}/subscription`; review `POST v1/apps/review?app_id=`.

**States.**

- locked (setup not done): Enable disabled with reason
- can enable
- setup step open (OAuth in progress)
- enabled
- paid app: price row from the server

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | Enable | locked | Disabled Enable (setup incomplete) — VoiceOver explains why. | — | — |
| 2 | Enable | canEnable | Enable the app; read back enabled state. | — | in place |
| 3 | Open Setup | showOpen | Open Setup: start the provider's OAuth in `ASWebAuthenticationSession`; on callback mark the step done. | — | system |
| 4 | See All | always | See all reviews (push list). | — | in place |
| 5 | Back | always | Back (pop). Swipe from the left edge does the same. | `Apps` | pop |
| 6 | Share app | always | Share the app link (system share sheet). | — | system |


## Integrations — Integrations

`Integrations.dc.html` · plan IDs **I01** · stage **S11** · presented as **push (from Settings, Tasks)** · theme: dark + light

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/Integrations.webp`, `specs/geometry/<device>/Integrations.json`.

**Purpose.** Where tasks and context go: one default task destination, plus data sources.

**Reached from.** `Settings`, `TaskEdit`, `Tasks`.

**Data (existing contracts only).** `GET v1/task-integrations`; `PUT v1/task-integrations/default`; connect `GET v1/task-integrations/{app}/oauth-url`; save `PUT v1/task-integrations/{app}`; disconnect `DELETE v1/task-integrations/{app}`; Asana/ClickUp pickers (workspaces/projects, teams/spaces/lists); Apple Health `PUT v1/integrations/apple-health/sync` (HealthKit on device).

**States.**

- connected (default shows a check)
- not connected (Connect)
- connecting (OAuth)
- needs a list/project choice after OAuth (picker sheet)

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | Apple Health Read-only context like sleep and steps | always | Apple Health: HealthKit permission sheet (read-only types), then sync. | — | in place |
| 2 | Reminders On this iPhone | always | Reminders: EventKit permission, then make default. | — | in place |
| 3 | Todoist Not connected | always | Todoist: connect (OAuth) or make default if connected. | — | in place |
| 4 | Asana Not connected | always | Asana: connect, then pick workspace/project. | — | in place |
| 5 | ClickUp Not connected | always | ClickUp: connect, then pick team/space/list. | — | in place |
| 6 | Google Tasks Not connected | always | Google Tasks: connect. | — | in place |
| 7 | Create your own integration | always | Create your own integration → Apps (developer docs link). | `Apps` | push |
| 8 | Back | always | Back (pop). Swipe from the left edge does the same. | `Settings` | pop |


## Call — Active call

`Call.dc.html` · plan IDs **P05** · stage **S12** · presented as **full-screen cover (CallKit in-call UI when locked)** · theme: always dark

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/Call.webp`, `specs/geometry/<device>/Call.json`.

**Purpose.** Omi phone calls: call someone through Omi and capture the conversation.

**Reached from.** `Settings`.

**Data (existing contracts only).** Token `POST v1/phone/token`; audio `/v4/listen?source=phone_call…`; numbers `GET v1/phone/numbers`, verify `POST v1/phone/numbers/verify` + `/verify/check` (Flutter `api/phone_calls.dart`).

**States.**

- connecting
- active (timer from the call start)
- muted
- on speaker
- ended: summary appears later in Conversations

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | Mute | always | Mute (CallKit mute). | — | in place |
| 2 | Keypad | always | Keypad (DTMF pad sheet). | — | in place |
| 3 | Speaker | always | Speaker (audio route picker). | — | in place |
| 4 | End call | always | End call → uncover; conversation processes. | `Main` | push |
| 5 | Minimize | always | Minimize → Home with a green in-call pill; tap to return. | `Main` | uncover |

**Rules and edge cases.**

- Requires caller-number verification first (Settings → Phone calls).
- A timer that animates is not a call: all states come from the call engine.


## Settings — Settings

`Settings.dc.html` · plan IDs **M01** · stage **S12** · presented as **sheet (large) from Home's Account button** · theme: dark + light

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/Settings.webp`, `specs/geometry/<device>/Settings.json`.

**Purpose.** Account, device, capture, privacy and app preferences in one list.

**Reached from.** `Appearance`, `FirstDay`, `Integrations`, `Main`, `Notifications`, `People`, `Plan`, `Privacy`, `Transcription`.

**Data (existing contracts only).** Profile `GET v1/users/profile`; subscription `GET v1/users/me/subscription` + usage `GET v1/users/me/usage?period=`; device from BLE owner; language `GET v1/users/language`; people count `GET v1/users/people`.

**Layout, top to bottom.** Done · search · account row · plan usage row · Device group (pendant, offline recordings, phone calls) · Capture (permissions, transcription, language, your voice, people) · App (appearance, notifications, home screen, conversation display) · Data (privacy, memories, integrations) · Help (help center, what's new, developer) · Sign Out.

**States.**

- loaded from cache instantly; values refresh in place

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | Search settings | always | Search settings (filters rows; results jump to the row and flash it). | — | in place |
| 2 | A Alex Signed in with Apple | always | Account: name, email, sign-in method (push). | — | in place |
| 3 | Free plan 214 of 300 premium minutes left this month | always | Plan & usage (push). | `Plan` | push |
| 4 | Omi pendant 42% | always | Device (push inside the sheet). | `Device` | sheet |
| 5 | Offline recordings 12 min | always | Offline recordings (push). | `Sync` | push |
| 6 | Phone calls | always | Phone calls: numbers + verification (push). Design placeholder links to the call screen. | `Call` | cover |
| 7 | Permissions | always | Permissions (push the same list as onboarding, in settings mode). | `Permissions` | push |
| 8 | Transcription Omi cloud | always | Transcription (push). | `Transcription` | push |
| 9 | Language English | always | Language (push). | `Language` | push |
| 10 | Your voice | always | Your voice: speech profile management (push Voice in settings mode). | `Voice` | push |
| 11 | People 3 | always | People (push). | `People` | push |
| 12 | Appearance System | always | Appearance (push). | `Appearance` | push |
| 13 | Notifications | always | Notifications (push). | `Notifications` | push |
| 14 | Home screen | always | Home screen: choose Home sections (local preference). Placeholder in design. | `Main` | push |
| 15 | Conversation display | always | Conversation display: e.g. show discarded/short conversations (`include_discarded`). Placeholder in design. | `Conversations` | push |
| 16 | Data & privacy | always | Data & privacy (push). | `Privacy` | push |
| 17 | Memories | always | Memories (push). | `Memories` | push |
| 18 | Integrations | always | Integrations (push). | `Integrations` | push |
| 19 | Help center | always | Help center (in-app Safari view). | — | system |
| 20 | What’s new | always | What's new: changelogs `GET v1/announcements/changelogs` (push). | — | in place |
| 21 | Developer | always | Developer: webhooks `v1/users/developer/webhook/*`, API keys (push). | — | in place |
| 22 | Sign Out | always | Sign Out: confirm alert → stop protected work, clear session; local recordings stay bound to this account. | — | system alert |
| 23 | Done | always | Done: dismiss the sheet. | `Main` | dismiss |


## Appearance — Appearance

`Appearance.dc.html` · plan IDs **(new)** · stage **S01** · presented as **push (settings stack)** · theme: dark + light

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/Appearance.webp`, `specs/geometry/<device>/Appearance.json`.

**Purpose.** Theme, app icon and feel.

**Reached from.** `Settings`.

**Data (existing contracts only).** Local only: `overrideUserInterfaceStyle` preference; `setAlternateIconName`; haptics/sounds preferences.

**States.**

- one theme selected
- icon change: system confirmation alert appears (iOS)

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | System | always | Theme: System. | — | in place |
| 2 | Light | always | Theme: Light (whole app cross-fades 0.3 s). | — | in place |
| 3 | Dark | always | Theme: Dark. | — | in place |
| 4 | Default | always | Icon: Default. | — | in place |
| 5 | Dark | always | Icon: Dark. | — | in place |
| 6 | Tinted | always | Icon: Tinted. | — | in place |
| 7 | Clear | always | Icon: Clear. | — | in place |
| 8 | Haptics | always | Haptics on/off. | — | in place |
| 9 | Sounds | always | Sounds on/off. | — | in place |
| 10 | Back | always | Back (pop). Swipe from the left edge does the same. | `Settings` | pop |


## Plan — Plan and usage

`Plan.dc.html` · plan IDs **S16** · stage **S12** · presented as **push (settings stack)** · theme: dark + light

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/Plan.webp`, `specs/geometry/<device>/Plan.json`.

**Purpose.** Current plan, what's left this month, and personal stats.

**Reached from.** `Settings`, `Upgrade`.

**Data (existing contracts only).** `GET v1/users/me/subscription`; `GET v1/users/me/usage?period=today|month|year|all`; fair use `GET v1/fair-use/status`; manage billing `POST v1/payments/customer-portal` (Stripe portal URL); cancel `DELETE v1/payments/subscription`.

**States.**

- free with allowance
- allowance exhausted (distinct from verification failure)
- paid active
- paid cancelling at period end
- desktop plan (Operator/Architect): manage-only, no upgrade CTA (plan-catalog rule 2)

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | See Plans | always | See Plans → Plans sheet. | `Upgrade` | sheet |
| 2 | Today | always | Period Today. | — | in place |
| 3 | Month | always | Period Month. | — | in place |
| 4 | Year | always | Period Year. | — | in place |
| 5 | All | always | Period All. | — | in place |
| 6 | Fair use How premium minutes are balanced | always | Fair use explainer (push a text page with the fair-use status). | — | in place |
| 7 | Use on-device transcription Unlimited and private, slightl… | always | Use on-device transcription → Transcription. | `Transcription` | push |
| 8 | Payment method | always | Payment method / manage billing: open the Stripe customer portal URL in an in-app Safari view. | — | system |
| 9 | Back | always | Back (pop). Swipe from the left edge does the same. | `Settings` | pop |
| 10 | Share stats | always | Share stats image (system share sheet). | — | system |


## Upgrade — Plans

`Upgrade.dc.html` · plan IDs **M51** · stage **S12** · presented as **sheet (large)** · theme: dark + light

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/Upgrade.webp`, `specs/geometry/<device>/Upgrade.json`.

**Purpose.** Compare and choose Plus or Unlimited (iOS storefront), or stay Free.

**Reached from.** `Plan`, `Privacy`.

**Data (existing contracts only).** Same as OnboardPlan: `GET v1/payments/available-plans`; new subscription `POST v1/payments/checkout-session` → Stripe Checkout; existing paid subscriber changing tier `POST v1/payments/upgrade-subscription`; training-data program `GET/POST v1/users/training-data-opt-in`.

**States.**

- current plan marked
- annual Continue hidden when already on that exact annual price (plan-catalog rule 3)
- checkout open
- success: sheet dismisses, Plan screen refreshes

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | Close | always | Close. | `Plan` | dismiss |
| 2 | Monthly | always | Monthly prices. | — | in place |
| 3 | Yearly | always | Yearly prices. | — | in place |
| 4 | Unlimited ‹dynamic› Unlimited premium transcription 1,000 … | always | Select Unlimited (card expands; bouncy check). | — | in place |
| 5 | Plus ‹dynamic› 25 hours of premium transcription a month 2… | always | Select Plus. | — | in place |
| 6 | Free Current Included 300 minutes of premium transcription… | always | Select Free (current). | — | in place |
| 7 | Get Unlimited free Contribute anonymized data to help trai… | always | Get Unlimited free: training-data program explainer → opt in (`POST v1/users/training-data-opt-in`). | — | in place |
| 8 | Promo code | always | Promo code: alert with field; passed as `promotion_code`. | — | system |
| 9 | ‹dynamic› | always | Continue: paid → checkout session (new) or upgrade-subscription (existing); Free → close. | `Plan` | push |


## Privacy — Data and privacy

`Privacy.dc.html` · plan IDs **S19** · stage **S12** · presented as **push (settings stack)** · theme: dark + light

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/Privacy.webp`, `specs/geometry/<device>/Privacy.json`.

**Purpose.** Where audio lives, which apps see what, export/import, delete.

**Reached from.** `DeleteAccount`, `Settings`.

**Data (existing contracts only).** Phone audio `GET/POST v1/users/store-recording-permission`, delete recordings `DELETE v1/users/store-recording-permission`; private cloud `v1/users/private-cloud-sync`; export `GET v1/users/export`; import `GET v1/import/limitless?language=` + `GET v1/import/jobs`; training program `v1/users/training-data-opt-in`.

**States.**

- values from server
- export preparing (progress) → share sheet with file
- import job running (status row)

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | Keep audio on this iPhone | always | Keep audio on this iPhone. Toggle; write first, show the new value only after the server/device accepts it (keep the old value + error toast on failure). | — | in place |
| 2 | Keep audio in your private cloud | always | Keep audio in private cloud. Toggle; write first, show the new value only after the server/device accepts it (keep the old value + error toast on failure). | — | in place |
| 3 | Google Drive Transcripts and summaries · when a conversati… | always | App with access: Google Drive → its detail. | `AppDetail` | push |
| 4 | Conversation Coach Transcripts · when you chat with it | always | App with access: Conversation Coach → its detail. | `AppDetail` | push |
| 5 | Export all conversations | always | Export all conversations: prepare file, then system share sheet. | — | system |
| 6 | Import history From a Limitless export | always | Import history from a Limitless export: document picker → start import job → status row. | — | in place |
| 7 | Training data program Off | always | Training data program → Plans sheet section. | `Upgrade` | sheet |
| 8 | Delete Account | always | Delete Account (push). | `DeleteAccount` | push |
| 9 | Back | always | Back (pop). Swipe from the left edge does the same. | `Settings` | pop |


## People — People

`People.dc.html` · plan IDs **S15** · stage **S09** · presented as **push (settings stack)** · theme: dark + light

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/People.webp`, `specs/geometry/<device>/People.json`.

**Purpose.** People Omi recognises by voice, and their samples.

**Reached from.** `Settings`, `Speaker`.

**Data (existing contracts only).** `GET v1/users/people?include_speech_samples=true`; create `POST v1/users/people`; rename `…/people/{id}/name?value=`; delete `DELETE v1/users/people/{id}`; delete sample `DELETE …/speech-samples/{idx}`.

**States.**

- list
- empty
- person expanded (samples with play)

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | C Chitapa 2 samples · from calls and conversations | always | Open Chitapa (samples, rename, delete). | — | in place |
| 2 | J Jordan 2 samples | always | Open Jordan. | — | in place |
| 3 | Recognize people automatically | always | Recognise people automatically (local/profile preference Flutter uses). Toggle; write first, show the new value only after the server/device accepts it (keep the old value + error toast on failure). | — | in place |
| 4 | Back | always | Back (pop). Swipe from the left edge does the same. | `Settings` | pop |
| 5 | Add person | always | Add person: name sheet → `POST v1/users/people`. | — | in place |


## Notifications — Notifications

`Notifications.dc.html` · plan IDs **S13** · stage **S12** · presented as **push (settings stack)** · theme: dark + light

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/Notifications.webp`, `specs/geometry/<device>/Notifications.json`.

**Purpose.** How proactive Omi is, and the daily recap.

**Reached from.** `Settings`.

**Data (existing contracts only).** Frequency 0–5 `GET/PATCH v1/users/mentor-notification-settings` (0 = off … 5 = maximum); recap `GET/PATCH v1/users/daily-summary-settings` (hour); iOS authorization status.

**States.**

- system notifications off: banner “Turn on in Settings” on top, options dimmed
- one frequency selected

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | Off Only what you ask for | always | Off (0). | — | in place |
| 2 | Minimal Just daily recaps | always | Minimal (1). | — | in place |
| 3 | Low Important follow-ups | always | Low (2). | — | in place |
| 4 | Balanced Follow-ups and timely nudges | always | Balanced (3). | — | in place |
| 5 | High More suggestions during the day | always | High (4). | — | in place |
| 6 | Maximum Everything Omi notices | always | Maximum (5). | — | in place |
| 7 | Daily recap | always | Daily recap on/off (+ time row when on). Toggle; write first, show the new value only after the server/device accepts it (keep the old value + error toast on failure). | — | in place |
| 8 | Back | always | Back (pop). Swipe from the left edge does the same. | `Settings` | pop |


## Transcription — Transcription

`Transcription.dc.html` · plan IDs **S08** · stage **S06** · presented as **push (settings stack)** · theme: dark + light

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/Transcription.webp`, `specs/geometry/<device>/Transcription.json`.

**Purpose.** Where transcription happens and how.

**Reached from.** `Plan`, `Settings`.

**Data (existing contracts only).** `GET/PATCH v1/users/transcription-preferences`; language `v1/users/language`; custom vocabulary within transcription preferences (as Flutter).

**States.**

- Omi cloud / On-device / Custom selected
- on-device model downloading (progress)

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | Omi cloud | always | Omi cloud. | — | in place |
| 2 | On-device | always | On-device (unlimited, private). | — | in place |
| 3 | Custom | always | Custom (bring your own provider — shows fields). | — | in place |
| 4 | Language English | always | Language (push). | `Language` | push |
| 5 | Detect other languages | always | Detect other languages. Toggle; write first, show the new value only after the server/device accepts it (keep the old value + error toast on failure). | — | in place |
| 6 | Add word | always | Add custom word (alert with field). | — | in place |
| 7 | Transcribe later | always | Transcribe later (batch after capture). Toggle; write first, show the new value only after the server/device accepts it (keep the old value + error toast on failure). | — | in place |
| 8 | Back | always | Back (pop). Swipe from the left edge does the same. | `Settings` | pop |


## DeleteAccount — Delete account

`DeleteAccount.dc.html` · plan IDs **S24** · stage **S12** · presented as **push (settings stack)** · theme: dark + light

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/DeleteAccount.webp`, `specs/geometry/<device>/DeleteAccount.json`.

**Purpose.** Irreversible account deletion with a clear list of consequences and an export first.

**Reached from.** `Privacy`.

**Data (existing contracts only).** `DELETE v1/users/delete-account`; export `GET v1/users/export`.

**States.**

- confirm text empty: Delete disabled
- typed DELETE: enabled
- deleting
- deleted: sign out → Welcome (replace root)

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | Export your data first | always | Export your data first → Privacy export. | `Privacy` | push |
| 2 | Type DELETE to confirm | always | Confirmation field (exact “DELETE”). | — | in place |
| 3 | Back | always | Back (pop). Swipe from the left edge does the same. | `Privacy` | pop |
| 4 | Delete Account | ok | Delete Account: final destructive alert → delete → replace root with Welcome. | — | system alert |
| 5 | Delete Account | notOk | Disabled Delete Account (until typed). | — | — |
| 6 | Keep My Account | always | Keep My Account (pop). | `Privacy` | push |

**Rules and edge cases.**

- Never exercise on a real user's device account in tests.

