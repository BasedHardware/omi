# Tasks, Ask Omi and memories

Generated by `spec/generate.py` from the artboards and `spec/content_*.py`. Read `spec/README.md` first for the vocabulary (presentation, transitions, gestures) and global rules.

## Tasks — Tasks and goals

`Tasks.dc.html` · plan IDs **C12** · stage **S08** · presented as **tab root (Tasks)** · theme: dark + light

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/Tasks.webp`, `specs/geometry/<device>/Tasks.json`.

**Purpose.** Everything the user said they would do, in due-date buckets, with fast capture.

**Reached from.** `Apps`, `Conversations`, `FirstDay`, `Main`, `Offline`, `TaskEdit`.

**Data (existing contracts only).** `GET v1/action-items?limit&offset&include_completed`; create `POST v1/action-items` (supports `Idempotency-Key`); update `PATCH v1/action-items/{id}`; complete `PATCH …/{id}/completed`; delete `DELETE …/{id}`; batch `PATCH v1/action-items/batch`; goals `GET v1/goals`, `PATCH v1/goals/{id}/progress`.

**Layout, top to bottom.** Search · Export destination · More · large title · goal ring card · duplicate suggestion (only when present) · buckets: Overdue, Today, Tomorrow, This week, Later, No date (always all buckets that have items — INV-TASK-1) · quick-add bar above the tab bar.

**States.**

- loading (cached first)
- empty: “Nothing to do” + how tasks arrive
- duplicate suggestion shown
- completed task: strike → leaves after 0.6 s → Undo toast
- offline: edits queue locally with a clock icon until confirmed

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | Keep One | showDup | Keep One: complete/delete the duplicate the user did not keep (explicit choice; never automatic). | — | in place |
| 2 | Keep Both | showDup | Keep Both: dismiss the suggestion for this pair. | — | in place |
| 3 | ‹dynamic› | each row, each row | Complete/uncomplete this task (checkbox pops, bouncy; success haptic). | — | in place |
| 4 | ‹dynamic› | always | Open this task in TaskEdit (sheet). | `TaskEdit` | sheet |
| 5 | Search tasks | always | Search tasks: field expands in the toolbar; `GET v1/action-items/search`. | — | in place |
| 6 | Export destination | always | Export destination → Integrations (push). | `Integrations` | push |
| 7 | More | always | More: Show completed, Sort, Select. | — | in place |
| 8 | New task | always | Quick-add field (rides the keyboard). | — | in place |
| 9 | Add task | hasDraft | Add: `POST v1/action-items` with a fresh idempotency key; row inserts into its bucket (rise). Keep the draft if it fails. | — | in place |
| 10 | Home | always | Home tab. If already on Home: pop Home's stack to root, then scroll to top. | `Main` | tab |
| 11 | Conversations | always | Conversations tab (keeps its own stack and scroll). Tap again when active: pop to root / scroll to top. | `Conversations` | tab |
| 12 | Tasks | always | Tasks tab. Same re-tap rule. | — | tab |
| 13 | Apps | always | Apps tab. Same re-tap rule. | `Apps` | tab |
| 14 | Ask Omi | always | Ask Omi: push Chat onto the current tab's stack (Chat is not a tab). | `Chat` | push |
| 15 | Undo | toastOn | Undo the last completion/deletion (re-open via `PATCH …/completed` false; for delete only if the server supports restore). | — | in place |

**Gestures and motion.**

- Swipe right on a task: complete.
- Swipe left: Snooze (move due date) and Delete.
- Long-press + drag: reorder within a bucket.
- Pull to refresh.


## TaskEdit — Edit task

`TaskEdit.dc.html` · plan IDs **M34** · stage **S08** · presented as **sheet (large)** · theme: dark + light

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/TaskEdit.webp`, `specs/geometry/<device>/TaskEdit.json`.

**Purpose.** Edit one task: text, notes, due date, source, where it is exported.

**Reached from.** `Conversation`, `Tasks`.

**Data (existing contracts only).** `PATCH v1/action-items/{id}`; delete `DELETE v1/action-items/{id}`; source conversation id from the task.

**States.**

- clean: Save disabled
- dirty: Save enabled
- saving
- failed: stay, keep edits, error
- dismiss with unsaved edits: confirmation (Discard / Keep Editing)

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | Cancel | always | Cancel (confirm if dirty). | `Tasks` | dismiss |
| 2 | Save | always | Save, then dismiss; the row updates in place. | `Tasks` | push |
| 3 | Task | always | Task text. | — | in place |
| 4 | Notes | always | Notes. | — | in place |
| 5 | Today | always | Due today. | — | in place |
| 6 | Tomorrow | always | Due tomorrow. | — | in place |
| 7 | Next week | always | Due next week. | — | in place |
| 8 | No date | always | No due date. | — | in place |
| 9 | Call Chitapa reminder Today at 3:14 PM · 14 s | always | Open the source conversation (dismiss the sheet, push Conversation on the current tab). | `Conversation` | push |
| 10 | Change default app | always | Change default export app → Integrations. | `Integrations` | push |
| 11 | Delete Task | always | Delete Task: destructive alert → delete → dismiss. | — | system alert |

**Required in production but not drawn.**

- Custom date/time: the due row opens a graphical DatePicker inline.


## Chat — Ask Omi

`Chat.dc.html` · plan IDs **C09 · M38 · M39** · stage **S10** · presented as **push (from the Ask button on any tab, or scoped from a conversation)** · theme: dark + light

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/Chat.webp`, `specs/geometry/<device>/Chat.json`.

**Purpose.** Ask anything about your conversations and memory; answers cite sources.

**Reached from.** `Apps`, `Conversation`, `Conversations`, `FirstDay`, `Main`, `Offline`, `Search`, `Tasks`, `VoiceAsk`.

**Data (existing contracts only).** History `GET v2/messages?app_id=`; send (streaming) `POST v2/messages?app_id=` with `think:`, `data:`, `done:`, `message:`, `error:` events; files `POST v2/files?app_id=`; rating `PATCH v2/messages/{id}/rating`; report `POST v2/messages/{id}/report`; clear `DELETE v2/messages?app_id=`; initial message `POST v2/initial-message?app_id=`; installed chat apps `GET v1/apps/enabled`.

**Layout, top to bottom.** Back · assistant switcher (Omi ▾) · New chat · More · messages (answer text, source cards, actions: Copy, Helpful, Not helpful, Share) · suggestion chips · composer (+, field, Send / Voice).

**States.**

- empty thread: greeting + suggestions
- sending: user bubble appears, then thinking row (live `think:` text)
- streaming: text grows with a soft caret; sources appear when `done:` lands
- error after partial text: keep text, show “Answer stopped — Retry”
- offline: composer disabled with reason; history readable

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | ‹dynamic› | m.isOmi, each row | Source card → the cited conversation (push). | `Conversation` | push |
| 2 | Copy | always | Copy the answer. | — | in place |
| 3 | Helpful | always | Rate helpful (`PATCH v2/messages/{id}/rating` value 1; toggles off with 0). | — | in place |
| 4 | Not helpful | always | Rate not helpful (value -1) and offer Report. | — | in place |
| 5 | Share | always | Share the answer text (system share sheet). | — | system |
| 6 | ‹dynamic› | canSuggest | Suggestion 1: send it. | — | in place |
| 7 | ‹dynamic› | canSuggest | Suggestion 2: send it. | — | in place |
| 8 | ‹dynamic› | canSuggest | Suggestion 3: send it. | — | in place |
| 9 | Back | always | Back (pop). Swipe from the left edge does the same. | `Main` | pop |
| 10 | ‹dynamic› | always | Assistant switcher: open the chat-app picker. | — | in place |
| 11 | New chat | always | New chat: start a fresh session (history remains available). | — | in place |
| 12 | More | always | More: Clear chat (confirm; `DELETE v2/messages?app_id=`), Report a problem. | — | in place |
| 13 | Add photo or file | always | Attach photo or file (PHPicker / document picker; upload shows progress on the chip). | — | in place |
| 14 | Message Omi | always | Message field (keeps the draft if the user leaves). | — | in place |
| 15 | Send | hasDraft | Send (disabled while a send is in flight; never auto-resend an ambiguous request). | — | in place |
| 16 | Voice mode | noDraft | Voice mode → VoiceAsk (cover growing from the mic button). | `VoiceAsk` | cover |
| 17 | Close | asstOpen | Close the picker. | — | in place |
| 18 | Omi Your conversations and memory | asstOpen | Pick Omi (default assistant). | — | in place |
| 19 | Notion Helper An installed chat app | always | Pick an installed chat app (switches `app_id`; thread reloads for that app). | — | in place |
| 20 | Find chat apps | always | Find chat apps → Apps filtered to chat capability. | `Apps` | push |

**Gestures and motion.**

- Composer rides the keyboard; thread keeps the last message visible.
- Long-press a message: Copy, Share, Report.


## VoiceAsk — Voice mode

`VoiceAsk.dc.html` · plan IDs **C09** · stage **S10** · presented as **full-screen cover** · theme: always dark

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/VoiceAsk.webp`, `specs/geometry/<device>/VoiceAsk.json`.

**Purpose.** Talk to Omi hands-free; answer is spoken and shown.

**Reached from.** `Chat`.

**Data (existing contracts only).** `WEBSOCKET v2/voice-message/transcribe-stream` / `GET v2/voice-messages` streaming (Flutter `sendVoiceMessageStreamServer`); TTS `POST v2/tts/synthesize`.

**States.**

- listening (ring follows real level)
- thinking
- speaking (answer text scrolls)
- interrupted by a call/Siri: pause and say so

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | Close | always | Close (uncover) back to Chat; the exchange is in the thread. | `Chat` | uncover |
| 2 | Show text | always | Show/hide the text of the answer. | — | in place |
| 3 | Done | always | Done (same as Close after the answer). | `Chat` | push |

**Rules and edge cases.**

- Shares the single audio session owner with capture; if the pendant is capturing, voice mode uses phone mic only after the user confirms.


## Memories — Memories

`Memories.dc.html` · plan IDs **C10** · stage **S09** · presented as **push (from Home memories card, Settings)** · theme: dark + light

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/Memories.webp`, `specs/geometry/<device>/Memories.json`.

**Purpose.** What Omi remembers, by tier (Short-term, Long-term, Archive) and category.

**Reached from.** `Graph`, `Main`, `MemoryDetail`, `Settings`.

**Data (existing contracts only).** `GET v3/memories` (paged/cursor as Flutter); create `POST v3/memories`; categories system (About you) / interesting (Insights) / manual (Manual); tiers short_term / long_term / archive.

**States.**

- loading (cached first)
- empty per filter
- archive collapsed row with count

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | You Omi app Subscriptions Battery Chitapa Pricing Expand | always | Graph preview → Memory graph (push). | `Graph` | push |
| 2 | All | always | Filter All. | — | in place |
| 3 | About you | always | Filter About you (category system). | — | in place |
| 4 | Insights | always | Filter Insights (interesting). | — | in place |
| 5 | Manual | always | Filter Manual (manual). | — | in place |
| 6 | ‹dynamic› · ‹dynamic› | each row | Open this memory (push MemoryDetail). | `MemoryDetail` | push |
| 7 | Archive Kept, but not used in answers unless you ask 18 | always | Archive → the archive list (push, same screen filtered to archive tier). | — | in place |
| 8 | Back | always | Back (pop). Swipe from the left edge does the same. | `Main` | pop |
| 9 | Search memories | always | Search memories (field expands in the toolbar). | — | in place |
| 10 | New memory | always | New memory → MemoryDetail in create mode (sheet). | `MemoryDetail` | sheet |
| 11 | Manage memories | always | Manage: sheet with Delete all memories (`DELETE v3/memories`, typed confirmation) and export. | — | in place |

**Gestures and motion.**

- Swipe left on a memory: Archive, Delete (toast + Undo only where the server can revert).
- Long-press: Copy, Make private, Delete.

**Rules and edge cases.**

- Users cannot fast-track a memory to Long-term (product invariant); only the server promotes tiers.


## MemoryDetail — Memory

`MemoryDetail.dc.html` · plan IDs **M32** · stage **S09** · presented as **push (existing) / sheet (new)** · theme: dark + light

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/MemoryDetail.webp`, `specs/geometry/<device>/MemoryDetail.json`.

**Purpose.** Read and edit one memory, see where it came from, control how it is used.

**Reached from.** `Memories`.

**Data (existing contracts only).** Edit `PATCH v3/memories/{id}`; revert `POST v3/memories/{id}/revert`; use in answers `POST v3/memories/{id}/use`; visibility `PATCH v3/memories/{id}/visibility?value=`; baseline `PATCH v3/memories/{id}/baseline?value=`; review `POST …/review?value=`; delete `DELETE v3/memories/{id}`.

**States.**

- clean
- editing (Save enabled)
- saving
- saved (check fades in next to the title)
- failed: keep text

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | Cancel | always | Cancel (discard edits, confirm if dirty). | `Memories` | dismiss |
| 2 | Save | always | Save the edit (`PATCH v3/memories/{id}`). | `Memories` | push |
| 3 | Memory | always | Memory text. | — | in place |
| 4 | App UX and battery concerns Today at 2:42 PM · recorded on… | always | Source conversation → push Conversation. | `Conversation` | push |
| 5 | Use in answers | always | Use in answers toggle (`POST …/use`). Toggle; write first, show the new value only after the server/device accepts it (keep the old value + error toast on failure). | — | in place |
| 6 | Private | always | Private toggle (`PATCH …/visibility?value=private\|public`). Toggle; write first, show the new value only after the server/device accepts it (keep the old value + error toast on failure). | — | in place |
| 7 | Pin as baseline | always | Pin as baseline (`PATCH …/baseline?value=`). Toggle; write first, show the new value only after the server/device accepts it (keep the old value + error toast on failure). | — | in place |
| 8 | Delete Memory | always | Delete Memory: destructive alert → `DELETE` → pop with toast. | — | system alert |


## Graph — Memory graph

`Graph.dc.html` · plan IDs **C11** · stage **S09** · presented as **push** · theme: dark + light

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/Graph.webp`, `specs/geometry/<device>/Graph.json`.

**Purpose.** People and topics Omi connects, drawn from the real knowledge graph.

**Reached from.** `Memories`.

**Data (existing contracts only).** `GET v1/knowledge-graph` (nodes + edges as returned; never invent links).

**States.**

- loading (nodes settle with gentle springs)
- empty
- large graph: cluster + zoom
- failed: show the accessible list version with retry

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | You | always | Select You. | — | in place |
| 2 | Omi app | always | Select Omi app. | — | in place |
| 3 | Subscriptions | always | Select Subscriptions. | — | in place |
| 4 | Battery drain | always | Select Battery drain. | — | in place |
| 5 | Chitapa | always | Select Chitapa. | — | in place |
| 6 | Jordan | always | Select Jordan. | — | in place |
| 7 | Pricing | always | Select Pricing. | — | in place |
| 8 | UX consistency | always | Select UX consistency. | — | in place |
| 9 | Running | always | Select Running. | — | in place |
| 10 | Back | always | Back (pop). Swipe from the left edge does the same. | `Memories` | pop |
| 11 | Share | always | Share an image of the graph (system share sheet). | — | system |

**Required in production but not drawn.**

- Selecting a node shows a bottom card: its memories and conversations (tap → detail).
- Accessible list alternative (VoiceOver rotor + a List toggle).

**Gestures and motion.**

- Pinch to zoom, drag to pan, double-tap to recentre.

