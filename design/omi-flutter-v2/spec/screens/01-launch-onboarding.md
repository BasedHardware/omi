# Launch and onboarding

Generated by `spec/generate.py` from the artboards and `spec/content_*.py`. Read `spec/README.md` first for the vocabulary (presentation, transitions, gestures) and global rules.

## Startup — Startup failure

`Startup.dc.html` · plan IDs **L01** · stage **S02** · presented as **root (replaces app shell)** · theme: always dark

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/Startup.webp`, `specs/geometry/<device>/Startup.json`.

**Purpose.** Shown only when the app cannot reach a usable state at launch (config/auth bootstrap failed). Never shown for a slow network: a returning user with cached data goes straight to Home and loads in place.

**Reached from.** —.

**Data (existing contracts only).** None of its own. Retry re-runs the launch sequence: Keychain session restore → `GET v1/users/profile` (optional; cached first) → onboarding state `GET v1/users/onboarding`.

**States.**

- idle: message + Try Again
- retrying: button shows spinner + “Starting…”, disabled
- success: root is replaced by Home or onboarding (cross-fade 0.3 s)

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | Try Again | idle | Re-run launch bootstrap. Disable while running; on success replace root; on failure return to idle with the new error (one attempt per tap, no auto-loop). | — | in place |
| 2 | Contact Support | always | Open support: `mailto:` or the help-center URL the Flutter app uses, in an in-app Safari view. Include app version + anonymous diagnostics id, never tokens. | — | system |

**Rules and edge cases.**

- Distinguish: no network (offer Retry) vs server 5xx (Retry) vs invalid build config (Contact Support only).
- Keep existing Keychain session and local recordings; never sign out from this screen.


## Gate — Update / migration gate

`Gate.dc.html` · plan IDs **L02** · stage **S02** · presented as **root (blocking)** · theme: always dark

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/Gate.webp`, `specs/geometry/<device>/Gate.json`.

**Purpose.** Blocks the app when the account requires an app update or a data-protection migration before it can be used safely.

**Reached from.** —.

**Data (existing contracts only).** Migration: `GET/POST v1/users/migration/requests`, `POST v1/users/migration/batch-requests`, `POST v1/users/migration/requests/data-protection-level/finalize` (Flutter `api/users.dart`). Update: App Store version check the Flutter app uses.

**States.**

- update required: primary = “Update Omi” (opens App Store page)
- migration required: primary = “Continue”, then progress with counts
- migration running: progress bar fills from real counts; the screen cannot be dismissed
- failed: error + primary “Try Again”
- done: cross-fade into Home

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | ‹dynamic› | always | Primary (label from state): Update → open the App Store product page; Migration → start/continue the migration job and show real progress; Failed → retry the failed step only. | — | in place |
| 2 | Check Again | always | Re-check gate status from the server; if cleared, continue into the app. | — | in place |

**Rules and edge cases.**

- Never let the user into the app half-migrated.
- Migration progress survives app termination: resume from the server's request state, not from 0.


## Welcome — Welcome

`Welcome.dc.html` · plan IDs **L03** · stage **S02** · presented as **root of the onboarding stack** · theme: always dark

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/Welcome.webp`, `specs/geometry/<device>/Welcome.json`.

**Purpose.** First screen for a signed-out user. Brand moment (pendant rises out of its box, LED fades on) and two ways in.

**Reached from.** `SignIn`.

**Data (existing contracts only).** None.

**States.**

- default only. Entrance: pendant rises 0.8 s gentle spring, then LED fades in, then text and buttons rise with 60 ms stagger.

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | Get Started | always | Go to Sign in (new account path). | `SignIn` | push |
| 2 | Sign in | always | Go to Sign in (returning account path; same screen, same providers). | `SignIn` | push |

**Gestures and motion.**

- Reduce Motion: pendant and text appear with a 0.3 s fade, LED already on.


## SignIn — Sign in

`SignIn.dc.html` · plan IDs **O01** · stage **S02** · presented as **push (onboarding stack)** · theme: always dark

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/SignIn.webp`, `specs/geometry/<device>/SignIn.json`.

**Purpose.** Authenticate with Apple or Google. Nothing else on this screen.

**Reached from.** `Consent`, `Welcome`.

**Data (existing contracts only).** Firebase Auth (Apple, Google) exactly as Flutter `app/lib/services/auth_service.dart`; then `GET v1/users/onboarding` decides where to go.

**States.**

- idle
- Apple in progress: that button shows spinner + “Signing in…”, both buttons disabled
- Google in progress: same
- cancelled by user: back to idle, no error
- failed: inline error under the buttons, both enabled

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | Sign in with Apple | appleIdle | Start Sign in with Apple (ASAuthorizationController). On success → onboarding state: completed → Home (replace root); not completed → Consent. | — | in place |
| 2 | Continue with Google | googleIdle | Start Google sign-in (ASWebAuthenticationSession / Google SDK as Flutter does). Same routing as Apple. | — | in place |
| 3 | Terms of Service | always | Open Terms of Service URL (Flutter uses `omi.me/pages/terms-of-service`) in an in-app Safari view. | `Consent` | system |
| 4 | Privacy Policy | always | Open Privacy Policy URL (`omi.me/pages/privacy`) in an in-app Safari view. | `Consent` | system |
| 5 | Back | always | Back to Welcome. Cancels an in-flight sign-in first. | `Welcome` | pop |

**Rules and edge cases.**

- Duplicate callback or double tap → one sign-in operation.
- If a different account is already signed in on this device, stop protected work and clear its in-memory data before loading the new account; keep the other account's owner-bound recordings on disk.


## Consent — Data and AI consent

`Consent.dc.html` · plan IDs **O02** · stage **S02** · presented as **push (onboarding stack)** · theme: dark + light

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/Consent.webp`, `specs/geometry/<device>/Consent.json`.

**Purpose.** Plain-words explanation of what is stored, who processes it and what the user controls. Step 1 of 8.

**Reached from.** `Name`, `OnboardPlan`, `SignIn`.

**Data (existing contracts only).** Consent acknowledgement is part of onboarding progress (Flutter `ai_consent_widget.dart`). No new endpoint.

**Layout, top to bottom.** progress bar (1/8) · title · three explainer cards (What's stored / Who processes it / What you control) · Read the Privacy Policy · footer: Agree and Continue, Use a Different Account.

**States.**

- default
- returning mid-onboarding: opens at the step the server says is next

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | Read the Privacy Policy | always | Open the privacy policy (`omi.me/pages/privacy`) in an in-app Safari view. | — | system |
| 2 | Agree and Continue | always | Record consent, then go to Name. Progress bar segment 2 fills (0.45 s spring). | `Name` | push |
| 3 | Use a Different Account | always | Sign out of this account and return to Sign in (pop to Sign in). Confirm nothing is lost: no data exists yet. | `SignIn` | push |


## Name — Name

`Name.dc.html` · plan IDs **O03** · stage **S02** · presented as **push (onboarding stack)** · theme: dark + light

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/Name.webp`, `specs/geometry/<device>/Name.json`.

**Purpose.** What Omi calls the user. Step 2 of 8.

**Reached from.** `Consent`, `Language`.

**Data (existing contracts only).** Same as Flutter `AuthService.updateGivenName`: Firebase display name + local given/family name. Pre-fill from the Apple/Google name.

**States.**

- empty: Continue disabled
- valid (≥1 non-space char): Continue enabled
- saving: Continue spinner
- save failed: inline error, text kept

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | Your name | always | Text field. Keyboard opens on appear (not during the push animation: focus after the transition ends). Return key = Continue. | — | in place |
| 2 | Back | always | Back to Consent. | `Consent` | pop |
| 3 | Continue | ok | Save name, then go to Language. | `Language` | push |
| 4 | Continue | notOk | Disabled Continue (shown while the field is empty). Not tappable; VoiceOver hint “Enter your name to continue”. | — | — |


## Language — Primary language

`Language.dc.html` · plan IDs **O04** · stage **S02** · presented as **push (onboarding stack)** · theme: dark + light

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/Language.webp`, `specs/geometry/<device>/Language.json`.

**Purpose.** Primary spoken language for transcription. Step 3 of 8.

**Reached from.** `Name`, `Settings`, `Source`, `Transcription`.

**Data (existing contracts only).** List: `GET v1/users/available-languages`. Save: `PATCH v1/users/language`. Current: `GET v1/users/language`.

**States.**

- loading list (skeleton rows)
- list loaded; current language checked
- search with no match: “No languages match” + clear
- list failed: retry row; English still selectable offline

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | Search languages | always | Search field filters the server list by native and English name (client-side over the full list). | — | in place |
| 2 | English English | always | Select English (checkmark pops with bouncy spring; previous check fades). | — | in place |
| 3 | 中文（普通话） Mandarin Chinese | always | Select Mandarin Chinese. | — | in place |
| 4 | 繁體中文 Traditional Chinese | always | Select Traditional Chinese. | — | in place |
| 5 | Español Spanish | always | Select Spanish. | — | in place |
| 6 | हिन्दी Hindi | always | Select Hindi. | — | in place |
| 7 | Français French | always | Select French. | — | in place |
| 8 | Deutsch German | always | Select German. | — | in place |
| 9 | 日本語 Japanese | always | Select Japanese. | — | in place |
| 10 | Português Portuguese | always | Select Portuguese. | — | in place |
| 11 | Back | always | Back to Name. | `Name` | pop |
| 12 | Continue | always | Save the selected language (`PATCH v1/users/language`), then go to Source. On failure stay, show error, keep selection. | `Source` | push |

**Required in production but not drawn.**

- Rows come from the server list, not the nine drawn. Keep the drawn row design for every entry.


## Source — How did you hear about Omi

`Source.dc.html` · plan IDs **O05** · stage **S02** · presented as **push (onboarding stack)** · theme: dark + light

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/Source.webp`, `specs/geometry/<device>/Source.json`.

**Purpose.** Optional attribution. Step 4 of 8. Always skippable.

**Reached from.** `Language`, `Permissions`.

**Data (existing contracts only).** `PATCH v1/users/onboarding` with `acquisition_source` (Flutter `updateUserOnboardingState`).

**States.**

- nothing picked: Continue disabled, Skip visible
- one chip picked (single choice)
- Other picked: text field appears (rise 0.3 s) and gets focus

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | A friend | always | Pick “A friend”. | — | in place |
| 2 | YouTube | always | Pick “YouTube”. | — | in place |
| 3 | X (Twitter) | always | Pick “X (Twitter)”. | — | in place |
| 4 | TikTok | always | Pick “TikTok”. | — | in place |
| 5 | Instagram | always | Pick “Instagram”. | — | in place |
| 6 | Reddit | always | Pick “Reddit”. | — | in place |
| 7 | LinkedIn | always | Pick “LinkedIn”. | — | in place |
| 8 | Google search | always | Pick “Google search”. | — | in place |
| 9 | An event | always | Pick “An event”. | — | in place |
| 10 | Other | always | Pick “Other” (reveals the free-text field). | — | in place |
| 11 | Where did you hear about Omi | isOther | Free text for Other (max ~100 chars). | — | in place |
| 12 | Back | always | Back to Language. | `Language` | pop |
| 13 | Continue | always | Save `acquisition_source` (fire-and-forget is acceptable: failure never blocks onboarding), go to Permissions. | `Permissions` | push |
| 14 | Skip | always | Skip without saving, go to Permissions. | `Permissions` | push |


## Permissions — Permissions

`Permissions.dc.html` · plan IDs **O06** · stage **S02** · presented as **push (onboarding stack)** · theme: dark + light

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/Permissions.webp`, `specs/geometry/<device>/Permissions.json`.

**Purpose.** Ask for each permission in context with a one-line reason. Step 5 of 8. Continue is always enabled — nothing is mandatory here.

**Reached from.** `Settings`, `Source`, `Voice`.

**Data (existing contracts only).** iOS permission APIs only: AVAudioSession record permission, CBCentralManager (Bluetooth), UNUserNotificationCenter, CLLocationManager (when-in-use). Push token → `POST v1/users/fcm-token` after notification grant.

**States.**

- per row: not asked (Allow button) → granted (check, row dims) → denied (“Open Settings” link)
- system prompt visible (the drawn alert is the real iOS prompt — do not rebuild it)

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | Allow | mic.pending | Microphone: trigger the iOS microphone prompt. | — | system |
| 2 | Open Settings | mic.denied | Microphone denied: open this app's page in iOS Settings (`UIApplication.openSettingsURLString`). Re-check when the app returns to foreground. | — | system |
| 3 | Allow | bt.pending | Bluetooth: trigger the iOS Bluetooth prompt (instantiate the central manager only now). | — | system |
| 4 | Open Settings | bt.denied | Bluetooth denied: open iOS Settings; re-check on return. | — | system |
| 5 | Allow | notif.pending | Notifications: request authorization; on grant register for remote notifications and send the token. | — | system |
| 6 | Open Settings | notif.denied | Notifications denied: open iOS Settings; re-check on return. | — | system |
| 7 | Allow | loc.pending | Location: request when-in-use authorization (used only to tag conversations with a place). | — | system |
| 8 | Open Settings | loc.denied | Location denied: open iOS Settings; re-check on return. | — | system |
| 9 | Back | always | Back to Source. | `Source` | pop |
| 10 | Continue | always | Continue to Voice regardless of what was granted. | `Voice` | push |
| 11 | Don’t Allow | ask_mic | (Drawn iOS prompt) Don't Allow — system UI, not app UI. | — | system |
| 12 | Allow | ask_mic | (Drawn iOS prompt) Allow — system UI. | — | system |
| 13 | Don’t Allow | ask_bt | (Drawn iOS prompt) Don't Allow — system UI. | — | system |
| 14 | Allow | ask_bt | (Drawn iOS prompt) Allow — system UI. | — | system |
| 15 | Don’t Allow | ask_notif | (Drawn iOS prompt) Don't Allow — system UI. | — | system |
| 16 | Allow | ask_notif | (Drawn iOS prompt) Allow — system UI. | — | system |
| 17 | Allow Once | ask_loc | (Drawn iOS prompt) Allow Once — system UI. | — | system |
| 18 | Allow While Using App | ask_loc | (Drawn iOS prompt) Allow While Using App — system UI. | — | system |
| 19 | Don’t Allow | ask_loc | (Drawn iOS prompt) Don't Allow — system UI. | — | system |

**Rules and edge cases.**

- Never request a permission on screen appear; only on the row's Allow.
- Returning from Settings updates every row without a reload flash.


## Voice — Teach Omi your voice

`Voice.dc.html` · plan IDs **O07** · stage **S06** · presented as **push (onboarding stack)** · theme: dark + light

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/Voice.webp`, `specs/geometry/<device>/Voice.json`.

**Purpose.** Guided speech-profile capture: the user reads short prompts; Omi shows live recognition so the user sees it working. Step 6 of 8. Skippable.

**Reached from.** `Permissions`, `Settings`, `VoiceReview`.

**Data (existing contracts only).** Existing speech-profile flow: `GET v3/speech-profile`, `GET v4/speech-profile`, upload via `v3/upload-audio`, STT availability `GET v3/speech-profile/stt-availability` (Flutter `speech_profile_widget.dart`, `guided_voice_controller.dart`, `guided_voice_io.dart`).

**Layout, top to bottom.** progress (6/8) · title · prompt card (question + “Try another prompt” / “Skip this prompt”) · live words area · level meter ring · primary button · “Set Up My Voice Later”.

**States.**

- ready: Start Speaking
- recording: meter follows real input level; recognised words appear live; Done
- silent (no speech for ~6 s): hint + Try Again
- captured (not last prompt): Next Prompt
- captured (last prompt): Review Answers
- mic denied: explain + Open Settings (reuse Permissions row pattern)
- STT unavailable: explain and allow skip

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | Try another prompt | always | Swap the prompt text (no recording change). Cross-fade 0.2 s. | — | in place |
| 2 | Skip this prompt | always | Skip this prompt; count it as skipped (not failed). | — | in place |
| 3 | Back | always | Back to Permissions. Stops and discards an in-progress take. | `Permissions` | pop |
| 4 | Start Speaking | ready | Start recording this prompt (microphone owner = the single app audio session; LED/blue state only while actually recording). | — | in place |
| 5 | Done | recording | Stop recording; keep the take. | — | in place |
| 6 | Try Again | silent | Discard the silent take and record again. | — | in place |
| 7 | Next Prompt | capturedMid | Advance to the next prompt (progress dots advance). | — | in place |
| 8 | Review Answers | capturedLast | Go to Review. | `VoiceReview` | push |
| 9 | Set Up My Voice Later | always | Skip voice setup entirely → What Omi knows. Can be resumed later from Settings → Your voice. | `Knows` | push |

**Rules and edge cases.**

- This was a reported bug (0% enrollment): progress must come from accepted samples, never from a timer.
- Waveform/meter must be real audio level; never animate it when silent.


## VoiceReview — Review your answers

`VoiceReview.dc.html` · plan IDs **O07** · stage **S06** · presented as **push (onboarding stack)** · theme: dark + light

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/VoiceReview.webp`, `specs/geometry/<device>/VoiceReview.json`.

**Purpose.** User keeps or drops each transcribed answer before anything is saved to their profile.

**Reached from.** `Knows`, `Voice`.

**Data (existing contracts only).** Save through the same speech-profile/profile path Flutter uses after guided voice (`guided_voice_io.dart`). Unchecked answers are not uploaded as facts.

**States.**

- all kept (default)
- some unchecked
- saving
- saved: primary becomes Continue
- save failed: error + retry; answers kept

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | Keep this answer | always | Keep/drop answer 1 (checkbox, light haptic). | — | in place |
| 2 | Keep this answer | always | Keep/drop answer 2. | — | in place |
| 3 | Keep this answer | always | Keep/drop answer 3. | — | in place |
| 4 | Keep this answer | always | Keep/drop answer 4. | — | in place |
| 5 | Back | always | Back to Voice (answers kept). | `Voice` | pop |
| 6 | Save and Finish | notSaved | Save kept answers; on success swap to Continue (button morph, success haptic). | — | in place |
| 7 | Continue | saved | Go to What Omi knows. | `Knows` | push |
| 8 | Continue Without Saving Answers | always | Discard answers and go on. Voice samples already accepted stay; answers are not saved as facts. | `Knows` | push |


## Knows — What Omi knows

`Knows.dc.html` · plan IDs **O08** · stage **S09** · presented as **push (onboarding stack)** · theme: dark + light

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/Knows.webp`, `specs/geometry/<device>/Knows.json`.

**Purpose.** Shows the start of the user's knowledge graph so the value is visible before the app opens. Step 7 of 8. Empty is fine.

**Reached from.** `OnboardPlan`, `Voice`, `VoiceReview`.

**Data (existing contracts only).** `GET v1/knowledge-graph` (Flutter `knowledge_graph_step.dart`). Draw only nodes/edges the server returns.

**States.**

- loading: nodes fade in as they arrive
- has nodes: graph draws itself (gentle spring)
- empty: centre “You” node only + the explainer line
- failed: same as empty (never block onboarding)

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | Back | always | Back to Voice Review. | `VoiceReview` | pop |
| 2 | Continue | always | Go to Choose a plan. | `OnboardPlan` | push |


## OnboardPlan — Choose a plan (new)

`OnboardPlan.dc.html` · plan IDs **M51 (onboarding placement)** · stage **S12 (placeholder in S02)** · presented as **push (onboarding stack)** · theme: dark + light

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/OnboardPlan.webp`, `specs/geometry/<device>/OnboardPlan.json`.

**Purpose.** Step 8 of 8. Offer Free / Plus / Unlimited once, honestly, with Free always one tap away. The only plans iOS sells are Plus and Unlimited (`backend/config/plan_catalog.json`, `.github/agent-docs/plan-catalog.md`).

**Reached from.** `Knows`.

**Data (existing contracts only).** Plans and prices: `GET v1/payments/available-plans` (never hard-code prices; `[Price]` is a placeholder). Current plan: `GET v1/users/me/subscription`. Purchase: `POST v1/payments/checkout-session` {price_id, promotion_code?} → open returned Stripe Checkout `url` in `ASWebAuthenticationSession`/SFSafariViewController → on return re-read `GET v1/users/me/subscription`. The app does not use StoreKit.

**Layout, top to bottom.** progress (8/8) · “Choose your plan” · subtitle · Monthly/Yearly · three plan cards (selected expands, others one line) · terms line with Terms · Privacy · Promo code · footer CTA.

**States.**

- loading plans: cards show names + limits immediately, prices shimmer
- prices failed: cards stay, prices read “—”, CTA for paid disabled with “Try again”; Free always works
- Free selected: CTA “Continue with Free”, terms line = no payment
- Plus/Unlimited selected: CTA “Continue with Plus/Unlimited”, terms = Stripe checkout + renews
- checkout open: app waits; on cancel return to this screen unchanged
- checkout success: success haptic, then Complete
- already paid (e.g. returning user): skip this step entirely

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | Monthly | always | Show monthly prices (segmented thumb slides; prices cross-fade). | — | in place |
| 2 | Yearly | always | Show yearly prices. | — | in place |
| 3 | Free Included 300 minutes of premium transcription a month… | always | Select Free (radio; card expands to full list, others collapse — 0.45 s spring; bouncy check). | — | in place |
| 4 | Plus ‹dynamic› 25 hours of premium transcription a month 2… | always | Select Plus. | — | in place |
| 5 | Unlimited ‹dynamic› Unlimited premium transcription 1,000 … | always | Select Unlimited. | — | in place |
| 6 | Terms | always | Open Terms in an in-app Safari view. | `Consent` | system |
| 7 | Privacy | always | Open Privacy Policy in an in-app Safari view. | `Consent` | system |
| 8 | Promo code | always | Show a promo-code field (alert with text field); the code is passed as `promotion_code` to checkout. | — | system |
| 9 | Back | always | Back to What Omi knows. | `Knows` | pop |
| 10 | ‹dynamic› | always | Free → mark onboarding step done and go to Complete. Paid → create checkout session, open Stripe Checkout; success → Complete; cancel → stay. | `Complete` | push |

**Rules and edge cases.**

- Default selection is a product decision (drawn: Plus). Never pre-select annual without showing the monthly equivalent.
- Selling digital subscriptions through web checkout inside an iOS app needs an App Store policy/legal decision per storefront before release — record it; the design does not decide it.
- Neo/Operator/Architect are never offered here (plan-catalog audience rules).


## Complete — Onboarding complete

`Complete.dc.html` · plan IDs **O09** · stage **S12** · presented as **push, then replace root with Home** · theme: always dark

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/Complete.webp`, `specs/geometry/<device>/Complete.json`.

**Purpose.** Celebrates completion and teaches three things: blue light, Home, Ask.

**Reached from.** `OnboardPlan`.

**Data (existing contracts only).** `PATCH v1/users/onboarding` {completed: true} (Flutter `updateUserOnboardingState`). Do not mark complete locally before the server accepts it.

**States.**

- default: LED breathing, tips rise with stagger
- saving on tap: button spinner
- save failed: error, stay (retry)

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | Start Using Omi | always | Persist completion, then replace the root with the tab shell on Home. The Omi mark flies into Home's status pill (0.5 s spring); Home content staggers in. | `Main` | replace root |

