# iPhone system surfaces (proposed, on-device only)

Generated by `spec/generate.py` from the artboards and `spec/content_*.py`. Read `spec/README.md` first for the vocabulary (presentation, transitions, gestures) and global rules.

## LockScreen — Lock Screen Live Activity

`LockScreen.dc.html` · plan IDs **(proposed)** · stage **S13** · presented as **ActivityKit Live Activity** · theme: always dark

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/LockScreen.webp`, `specs/geometry/<device>/LockScreen.json`.

**Purpose.** Capture status on the Lock Screen while recording.

**Reached from.** —.

**Data (existing contracts only).** On-device ActivityKit only; buttons are App Intents that call the capture owner. No polling, no new backend.

**States.**

- listening (timer)
- paused
- ended (dismisses after 5 s)

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | ‹dynamic› | always | Pause/resume (App Intent). | — | in place |
| 2 | Star | always | Star (App Intent). | — | in place |
| 3 | Finish | always | Finish (App Intent → processing; opens the app to Processing). | — | in place |
| 4 | Flashlight | always | System flashlight (not ours). | — | system |
| 5 | Camera | always | System camera (not ours). | — | system |


## HomeScreen — Home Screen widgets and Dynamic Island

`HomeScreen.dc.html` · plan IDs **(proposed)** · stage **S13** · presented as **WidgetKit + Live Activity** · theme: always dark

Reference: `renders/{se,mini,iphone16,max}/{dark,light}/HomeScreen.webp`, `specs/geometry/<device>/HomeScreen.json`.

**Purpose.** Glanceable tasks and capture status.

**Reached from.** —.

**Data (existing contracts only).** App Group cache written by the app; interactive widget buttons are App Intents (`PATCH v1/action-items/{id}/completed` via the app's queue).

**States.**

- widget shows cached tasks; stale indicator when cache is old

**Controls** (every interactive element in the design, in source order).

| # | Control | Shown when | Production behaviour | Goes to | Transition |
|---|---|---|---|---|---|
| 1 | ‹dynamic› | always | Complete task A (App Intent; widget reloads timeline). | — | in place |
| 2 | ‹dynamic› | always | Complete task B. | — | in place |
| 3 | ‹dynamic› | always | Complete task C. | — | in place |

