{
  "name": "@omi-core/kernel",
  "version": "0.0.0",
  "private": true,
  "description": "Runtime primitives enforcing the contracts: the sole Degraded constructor, slug generation, injected clock/rng. Dependency-free.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "echo kernel:tested-via-testkit",
    "lint": "echo kernel:no-lint-yet"
  },
  "dependencies": {
    "@omi-core/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
