package expo.modules.hardware

import android.content.Context
import expo.modules.kotlin.AppContext
import expo.modules.kotlin.views.ExpoView

class HardwareView(context: Context, appContext: AppContext) : ExpoView(context, appContext)
