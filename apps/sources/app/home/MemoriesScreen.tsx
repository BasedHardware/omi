import * as React from 'react';
import { Text, View } from 'react-native';

export const MemoriesScreen = React.memo(() => {
    return (
        <View>
            <Text>Memories</Text>
        </View>
    );
});