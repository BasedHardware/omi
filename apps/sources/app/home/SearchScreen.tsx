import * as React from 'react';
import { Text, View } from 'react-native';

export const SearchScreen = React.memo(() => {
    return (
        <View>
            <Text>Search</Text>
        </View>
    );
});