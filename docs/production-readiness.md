# v5 production readiness — 2026-09-07

Status: code-side fixes verified locally; production configuration and physical-device validation remain outstanding.

The deployed check now runs the PostgreSQL deletion registry and cleanup participant contracts. The real PostgreSQL runner also executes deletion-receipt qualification. Broader Linux testing found five tables present in the SQL cleanup registry but missing from its typed inventory; the inventories now agree. The combined real-database run passed 28 tests and 1,008 assertions, including cleanup rollback and retained tombstones (`/tmp/v5-audio-batch-deletion-postgres.log`). This verifies the cleanup implementation, not deployment or a live account deletion.

## Verified changes

- `919dadae`: account partition protection, bounded multi-turn chat context, persisted upload acknowledgment and completion guard. Migration 0005 was applied to the local emulator only.
- `77e8cdbbd1`: mobile destination wiring, truthful loading/error states, full loaded conversation list, read-only task presentation, visible recording failure, stale authentication protection and failed-draft recovery.
- `c6004334`: release cache freshness, web dependency resolution, animation cleanup crash fix and viewport-contained navigation.
- `20d1355c`: R2 signing targets the verified deployment account and bound bucket.
- `6bc4fce0`: native bundles execute their platform initializer before app entry.
- `7436312476`: native authentication cancellation, Android OAuth/session/SSE integration and macOS onboarding contrast.

The branch was pulled from `BasedHardware/omi:v5`, already at `a14c240bec`; a final fetch confirms no newer upstream commits. Changes remain local; no remote migration, push, merge or deployment occurred. Existing untracked screenshots were preserved.

## Evidence

`bun run setup` completed; native dependency resolution was subsequently aligned with the hoisted workspace and the pinned CocoaPods version. `bun run check` passed: 308 React Native tests, 245 Worker contract tests, 50 Workers/D1 integration tests, 33 ratified-contract tests, 46 PWA tests and one C++ boundary test, plus native JVM transport, Apple OAuth callback, and real Bun Metro startup and macOS/iOS/Android bundle-initialization regressions. Formatting, lint, typecheck and the PWA production build passed. 11 React Native lint warnings and the web bundle-size warning remain. Worker strict deployment dry-run passed.

The real local Wrangler Worker on 8787 and PWA proxy on 5178 passed 19 HTTP requests again after the second pass covering read routes, invalid inputs, recording creation/upload/completion, repeated completion, rejection of late audio, chat admission/replay/conflict and persisted history. Missing/wrong authorization returned 401; another client could not read the validation chat. The local emulator used actual Durable Objects, D1 and R2 with synthetic test inputs. The local-only AI binding produced an explicit failed-generation event. A separate local Worker on 8790 used a remote Workers AI binding with local D1, R2 and Durable Objects: the real PWA on 5180 received READY, recalled READY in a follow-up, and showed both persisted replies after a fresh page load. Only synthetic prompts were sent to the live model; no Worker was deployed.

Browser checks at 390×844 and 320×640 exercised settings, memories, return navigation, chat submission, persisted transcript, provider-failure display and composer recovery. After the animation fix, failed generation no longer blanked the app. The mobile settings button stays visible with long device text. Navigation previously extended to 942px in an 844px viewport; it now ends at the viewport boundary (640px in the 320×640 check). This measurement supports the updated static CSS contract test.

Latest backend completeness gate: `/tmp/omi-v5-backend-completeness-final-check.log`; app integration gate: `/tmp/omi-v5-transcript-app-final-check.log`; strict dry-run: `/tmp/omi-v5-backend-completeness-dry-run.log`.

Earlier logs: `/tmp/omi-v5-recording-lifecycle-check.log`, `/tmp/omi-v5-final-visual-check.log`, `/tmp/omi-v5-final-auth-stream-check.log`, `/tmp/omi-v5-macos-auth-build.log`, `/tmp/omi-v5-ios-auth-build.log`, `/tmp/omi-v5-native-sse-live-result.txt`, `/tmp/omi-v5-live-ai/dev.log`. Earlier logs: `/tmp/omi-v5-readiness-final.log`, `/tmp/omi-v5-pass2-final.log`, `/tmp/omi-v5-pass2-complete-check.log`, `/tmp/omi-v5-pass2-acceptance.log`, `/tmp/omi-v5-macos-pass2.log`, `/tmp/omi-v5-ios-final-build.log`, `/tmp/omi-v5-final-acceptance.log`, `/tmp/omi-v5-proxy-smoke-results.json`, `/tmp/omi-v5-proxy-pass2.log`, `/tmp/omi-v5-proxy-final.log`, `/tmp/omi-v5-local-migrations.log`, `/tmp/v5-backend-audit-tests.log`, `/tmp/v5-backend-audit-dry-run.log`.

## Remaining release blockers

1. Native validation: macOS Debug, iOS arm64 Simulator Debug, and Android arm64 Debug builds pass. Stale CocoaPods metadata and inconsistent native dependency paths are repaired. Windows was not built in this macOS environment. Android now has native browser OAuth with PKCE, AndroidKeyStore-backed session storage, refresh and cancellation. Android generation streaming now uses the native session, resumes event cursors and cancels real requests. Its production Java engine received NATIVE_SSE_READY in a real terminal frame from the isolated live-AI Worker. This is transport proof, not a completed signed-in Android app flow. The Android emulator and iOS simulator render Welcome. Android reaches the real Google sign-in form; browser Back followed by Cancel restores Sign in immediately, and a late callback leaves the app signed out. macOS renders Welcome and reaches the Google account chooser. Android Nearby devices denial and regrant were exercised; scan registers successfully and returns after its timeout. Account selection/callback completion and physical-device capture remain unverified. A normal installed Android APK cannot inherit shell environment configuration; its staged Worker origin still needs explicit native/build configuration for a full signed-in app-to-Worker check.
2. Backend provisioning: recording transcription is implemented and verified through real Workers AI and the app. The Worker now forwards ratified memories and task reads/writes through an optional `CANONICAL_SERVICE` binding, validating wire responses and forwarding only an already verified Firebase bearer. Contract tests exercise real canonical task routes with SQLite control and atomic writes, including account isolation, idempotency, stale revision and epoch behavior. No deployed canonical target is configured. It still requires production PostgreSQL, account/control and grant authority, Firebase application bindings, persistent codec keys and real rendering/cursor composition. Its local bootstrap intentionally renders no memories, and legacy api.omi.me/v3/memories is not the ratified model. Production identity/entitlement configuration remains staging-oriented. Recovery of bytes lost before upload is still missing; durable transcription retry covers processing after verified completion only.
3. Live integration: live model inference through the PWA now passes with synthetic text. Signed attachment upload/download, completed native authentication and production service configuration still need end-to-end validation. Read-only inspection of the deployed Worker found no R2 signing secrets. The local configuration now corrects a one-character R2 account-ID mismatch and pins the deployment account; its signer regression failed before the correction and passes afterward. No remote configuration was changed.
4. Rollout: read-only `wrangler d1 migrations list --remote` confirms migration 0005 is still pending on the configured staging database (`/tmp/omi-v5-remote-migrations-status.log`). Migrations 0006 through 0008 are new and remain local. Review all four before remote application. It conservatively marks only nonempty legacy open recordings failed, retaining bytes/metadata and preserving historical completed/failed states. Previously completed recordings are not retroactively verified. Follow `apps/backend-worker/RELEASE.md` for migration evidence and deployment gates.

The final macOS visual check found dark-theme text on the native light window. Onboarding now uses the existing desktop ink palette for copy, logo and controls; the inspected screenshot `/tmp/omi-v5-macos-ink.png` confirms readable text and the eight-dot mark. Mobile retains its white mark.

Recording lifecycle verification now covers native disconnect while session creation is pending and reconnect while an earlier open, upload or completion is delayed. Each capture owns its queue and server session, so old work cannot consume new bytes. Completion failures display an unconfirmed-save message, and sign-out retires delayed work and errors. These are production-hook tests with controlled native/backend responses; physical pendant validation remains outstanding.

The second pass fixes simultaneous duplicate chat admissions (the real proxy now returns 200/201 with one generation), rejects timestamps outside the platform Date range, exposes the existing mobile device selection controls, requests Android Bluetooth permissions before native access, and prevents Android authenticated HTTP redirects. iOS now shares the existing native session authority and gates product reads on sign-in. Apple callback validation and Android transport behavior have native regression runners in CI. Pending recording work is retired on sign-out/unmount, with deferred-open/upload regressions preventing cross-session continuation. Real native launch checks also found and repaired missing Android core-module registration, duplicate React Native bundle initialization, Bun Metro startup, and Android status-bar overlap, and missing native scan arguments. Screenshots: `/tmp/omi-v5-ios-auth-final.png`, `/tmp/omi-v5-ios-welcome.png` and `/tmp/omi-v5-android-insets.png`. These show rendering, not successful authenticated backend integration.

No new production mocks, TODOs or placeholder implementations were added. Missing services remain explicit blockers.

## Backend completeness pass

The real PWA proxy on 5182 accepted 396 synthetic firmware Opus packets into local R2/D1, completed the session, and the local scheduled Worker on 8792 called remote Whisper. It persisted “This is an OMI recording test. The meeting is tomorrow at 10 in the morning.” with timed segments and language. The real Conversations view rendered the saved recording and full transcript after a fresh page load. An unsupported-codec recording produced the visible failed-transcription state; another account received 404 for the completed transcript. Only generated test speech reached the live model; no user recording or remote database mutation was involved. Evidence: `/tmp/omi-v5-transcription-live/result.log` and `/tmp/omi-v5-whisper-speech-result.log`.

Migration 0006 atomically creates jobs only for future verified nonempty completions. Tests exercise real D1 migrations/R2 for queue idempotency, incomplete upload rejection, malformed audio, account isolation, provider retries, expired leases, stale publication and UTF-8 output limits. PCM8 and Opus framing have codec tests; rebuilt Opus was independently decoded with ffmpeg. The transcript UI uses native transport and tests full text, mobile Back navigation, reload, stale selection and authentication invalidation.

Operational limits and canonical binding prerequisites are recorded in `apps/backend-worker/RELEASE.md`. No production service, account epoch, memory rendering, or credentials were fabricated.

## Recording retry and identity pass

Indexed audio uploads now reserve and acknowledge each packet once through a D1 ledger. Same-index/same-byte retries are idempotent; changed bytes conflict. R2 failures leave pending work recoverable. The app retains packets until acknowledgment, retries transient failures three times with bounded backoff, and preserves ordering and authentication fences. Pending audio across captures is capped at 8 MiB. Longer outages and process death still need persistent offline storage; session creation now retries using its stable capture ID.

Chat conversation IDs now use the `chat:` namespace, keeping them distinct from `recording:` IDs even when a client supplies a recording-shaped chat session ID. Existing raw-session pagination cursors must restart; stored chat IDs and rows are unchanged.

A further audit confirms that Firebase chat admission still uses the staging lifetime limit rather than production subscription/period authority. This remains a production blocker. The local PWA also requests legacy Settings/Connectors routes absent from the Worker; native transport routes those reads to the existing legacy origin. A production web transport needs equivalent authenticated routing. Neither staging labels nor a fabricated subscription policy are proof of production entitlement.

Live retry evidence: `/tmp/omi-v5-transcription-live/retry-result.log`. The production app upload client recovered a deliberately lost acknowledgment, three concurrent identical retries, and a retry after completion without duplicate counts: 396 packets, 30,462 bytes. Changed bytes returned 409. The resulting real Whisper transcript matched the generated speech. The local emulator alone received migration 0007. Final code gate: `/tmp/omi-v5-upload-recovery-final-check.log`; strict dry-run: `/tmp/omi-v5-upload-recovery-dry-run.log`.

## Persistence and entitlement source check

The existing `StorageBridge` is a contract, with no native log/KV implementation. The sync outbox auto-replays and lacks a sign-out disposal API, so it cannot safely replace recording queues as-is. Idempotent capture creation is implemented. Persistent recording recovery still needs an account-bound encrypted native journal before replay can survive process death. Android has tested AES-GCM/KeyStore primitives; Apple currently stores credentials in Keychain but has no encrypted audio-file store. Native imported/environment sessions also need verified account identity before opening a persistent partition.

The legacy backend's `/v1/users/me/usage-quota` is a display snapshot, not an atomic reservation API. Its enforcement uses monthly subscription limits, cost/question units, BYOK rules and overage policy. Polling that snapshot from the Worker would not make Worker admissions part of the authoritative usage ledger. Production billing integration therefore remains unresolved.

Continuous recording currently reaches the per-session cap. Safe rollover needs firmware frame boundaries with space for a complete frame (62,208 raw bytes and 256 packets). Opus also needs decoder history/overlap handling across independently decoded segments; simply splitting byte queues would not prove continuous audio preservation.

Conversation metadata now selects only the first, first-human and last messages per session in D1, returning bounded UTF-8 title/overview bytes instead of whole chat histories. A real D1 regression reduced approximately 4 MB across 84 messages to two summaries below 9 KB serialized, preserving Unicode whitespace, null characters, duplicate JSON keys and lone UTF-16 surrogate IDs. Returned metadata still scales with conversation count; pagination remains in the existing caller. Live PWA proxy admission/read returned the expected chat summary alongside recording rows (`/tmp/omi-v5-conversation-live-read.log`). Final gate: `/tmp/omi-v5-conversation-metadata-final-check.log`.

## Provider independence and recording creation

The provider-independent backend remains in `backends/example-platform`, with the separate integration checkout at `/Users/undivisible/projects/omi-platform-integration` on `codex/track3-backend-integration` (inspected head `e4d85d7b`). It contains model ports/drivers and SQLite/PostgreSQL/Firebase adapters. Its documented app-facing local composition uses deterministic QA data; it is not proof of a production deployment. The live v5 path remains the Cloudflare Worker with D1/R2/Durable Objects, Workers AI or the OpenRouter gateway. The shared HTTP core also mounts in Elysia, but that does not remove its infrastructure dependencies. Canonical memory/task service bindings are an integration boundary, not a completed portable production composition.

Recording creation now owns a native cryptographic UUID across retries. Local migration 0008 and real app-client HTTP verification recovered a lost creation response, three concurrent replays and replay after completion with one server session; changed metadata returned 409 (`/tmp/omi-v5-capture-open-live.log`). macOS, iOS Simulator and Android Debug builds passed for the native UUID bridge. Persistent disk replay remains unimplemented.

## Mobile onboarding verification

The shared React Native app now separates native sign-in from explicit setup completion. A revisioned local preference retires the old auth-only completion flag. Setup explains cloud audio transcription and message processing, links the existing privacy and terms pages, and lets mobile users connect a device or continue without one. Reads and capture remain gated until completion. The local PWA flow was exercised at mobile width: disclosure rendered without clipping, continuing without a device opened Home, and reload retained completion. This verifies shared UI behavior, not native provider sign-in or physical Bluetooth.

iOS Simulator, Android Debug and macOS Debug builds passed after the native onboarding preference changes (`/tmp/omi-v5-onboarding-ios-build.log`, `/tmp/omi-v5-onboarding-android-build.log`, `/tmp/omi-v5-onboarding-macos-build.log`). Physical-device authentication, Bluetooth/background capture, encrypted offline recovery and continuous recording remain release gates. Main-app profile/language/speaker onboarding parity is not complete. Phone-microphone recording is not connected (the mobile capture strip is status UI); wearable capture is the current recording path. Task completion and description editing are now connected as described below; account/connectors settings still use legacy endpoints absent from the Worker.

The final combined verification passed `bun run check` with 653 JavaScript/TypeScript tests plus native gates (`/tmp/omi-v5-mobile-onboarding-final-check.log`); strict Worker dry-run also passed (`/tmp/omi-v5-onboarding-worker-dry-run.log`). The updated iOS app installed and launched on the simulator; native screen interaction was not verified in this pass.


## Task mutations and mobile parity

Mobile Home/Tasks, the wide shared Tasks page and the macOS Tasks page now offer completion, reopening and description edits. The native bridges mint 32 random bytes for each write identity. A prepared patch captures its account epoch, record revision and timestamps once; retries reuse its exact body. Unknown results retain the pending change, acknowledgements trigger a task read, and a failed read retries without resending the acknowledged write. Conflicts and stale epochs refresh authority without replaying the refused operation. Account invalidation retires pending transport work. Pending edits remain memory-only; process-death recovery still needs account-bound native storage.

Android, Apple and PWA task writes previously missed the selected backend route. Task reads and POST `/v1/tasks/ops` now use the same selected authority; other development routes stay restricted. The local provider-independent service at port 4851 persisted actual completion, returned idempotent acknowledgement for identical replay, and rejected a stale revision (`/tmp/v5-task-ui-write-proof.json`). Via the PWA at port 5197, the mobile UI completed, edited, reloaded and reopened a task. A concurrent API edit caused a real conflict; the UI refreshed the server value while preserving the user's draft. These are local SQLite QA proofs, not a production authority deployment.

Canonical task timestamps use milliseconds: `backends/example-platform/apps/service/qa/demo-persona.ts` produces `instantMs`, the read composition forwards the values, and the real service returned 13-digit timestamps. Task grouping and due-date rendering now use the same units. RN Web ignored aggregate `accessibilityState`, so the shared pressable maps supported ARIA state explicitly. Browser inspection confirmed checked/selected states, and a hermetic test renders the actual component against installed RN Web.

Native iOS Simulator, Android Debug and macOS Debug compiles passed after the secure write identity and routing changes (`/tmp/v5-task-ios-build.log`, `/tmp/v5-task-android-build.log`, `/tmp/v5-task-macos-build.log`). Native authenticated task interaction remains unverified. No remote migration or deployment was performed.

Task parity final gate: `bun run check` passed all 682 JS/TS tests plus native checks (`/tmp/v5-task-completion-commit-check.log`). A separate agent reviewed operation identity, account fences and read refresh handling; its stale-epoch refresh finding was fixed and regression-tested.


## Mobile navigation and conversation detail verification

Mobile Conversations now uses the shared searchable list and selectable detail view, including recording transcripts. Settings is a fifth bottom destination alongside Home, Conversations, Tasks and Apps; the shared Settings and Connectors pages stay inside that navigation. Browser settings reads use the real service settings endpoint and show a retryable, readable failure when the selected backend lacks it. No profile, billing identity or successful save is fabricated.

The local PWA at port 5197 was exercised at mobile width: conversation selection opened full details, bottom navigation remained available, Settings selected correctly, and Privacy remained reachable. The canonical local service currently refuses settings, and the UI displayed the expected friendly retry message. Root `bun run check` passed with 310 React Native tests and the existing Worker, contract, PWA and native checks, plus 14 deployed-platform tests (`/tmp/v5-mobile-production-entry-check.log`). This is UI and local verification, not deployed feature parity.

The design reference retains Mind Map collapsed on Home. Its keep/fix/drop annotations are explicitly an architect first-pass; they do not establish a completed graph feature or authorize removing it.
