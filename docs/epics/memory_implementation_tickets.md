# memory Memory Product Integration — Implementation Tickets

> **Historical:** 'v17' was the internal codename for the canonical memory system; fully renamed to neutral vocabulary on 2026-06-24.

**Created:** 2026-06-18T20:24Z  
**Status:** Normative decisions baked in; GO for local/default-off implementation and dev-cloud preparation, BLOCKED for dev-cloud GO until the Gate 2 evidence bundle passes, and NO-GO for production writes/read switch/vector/API cutover until dev-cloud GO plus production-only gates pass
**Normative Architecture:** `docs/epics/memory_normative_architecture.md`
**Source Epic:** `docs/epics/memory_product_integration_epic.md`
**Decision Brief:** `docs/epics/memory_product_integration_decision_brief.md`
**memory /v3 Proof Order:** `docs/rollout/memory-v3-proof-order.md`
**Dev-cloud Proof Runbook:** `docs/runbooks/memory-v3-dev-cloud-proof.md`
**Production Activation Runbook:** `docs/runbooks/memory-v3-production-activation.md`
**Repos:**
- Product: `/root/workspace/omi-memory-ingestion-pipeline`
- Benchmark/eval: `/root/workspace/omi-ingestion-benchmark`

---

## Locked product decisions

| Decision | Direction |
|---|---|
| Product terms | **Short-term**, **Long-term**, **Archive** |
| Short-term | Fresh source-backed memory before/during L2 processing; default-access while fresh/relevant |
| Long-term | Stable L2/ledger-backed memory; default-access |
| Archive | Explicit-query historical/source-backed context; not default-access |
| Context-only | Not a normal user-visible tier; internal alias only, normalized to Archive/non-default outcome |
| Canonical store | One tiered `users/{uid}/memory_items/{memory_id}` collection for product items; no separate canonical Short-term/Archive stores |
| Long-term authority | Existing ledger remains Long-term source of truth; `memory_items` is transactional product projection |
| Existing `short_term` store | Legacy/adapter input only, not memory canonical store |
| Stable identity | Opaque server `memory_id`; one-to-one tier transitions keep ID; many-to-one merges use canonical aliases |
| Rollout | Whitelist first; old memory behavior unchanged for everyone else; `read` is superset of `write` |
| Rollback | `read → write` uses memory-derived compatibility projection; `write → off` requires decommission reconciliation |
| Config | Keep simple external mode; hidden internal safety gates allowed |
| UI | Minimal labels/filter/provenance/edit/delete; users can manage memory by chatting with Omi |
| Agent mode | Memory-management tools: remember, forget, list, search, provenance, visibility/use policy, promote/demote, explicit archive search |
| Third-party/MCP/developer API | Existing broad memory permission maps to Long-term + Short-term; Archive/raw provenance require explicit capability + explicit query |
| Deletion | Follow current product behavior: memory deletion removes memory/projection/vector, not raw source; source/account deletion controls raw deletion |
| Vectors | KISS: existing `ns2` memory namespace only through fail-closed search gateway + authoritative hydration; separate namespace only evidence-driven fallback |
| Raw artifacts | Retain available raw/source artifacts indefinitely for now; copy ephemeral bytes durably where feasible; report already-missing losses honestly |

---

> **Archive note:** Full implementation journal relocated to `omi-ingestion-benchmark/docs/research-logs/pipeline-memory/memory_implementation_tickets_full.md` on 2026-06-25. Readiness markers referenced by unit tests live in `docs/operational/memory_readiness_evidence_markers.md`.

## Implementation progress log (readiness markers only)

| Date | Slice | Status | Verification | Next action |
|---|---|---|---|---|
| 2026-06-20 | memory-V3-F5 real-service read-only evidence preparation | **Complete for preparation/default-NOT_RUN tooling only; all runtime/external/aggregate readiness remains BLOCKED/NO-GO**: added `backend/scripts/v3_f5_real_service_evidence_readiness.py`, pure support module `backend/utils/memory/v3_f5_evidence.py`, and `backend/tests/unit/test_memory_v3_f5_real_service_evidence_readiness.py`. The runner defaults to `NOT_RUN`, constructs no cloud client by default, and gates `--execute` on exact shared-nonprod environment/project number/project id/service-account/approval subject/path set/Oracle review artifact metadata. Fake-only tests cover least-privilege identity/IAM, metadata-only bounded scope, cursor-secret metadata (no payload bytes), zero-write static/runtime/audit model, deadlines, index/schema checks, HMAC redaction fail-closed allowlist, F4 structural-disable/pagination/call-graph confirmations, no production app/router imports, and no readiness GO. Shared service execution still requires a subsequent Oracle review; production execution remains NO-GO without named human platform/security approval. | RED: `pytest tests/unit/test_memory_v3_f5_real_service_evidence_readiness.py -q` -> 18 failed before adding runner/module/docs/links. GREEN/focused/full/readiness outputs recorded in handoff. | Next: Oracle review of exact F5 script/principal/project/path set before any shared non-production `--execute`; production still requires named human platform/security approval and no activation. |
| 2026-06-19 | Oracle P0-4 Pinecone repair/shared-ns2 validation readiness runner | **Complete for readiness/non-claim artifact; production rollout still BLOCKED**: added `backend/scripts/v17_pinecone_repair_validation_readiness.py`, a safe-by-default Pinecone validation readiness runner for the duplicate stale physical-ID / tombstone precedence / live repair / retry-dead-letter / shared-`ns2` isolation proof Oracle still requires. Default mode emits `status=NOT_RUN`, `read_only=true`, pass/fail criteria, exact prerequisite env (`PINECONE_API_KEY`, `PINECONE_INDEX_NAME`, `PINECONE_INDEX_HOST`), and planned safe commands; it performs no Pinecone delete/upsert/query mutation. Execute mode remains gated by explicit `--allow-throwaway-mutation`, a non-`ns2` `--test-namespace`, a long `memory-proof-...` throwaway vector id prefix, exact prefix confirmation, and optional `--shared-ns2-readonly`; the runner refuses shared `ns2` mutation and broad delete/update terms. Updated Firestore/IAM deployment doc and Oracle review with commands, pass/fail criteria, non-claims, and remaining gates. No real Pinecone validation, vector deletion/upsert, tombstone precedence proof, retry/dead-letter proof, or shared-`ns2` isolation proof was run or claimed. | RED: `cd backend && pytest tests/unit/test_memory_pinecone_repair_validation_readiness.py -q` → `3 failed` (missing safe Pinecone validation readiness runner and doc references). GREEN/static after implementation/doc update: same command → `3 passed in 0.04s`. Readiness run: `python3 backend/scripts/v17_pinecone_repair_validation_readiness.py` → JSON `status: "NOT_RUN"`, `read_only: true`, `mutation_allowed: false`, prerequisites `PINECONE_API_KEY is required`, `PINECONE_INDEX_NAME is required`, `PINECONE_INDEX_HOST is required`. Safety-gated execute attempt: `python3 backend/scripts/v17_pinecone_repair_validation_readiness.py --execute` → exit 2 with `status: "NOT_RUN"`, prerequisites also including `--allow-throwaway-mutation is required for execute mode`, `--test-namespace is required for execute mode`, and `--throwaway-prefix is required for execute mode`. Format/focused regression: `black --line-length 120 --skip-string-normalization scripts/v17_pinecone_repair_validation_readiness.py tests/unit/test_memory_pinecone_repair_validation_readiness.py && pytest tests/unit/test_memory_pinecone_repair_validation_readiness.py tests/unit/test_vector_repair_outbox_worker_entrypoint.py tests/unit/test_vector_repair_outbox_worker.py tests/unit/test_memory_vector_search_service.py tests/unit/test_memory_product_memory_router.py tests/unit/test_memory_firestore_rules_iam_proof.py tests/unit/test_memory_vector_repair_outbox_oidc_iam_proof.py -q` → `64 passed in 0.34s`. Full memory regression: `pytest tests/unit/test_memory_*.py -q` → `255 passed, 1 warning in 1.39s`. Async scan exit 0 with pre-existing findings only (`HIGH async helpers with blocking: 41`, `STRUCTURAL mixed await+sync DB: 10`). | Remaining P0-4: implement/run real throwaway Pinecone fixture validation with exact PASS/FAIL output for duplicate stale physical IDs, delete/repair/tombstone precedence, retry/dead-letter, and post-run absence of prefix-scoped stale vectors; produce read-only shared `ns2` coexistence evidence proving legacy queries exclude memory schema records and baseline recall remains intact or choose separate namespace/filter; run OIDC/IAM and Firestore proof runners against target cloud projects; add retry/dead-letter alerts, overfetch/refill/budgets, app/key/scope auth, benchmarks, and explicit rollout gates. |
| 2026-06-19 | Oracle P0-1/P0-6 MCP API-key scope readiness runner / server-owned assignment contract | **Complete for readiness-runner/assignment-contract slice; production rollout still BLOCKED**: added `backend/scripts/v17_mcp_api_key_scope_readiness.py`, a safe-by-default MCP API-key scope readiness runner. Default CLI returns `status=NOT_RUN`, `read_only=true`, `mutation_allowed=false`, and performs no Firestore reads or writes. `--execute` inventories `mcp_api_keys/{key_id}` and distinguishes missing `app_id`, missing/malformed `scopes`, verified persisted `memories.read`, and unknown scopes without inferring scopes from advertised MCP tool metadata, OAuth security scheme advertisements, or client requests. Writes are unreachable unless both `--execute` and `--allow-write` are supplied with a deterministic server-owned assignment file; mutation preserves existing key IDs/users/hashes/prefixes and only permits allowlisted server-owned scopes (`memories.read`, `memories.write`, `memories.archive.read`), denying unknown scopes before writes. Added static/fake-backed tests, registered the test in `backend/test.sh`, and updated MCP readiness docs plus Oracle review. This is not a production execution, not OAuth introspection, not deployed Firestore/IAM proof, not app/key memory grant assignment, not production approval, and not benchmark/cloud evidence. | RED: `cd backend && pytest tests/unit/test_memory_mcp_api_key_scope_readiness.py -q` → `4 failed` (missing runner and doc references). GREEN: same command after implementation/doc update → `4 passed in 0.07s`. Format/readiness/focused/regression/async: `black --line-length 120 --skip-string-normalization scripts/v17_mcp_api_key_scope_readiness.py tests/unit/test_memory_mcp_api_key_scope_readiness.py` → `2 files reformatted`; `python3 backend/scripts/v17_mcp_api_key_scope_readiness.py` → JSON `status: "NOT_RUN"`, `read_only: true`, `mutation_allowed: false`, `non_claims` includes `no Firestore reads or writes were executed`; focused MCP/auth/product tests → `42 passed, 2 warnings`; full memory regression → `295 passed, 3 warnings`; async scan exit 0 with pre-existing findings only (`HIGH async helpers with blocking: 41`, `STRUCTURAL mixed await+sync DB: 10`). | Remaining P0-1/P0-6: run deployed Firestore/IAM proof against a real target project; migrate/assign production MCP key scopes and server-owned app/key grants; implement OAuth introspection if OAuth-issued tokens become the production path. |
| 2026-06-19 | Oracle P0-1/P0-6 Firestore/IAM proof scope for MCP keys and app/key grants | **Complete for proof-runner scope-extension/readiness artifact; production rollout still BLOCKED**: extended `backend/scripts/v17_firestore_rules_iam_proof.py` so the safe read-only Firestore IAM/deployed Security Rules proof inventory now covers `mcp_api_keys/{key_id}` and server-owned `users/{uid}/memory_control/app_key_memory_grants` in addition to the existing vector repair outbox/control paths. Pass/fail criteria now include `client_denial.app_key_memory_grants`, `app_key_grants.server_owned`, and `mcp_api_key_inventory`. Default mode remains `status=NOT_RUN` and read-only; `--execute` still only permits allowlisted `gcloud`/Firebase describe/get commands and never deploys rules, changes IAM, mutates Firestore, assigns MCP scopes, or assigns app/key memory grants. Updated checked-in `firestore.rules` comments and `docs/epics/memory_firestore_iam_deployment.md` with the MCP key/app-key grant boundaries. Local target/credential prerequisites are unavailable, so no deployed proof is claimed. | RED: `cd backend && pytest tests/unit/test_memory_firestore_rules_iam_proof.py -q` → `2 failed` (proof runner/doc missing `mcp_api_keys/{key_id}` and `users/{uid}/memory_control/app_key_memory_grants` scope). GREEN/static: same command after implementation/doc update → `2 passed in 0.03s`. Format: `black --line-length 120 --skip-string-normalization backend/scripts/v17_firestore_rules_iam_proof.py backend/tests/unit/test_memory_firestore_rules_iam_proof.py` → `2 files left unchanged`. Readiness: `python3 backend/scripts/v17_firestore_rules_iam_proof.py` → JSON `status: "NOT_RUN"`, `read_only: true`, prerequisites `["--project or MEMORY_FIRESTORE_PROOF_PROJECT is required"]`, pass/fail criteria include `client_denial.app_key_memory_grants` and `mcp_api_key_inventory`; `python3 backend/scripts/v17_firestore_rules_iam_proof.py --execute` → exit 2 JSON `status: "NOT_RUN"`, prerequisites include missing project plus `gcloud CLI is not installed or not on PATH` and `firebase CLI is not installed or not on PATH`. Focused regression: `pytest tests/unit/test_memory_firestore_rules_iam_proof.py tests/unit/test_memory_mcp_api_key_scope_readiness.py tests/unit/test_memory_mcp_api_key_auth_context.py tests/unit/test_memory_mcp_auth_context_static.py tests/unit/test_memory_product_authorization.py tests/unit/test_memory_mcp_memory_adapter.py -q` → `44 passed, 2 warnings in 0.36s`. Full memory regression: `pytest tests/unit/test_memory_*.py -q` → `295 passed, 3 warnings in 1.71s`. Async scan: `python3 backend/scripts/scan_async_blockers.py` → exit 0 with pre-existing findings only (`HIGH async helpers with blocking: 41`, `STRUCTURAL mixed await+sync DB: 10`). | Remaining P0-1/P0-6: run deployed Firestore/IAM proof against a real target project with authenticated `gcloud`/Firebase; migrate/assign production MCP key scopes; assign server-owned app/key grants at `users/{uid}/memory_control/app_key_memory_grants`; implement OAuth introspection if OAuth-issued tokens become the production path. |
| 2026-06-19 | Oracle P0-1/P0-6 memory app/key memory grant assignment readiness runner | **Complete for safe admin-readiness/assignment-contract slice; production rollout still BLOCKED**: added `backend/scripts/v17_app_key_memory_grant_assignment_readiness.py`, a safe-by-default runner for deterministic server/Admin-owned grant assignment at `users/{uid}/memory_control/app_key_memory_grants`. Default CLI returns `status=NOT_RUN`, `read_only=true`, `mutation_allowed=false`, and performs no Firestore reads or writes. `--execute --assignment-file ...` validates a deterministic plan only; writes are unreachable unless both `--execute` and `--allow-write` are supplied with an assignment file. Assignment schema requires `uid`, `consumer`, `app_id`, `key_id`, allowlisted persisted `scopes`, `default_read`, `archive_read`, `write`, and `archive_default_visible=false`. The runner denies unknown consumers/scopes/fields, malformed booleans, operation flags without their required scopes, and Archive default exposure; it never infers grants from MCP advertised metadata, client request fields, or key scopes alone. Valid plans target only `users/{uid}/memory_control/app_key_memory_grants` at `grants.<consumer>.apps.<app_id>.keys.<key_id>`. Updated the app/key grant readiness doc, Oracle review, and `backend/test.sh`. No production run or grant assignment is claimed. | RED: `cd backend && pytest tests/unit/test_memory_app_key_memory_grant_assignment_readiness.py -q` → `5 failed` (missing runner and doc references). GREEN: same command after implementation/docs → `5 passed in 0.07s`. Format: `black --line-length 120 --skip-string-normalization scripts/v17_app_key_memory_grant_assignment_readiness.py tests/unit/test_memory_app_key_memory_grant_assignment_readiness.py` → `1 file reformatted, 1 file left unchanged`. Readiness/default: `python3 backend/scripts/v17_app_key_memory_grant_assignment_readiness.py` → JSON `status: "NOT_RUN"`, `read_only: true`, `mutation_allowed: false`, non-claims include `no Firestore reads or writes were executed`; safety gate: `python3 backend/scripts/v17_app_key_memory_grant_assignment_readiness.py --execute --allow-write` → exit 2 JSON `status: "DENIED"`, error `--assignment-file or APP_KEY_MEMORY_GRANT_ASSIGNMENTS is required with --allow-write`. Focused regression: `cd backend && pytest tests/unit/test_memory_app_key_memory_grant_assignment_readiness.py tests/unit/test_memory_app_key_grant_store.py tests/unit/test_memory_product_authorization.py tests/unit/test_memory_mcp_api_key_scope_readiness.py tests/unit/test_memory_firestore_rules_iam_proof.py -q` → `29 passed in 0.23s`. Full memory regression: `pytest tests/unit/test_memory_*.py -q` → `300 passed, 3 warnings in 1.80s`. Async scan: `python3 backend/scripts/scan_async_blockers.py` → exit 0 with pre-existing findings only (`HIGH async helpers with blocking: 41`, `STRUCTURAL mixed await+sync DB: 10`). | Remaining P0-1/P0-6: do not assign production grants until product/admin input exists; run deployed Firestore/IAM proof with real project/credentials/CLI; migrate MCP key scopes or implement OAuth introspection; keep rollout BLOCKED/NO-GO. |
| 2026-06-19 | Oracle P0-5 shared ns2 legacy/memory isolation local readiness | **Complete for local guard/readiness artifact; production rollout still BLOCKED**: added `backend/scripts/v17_shared_ns2_legacy_isolation_readiness.py`, a safe-by-default NOT_RUN artifact for shared `ns2` legacy/memory vector isolation. It inventories legacy `ns2` memory search paths (`find_similar_memories`, `search_memories_by_vector`), required memory metadata barriers, stale/deleted physical-ID risk, and overfetch/refill implications. Added `build_legacy_memory_vector_filter(...)` and wired legacy `find_similar_memories(...)` plus `search_memories_by_vector(...)` to exclude memory schema records before top-k with `memory_schema_version $exists false`, preserving uid isolation and subject filtering. Default readiness output is `status=NOT_RUN`, `read_only=true`, `mutation_allowed=false`; no Pinecone query/mutation runs by default or in this slice. No real Pinecone shared `ns2` proof, recall benchmark, provider output, production coexistence validation, or rollout approval is claimed. | RED: `cd backend && pytest tests/unit/test_memory_shared_ns2_legacy_isolation_readiness.py -q` → `4 failed` (missing readiness runner/docs and legacy filter helper). GREEN/verification: `black --line-length 120 --skip-string-normalization database/vector_db.py scripts/v17_shared_ns2_legacy_isolation_readiness.py tests/unit/test_memory_shared_ns2_legacy_isolation_readiness.py tests/unit/test_memories_batch.py` → `1 file reformatted, 3 files left unchanged`; `pytest tests/unit/test_memory_shared_ns2_legacy_isolation_readiness.py -q` → `4 passed in 0.07s`; readiness/default `python3 backend/scripts/v17_shared_ns2_legacy_isolation_readiness.py` → JSON `status: "NOT_RUN"`, `read_only: true`, `mutation_allowed: false`, prerequisites `PINECONE_API_KEY`, `PINECONE_INDEX_NAME`, `PINECONE_INDEX_HOST`; safety/read-only execute `python3 backend/scripts/v17_shared_ns2_legacy_isolation_readiness.py --execute` → exit 2 with same missing provider prerequisites and no mutation; focused regression from backend `pytest tests/unit/test_memory_shared_ns2_legacy_isolation_readiness.py tests/unit/test_memory_vector_filters.py tests/unit/test_memory_vector_metadata.py tests/unit/test_memories_batch.py -q` → `20 passed, 1 warning`; full memory regression `pytest tests/unit/test_memory_*.py -q` → `304 passed, 3 warnings in 1.84s`; async scan `python3 backend/scripts/scan_async_blockers.py` → exit 0 with pre-existing findings only (`HIGH async helpers with blocking: 41`, `STRUCTURAL mixed await+sync DB: 10`). | Remaining: real Pinecone shared-`ns2` proof with credentials/config; production baseline recall/coexistence benchmark; provider proof that metadata filters behave as assumed; stale/deleted physical-ID cleanup proof. |
| 2026-06-19 | Oracle P0-7 bounded vector overfetch/refill/candidate-budget hardening | **Complete for first narrow local P0-7 hardening slice; production rollout still BLOCKED**: changed `fetch_default_vector_memory_search(...)` from querying exactly `limit` vector candidates and hydrating the full `users/{uid}/memory_items` collection to a bounded overfetch/refill seam. The service now validates configurable/fake-injectable `overfetch_factor` (default 3, max 10) and `max_candidates` (default 50, hard capped by `MAX_MEMORY_VECTOR_SEARCH_LIMIT=100`), queries an initial candidate window larger than the return limit, refills by increasing the vector request up to the hard candidate budget when early candidates are rejected, and hydrates only candidate IDs through `users/{uid}/memory_items/{memory_id}` document gets. Returned items are still clipped to caller `limit`; Archive remains default-unavailable; mandatory projection commit/account generation/item revision/source/content/uid fences remain fail-closed before or during hydration. Added low-cardinality response observability fields for overfetch factor, candidate budget/request limit, vector query count, queried/hydrated candidate counts, missing/stale/access-denied hydration reject counts, returned count, and budget exhaustion. Product, Omi chat, MCP, and developer vector caller tests were updated for the new default overfetch and candidate-ID hydration behavior. This is not a real Pinecone benchmark, not production latency/recall proof, not provider pagination proof, not a central telemetry integration, not a timeout/rate-limit implementation, and not production approval. | RED: `cd backend && pytest tests/unit/test_memory_vector_search_service.py -q` → `2 failed, 9 passed` (`TypeError: fetch_default_vector_memory_search() got an unexpected keyword argument 'overfetch_factor'`). GREEN/focused: same command after implementation → `11 passed in 0.08s`; caller focused regression `pytest tests/unit/test_memory_chat_memory_adapter.py tests/unit/test_memory_developer_memory_adapter.py tests/unit/test_memory_mcp_memory_adapter.py tests/unit/test_memory_product_memory_router.py -q` → `60 passed in 0.20s`; full memory regression `pytest tests/unit/test_memory_*.py -q` → `306 passed, 3 warnings in 1.82s`. Format/async: `black --line-length 120 --skip-string-normalization backend/utils/memory/vector_search_service.py backend/tests/unit/test_memory_vector_search_service.py backend/tests/unit/test_memory_chat_memory_adapter.py backend/tests/unit/test_memory_developer_memory_adapter.py backend/tests/unit/test_memory_mcp_memory_adapter.py backend/tests/unit/test_memory_product_memory_router.py` → `1 file reformatted, 5 files left unchanged`; `python3 backend/scripts/scan_async_blockers.py` → exit 0 with pre-existing findings only (`HIGH async helpers with blocking: 41`, `STRUCTURAL mixed await+sync DB: 10`). | Remaining: replace the re-query-by-larger-topK fake seam with a measured provider-supported pagination/refill contract if Pinecone supports it; add explicit timeouts/rate limits; central monotonic telemetry/alerts; high-volume load/recall/latency benchmarks with real Pinecone/Firestore; continue candidate-ID batch hydration optimization for production Firestore read patterns. |
| 2026-06-19 | Oracle P0-7 memory vector-search low-cardinality telemetry seam | **Complete for narrow fake-injectable central telemetry payload/emitter seam; production rollout still BLOCKED**: added `backend/utils/memory/v17_vector_search_telemetry.py` with `VectorSearchTelemetryConfig` and `emit_memory_vector_search_telemetry(...)`, then wired optional `telemetry_emitter`/`telemetry_config` into `fetch_default_vector_memory_search(...)`. The seam emits deterministic low-cardinality metric/event payloads for vector query count, queried/hydrated/vector-rejected candidates, candidate request limit/budget, hydration rejects by bounded reason, returned count, empty-after-hydration, and candidate-budget exhaustion. Emitter failures are recorded in `response["telemetry"]` and do not mask search results; disabled/missing telemetry remains a no-op. Labels are bounded to `component`, `consumer`, `surface`, `mode`, `status`, `reason`, and `event_type`; tests forbid uid, raw query text, memory IDs, and vector IDs from emitted payloads. Archive remains default-unavailable; mandatory projection/account-generation/item-revision/source/content/uid fences and bounded candidate-ID hydration are unchanged. This is not a Prometheus/OpenTelemetry/Cloud Monitoring integration, not an alert policy, not a timeout/rate-limit implementation, not a real Pinecone/Firestore benchmark, not provider pagination proof, and not production approval. | RED: `cd backend && pytest tests/unit/test_memory_vector_search_service.py -q` → collection failed with `ModuleNotFoundError: No module named 'utils.memory.v17_vector_search_telemetry'`. GREEN/focused after implementation: same command → `13 passed in 0.07s`; after formatting same command in regression chain → `13 passed in 0.09s`. Focused caller regression: `pytest tests/unit/test_memory_chat_memory_adapter.py tests/unit/test_memory_developer_memory_adapter.py tests/unit/test_memory_mcp_memory_adapter.py tests/unit/test_memory_product_memory_router.py -q` → `60 passed in 0.18s`. Full memory regression: `pytest tests/unit/test_memory_*.py -q` → `308 passed, 3 warnings in 1.82s`. Async scan from repo root: `python3 backend/scripts/scan_async_blockers.py` → exit 0 with pre-existing findings only (`HIGH async helpers with blocking: 41`, `STRUCTURAL mixed await+sync DB: 10`). Format: `black --line-length 120 --skip-string-normalization utils/memory/v17_vector_search_service.py utils/memory/v17_vector_search_telemetry.py tests/unit/test_memory_vector_search_service.py` → `1 file reformatted, 2 files left unchanged`. | Remaining P0-7: explicit timeout/rate-limit controls, real Pinecone/Firestore load/recall/latency benchmark/proof, provider pagination/refill proof, production telemetry sink/dashboard/alerts, and malformed/stale/cross-user/high-volume cloud evidence remain incomplete. |
| 2026-06-19 | Oracle P0-7 memory vector-search timeout/rate-limit control seam | **Complete for narrow fake-injectable local timeout/rate-limit controls; production rollout still BLOCKED**: extended `fetch_default_vector_memory_search(...)` with explicit bounded controls `max_vector_queries`, `max_candidate_hydration_reads`, `timeout_seconds`, and injected monotonic `clock`. The service checks required projection/account-generation fences before any vector query/hydration, stops refill when vector-query budget is exhausted, stops Firestore candidate document gets when the hydration-read budget or deadline is exhausted, returns already validated results clipped to `limit`, and marks low-cardinality response fields including `search_status`, `vector_query_budget_exhausted`, `hydration_read_budget_exhausted`, `timeout_exhausted`, budgets, read count, and `legacy_fallback_used=false`. Unread candidates due to hydration budget exhaustion are not treated as missing-authoritative repair/purge candidates. Telemetry emits bounded timeout/control-exhaustion metrics/events without uid, raw query, memory IDs, vector IDs, raw errors, or idempotency labels. Archive remains default-unavailable and hit-level uid/account-generation/item-revision/source/content/projection fences remain preserved for hydrated candidates. This is not a production metrics sink/alert integration, not a real Pinecone/Firestore timeout, not provider pagination proof, not load/recall/latency benchmark evidence, and not production approval. | RED: `cd backend && pytest tests/unit/test_memory_vector_search_service.py -q` → `4 failed, 13 passed in 0.16s` (`unexpected keyword argument` for `max_vector_queries`, `max_candidate_hydration_reads`, and `timeout_seconds`). GREEN/format/regression: `black --line-length 120 --skip-string-normalization utils/memory/v17_vector_search_service.py utils/memory/v17_vector_search_telemetry.py tests/unit/test_memory_vector_search_service.py` → `2 files reformatted, 1 file left unchanged`; `pytest tests/unit/test_memory_vector_search_service.py -q` → `17 passed in 0.11s`; focused caller regression `pytest tests/unit/test_memory_chat_memory_adapter.py tests/unit/test_memory_developer_memory_adapter.py tests/unit/test_memory_mcp_memory_adapter.py tests/unit/test_memory_product_memory_router.py -q` → `60 passed in 0.18s`; full memory regression `pytest tests/unit/test_memory_*.py -q` → `312 passed, 3 warnings in 1.80s`; async scan from repo root `python3 backend/scripts/scan_async_blockers.py` → exit 0 with pre-existing findings only (`HIGH async helpers with blocking: 41`, `STRUCTURAL mixed await+sync DB: 10`). | Remaining P0-7: real Pinecone/Firestore provider pagination/refill behavior and timeout semantics, load/recall/latency benchmark, central Prometheus/OpenTelemetry/Cloud Monitoring sink/dashboard/alerts, malformed metadata/cross-user/expired Short-term/Archive/tombstone/duplicate revision/partial outage/high-volume proof. Production rollout remains BLOCKED / NO-GO. |
| 2026-06-19 | Oracle P0-7 memory vector-search provider-proof readiness artifact | **Complete for safe readiness/non-claim artifact; production rollout still BLOCKED**: added `backend/scripts/vector_search_provider_readiness.py`, a safe-by-default memory vector search provider-proof/readiness runner. Default mode returns `status=NOT_RUN`, `read_only=true`, `mutation_allowed=false`, `provider_calls_executed=false`, `benchmark_evidence_collected=false`, and `production_rollout_approved=false`. It inventories required Pinecone/Firestore prerequisites (`PINECONE_API_KEY`, `PINECONE_INDEX_NAME`, `PINECONE_INDEX_HOST`, `V17_PROVIDER_PROOF_FIRESTORE_PROJECT`, `V17_PROVIDER_PROOF_UID`) and planned read-only proof cases for provider pagination/refill semantics, vector query timeout behavior, Firestore candidate-ID hydration read counts, malformed/stale metadata, cross-user hits, expired Short-term, Archive default-unavailable, deleted/tombstoned sources, duplicate revisions, partial outages, high-volume account candidate budgets, and load/recall/latency criteria. `--execute` remains read-only and exits nonzero with `NOT_RUN` when prerequisites are missing; no Pinecone upsert/delete/update and no Firestore create/set/update/delete calls are planned or performed. Added static tests and registered them in `backend/test.sh`. No real Pinecone/Firestore provider proof, benchmark evidence, load/recall/latency output, telemetry sink/alert policy, or production approval is claimed. | RED: `cd backend && pytest tests/unit/test_memory_vector_search_provider_readiness.py -q` → `4 failed in 0.07s` (missing runner/docs). Intermediate runner-only GREEN blocked on docs: after adding runner, same command → `1 failed, 3 passed in 0.05s`; after Oracle doc update and format, same command → `1 failed, 3 passed in 0.06s` (ticket row missing). Final GREEN/static: `pytest tests/unit/test_memory_vector_search_provider_readiness.py -q` → `4 passed in 0.04s`. Format: `black --line-length 120 --skip-string-normalization scripts/vector_search_provider_readiness.py tests/unit/test_memory_vector_search_provider_readiness.py` → `2 files left unchanged`. Readiness/default: `python3 backend/scripts/vector_search_provider_readiness.py` → JSON summary `status: "NOT_RUN"`, `read_only: true`, `mutation_allowed: false`, `provider_calls_executed: false`, missing prerequisites `PINECONE_API_KEY`, `PINECONE_INDEX_NAME`, `PINECONE_INDEX_HOST`, `V17_PROVIDER_PROOF_FIRESTORE_PROJECT`, `V17_PROVIDER_PROOF_UID`. Readiness/execute without prerequisites: `python3 backend/scripts/vector_search_provider_readiness.py --execute` → exit `2`, same `NOT_RUN`/read-only/no-mutation summary and missing prerequisites. Focused regression: `pytest tests/unit/test_memory_vector_search_service.py -q && pytest tests/unit/test_memory_chat_memory_adapter.py tests/unit/test_memory_developer_memory_adapter.py tests/unit/test_memory_mcp_memory_adapter.py tests/unit/test_memory_product_memory_router.py -q && pytest tests/unit/test_memory_*.py -q` → `17 passed in 0.07s`, `60 passed in 0.18s`, `316 passed, 3 warnings in 1.81s`. Async scan from repo root: `python3 backend/scripts/scan_async_blockers.py` → exit 0 with pre-existing findings only (`HIGH async helpers with blocking: 41`, `STRUCTURAL mixed await+sync DB: 10`). | Remaining P0-7: explicitly execute safe read-only provider proof only when approved credentials/config/proof UID exist; collect real Pinecone/Firestore pagination/refill/timeout, high-volume hydration read-count, load/recall/latency, malformed metadata/cross-user/expired/archive/deleted/duplicate/partial-outage evidence; do not mutate shared `ns2`; production rollout remains BLOCKED. |
| 2026-06-19 | Oracle P0-8 memory production cutover evidence readiness checklist | **Complete for safe cutover readiness/non-claim artifact; production rollout still BLOCKED**: added `backend/scripts/cutover_evidence_readiness.py`, a safe-by-default checklist runner that inventories required memory production-cutover evidence gates with `status=BLOCKED` or `NOT_RUN`, `read_only=true`, `mutation_allowed=false`, `network_or_provider_calls_executed=false`, `benchmark_evidence_collected=false`, `approval_claimed=false`, and `production_rollout_approved=false`. Gates cover milestone Oracle/final approval, real Pinecone validation, real Firestore/cloud IAM/rules validation, recall/precision/latency/no-silent-data-loss benchmarks, production metrics aggregation/central telemetry, T20 repair/projection-consistency, T21 `/v3` compatibility and cursor pagination, T22/T23 external writes and caller coverage, and production cutover approval. Each gate lists required proof commands/artifacts and blockers with empty evidence arrays. No cloud/provider calls, mutations, benchmark runs, central telemetry integration, T20/T21/T22/T23 completion, final approval, or production cutover approval is claimed. | RED: `cd backend && pytest tests/unit/test_memory_cutover_evidence_readiness.py -q` → `4 failed in 0.07s` (missing runner/docs). Intermediate: after adding runner/docs scaffolding, same command → `2 failed, 2 passed in 0.05s` (missing exact script/doc terms). Final GREEN/static/readiness/regression/async: `cd backend && black --line-length 120 --skip-string-normalization scripts/cutover_evidence_readiness.py tests/unit/test_memory_cutover_evidence_readiness.py && pytest tests/unit/test_memory_cutover_evidence_readiness.py -q` → `2 files left unchanged`, `4 passed in 0.04s`; readiness/default from repo root `python3 backend/scripts/cutover_evidence_readiness.py` → summary `{'status': 'BLOCKED', 'read_only': True, 'mutation_allowed': False, 'network_or_provider_calls_executed': False, 'benchmark_evidence_collected': False, 'approval_claimed': False, 'production_rollout_approved': False}`, gates `milestone_oracle_final_approval, production_cutover_approval, production_metrics_aggregation_central_telemetry, real_firestore_cloud_iam_rules_validation, real_pinecone_validation, recall_precision_latency_no_silent_data_loss_benchmarks, t20_repair_projection_consistency, t21_v3_compatibility_cursor_pagination, t22_t23_external_writes_and_caller_coverage`; readiness/execute from repo root `python3 backend/scripts/cutover_evidence_readiness.py --execute` → summary `{'status': 'BLOCKED', 'read_only': True, 'mutation_allowed': False, 'execute_requested': True, 'network_or_provider_calls_executed': False, 'benchmark_evidence_collected': False, 'approval_claimed': False, 'production_rollout_approved': False}`; full memory regression `cd backend && pytest tests/unit/test_memory_*.py -q` → `320 passed, 3 warnings in 1.88s`; async scan from repo root `python3 backend/scripts/scan_async_blockers.py` → exit 0 with pre-existing findings only (`HIGH async helpers with blocking: 41`, `STRUCTURAL mixed await+sync DB: 10`). | Next: execute real P0-8 gates only with explicit approved credentials/environments; otherwise continue the next Oracle P0/P1 slice by filling missing T20 repair/projection-consistency and T21 `/v3` compatibility/cursor-pagination evidence without claiming production approval. |
| 2026-06-19 | Oracle P0-8 T20 repair/projection-consistency readiness/proof matrix | **Complete for safe T20 evidence-matrix/non-claim artifact; production rollout still BLOCKED**: added `backend/scripts/t20_repair_projection_consistency_readiness.py`, a safe read-only readiness matrix for the P0-8 T20 gate. Default and `--execute` modes return `status=BLOCKED`, `read_only=true`, `mutation_allowed=false`, `network_or_provider_calls_executed=false`, `provider_calls_executed=false`, `benchmark_evidence_collected=false`, `approval_claimed=false`, and `production_rollout_approved=false`. The matrix inventories `NOT_RUN` proof cases with empty evidence arrays for projection_commit_id parity, account_generation parity, item_revision/source_commit_id/content_hash parity, tombstone/deleted source handling, stale physical vector detection, duplicate vector detection, repair outbox enqueue/dead-letter/backlog, repair worker convergence, shared ns2 legacy/memory isolation under stale candidates, and no silent data loss. It links existing seams/runners (`vector_search_provider_readiness.py`, `v17_shared_ns2_legacy_isolation_readiness.py`, `v17_pinecone_repair_validation_readiness.py`, `memory_vector_repair_outbox_telemetry.py`, repair outbox record/worker modules, vector metadata gateway, vector search service) and updates `cutover_evidence_readiness.py` so the T20 gate points at this artifact and the required projection_commit_id/account_generation/item_revision/source_commit_id/content_hash, repair outbox, and shared ns2 proof scope. No Pinecone/Firestore/cloud call, shared `ns2` mutation, Firestore mutation, benchmark, telemetry sink integration, T20 completion, final approval, or production approval is claimed. | RED: `cd backend && pytest tests/unit/test_memory_t20_repair_projection_consistency_readiness.py -q` → `4 failed in 0.08s` (missing runner/docs/cutover links). GREEN/static/readiness/regression/async: `cd backend && black --line-length 120 --skip-string-normalization scripts/t20_repair_projection_consistency_readiness.py scripts/cutover_evidence_readiness.py tests/unit/test_memory_t20_repair_projection_consistency_readiness.py && pytest tests/unit/test_memory_t20_repair_projection_consistency_readiness.py -q && pytest tests/unit/test_memory_cutover_evidence_readiness.py -q` → `3 files left unchanged`, `4 passed in 0.04s`, `4 passed in 0.04s`; readiness/default `python3 backend/scripts/t20_repair_projection_consistency_readiness.py` → JSON summary `status=BLOCKED`, `read_only=true`, `mutation_allowed=false`, `provider_calls_executed=false`, `production_rollout_approved=false`; readiness/execute `python3 backend/scripts/t20_repair_projection_consistency_readiness.py --execute` → same summary with `execute_requested=true` and no provider calls; full memory regression `cd backend && pytest tests/unit/test_memory_*.py -q` → `324 passed, 3 warnings in 1.93s`; async scan `python3 backend/scripts/scan_async_blockers.py` → exit 0, pre-existing findings only (`HIGH async helpers with blocking: 41`, `STRUCTURAL mixed await+sync DB: 10`). | Next: add safe T21 `/v3` compatibility/cursor proof matrix or T22/T23 external writes/caller coverage matrix; real T20 proof remains blocked until approved Pinecone/Firestore credentials/config and an explicit safe execution plan exist. |
| 2026-06-19 | Oracle P0-8 T21 `/v3` compatibility and cursor pagination readiness/proof matrix | **Complete for safe T21 evidence-matrix/non-claim artifact; production rollout still BLOCKED**: added `backend/scripts/v17_t21_v3_compatibility_cursor_readiness.py`, a safe read-only readiness matrix for the P0-8 T21 gate. Default and `--execute` modes return `status=BLOCKED`, `read_only=true`, `mutation_allowed=false`, `network_or_provider_calls_executed=false`, `provider_calls_executed=false`, `benchmark_evidence_collected=false`, `approval_claimed=false`, and `production_rollout_approved=false`. The matrix inventories `NOT_RUN` proof cases with empty evidence arrays for `/v3` endpoint compatibility, stable cursor pagination, category filters, stable ordering, disabled/malformed/no-grant behavior, enabled-but-empty behavior, deleted/non-active records, Archive default-unavailable, external response shape compatibility, developer category filtering, MCP REST/SSE shape consistency, and product/developer/MCP/chat caller regression evidence. It references existing route/adapter/test surfaces (`backend/routers/memories.py GET /v3/memories`, `backend/database/memories.py get_memories`, memory product/developer/MCP/chat adapters, MCP REST/SSE routers, developer router, existing memory read tests) and updates `cutover_evidence_readiness.py` so the T21 gate points at this artifact and required proof scope. No production traffic, Firestore/Pinecone/cloud call, mutation, benchmark, telemetry sink integration, T21 completion, final approval, or production approval is claimed; `production_rollout_approved=false`. | RED: `cd backend && pytest tests/unit/test_memory_t21_v3_compatibility_cursor_readiness.py -q` → `4 failed in 0.07s` (missing runner/docs/cutover links). GREEN/static/readiness/regression/async: `cd backend && black --line-length 120 --skip-string-normalization scripts/v17_t21_v3_compatibility_cursor_readiness.py scripts/cutover_evidence_readiness.py tests/unit/test_memory_t21_v3_compatibility_cursor_readiness.py && pytest tests/unit/test_memory_t21_v3_compatibility_cursor_readiness.py -q && pytest tests/unit/test_memory_cutover_evidence_readiness.py -q` → `2 files reformatted, 1 file left unchanged`, `4 passed in 0.05s`, `4 passed in 0.04s`; readiness/default from repo root `python3 backend/scripts/v17_t21_v3_compatibility_cursor_readiness.py` → summary `{'status': 'BLOCKED', 'read_only': True, 'mutation_allowed': False, 'network_or_provider_calls_executed': False, 'provider_calls_executed': False, 'benchmark_evidence_collected': False, 'approval_claimed': False, 'production_rollout_approved': False}` with proof cases `category_filters_and_developer_category_filtering`, `deleted_non_active_and_archive_default_unavailable`, `disabled_malformed_no_grant_behavior`, `enabled_but_empty_behavior`, `external_response_shape_compatibility`, `mcp_rest_sse_shape_consistency`, `product_developer_mcp_chat_caller_regression`, `stable_cursor_pagination_and_ordering`, `v3_endpoint_legacy_shape_compatibility`; readiness/execute `python3 backend/scripts/v17_t21_v3_compatibility_cursor_readiness.py --execute` → same summary with `execute_requested=true` and all proof cases `NOT_RUN`/empty evidence; cutover readiness T21 gate from `python3 backend/scripts/cutover_evidence_readiness.py` → `BLOCKED [] python3 backend/scripts/v17_t21_v3_compatibility_cursor_readiness.py`; full memory regression `cd backend && pytest tests/unit/test_memory_*.py -q` → `328 passed, 3 warnings in 1.96s`; async scan from repo root `python3 backend/scripts/scan_async_blockers.py` → exit 0, pre-existing findings only (`HIGH async helpers with blocking: 41`, `STRUCTURAL mixed await+sync DB: 10`). Next action/blockers: collect real `/v3` compatibility/cursor evidence and product/developer/MCP/chat caller regression output before any T21/cutover approval. |
| 2026-06-19 | Oracle P0-8 T22/T23 external writes and caller coverage readiness/proof matrix | **Complete for safe T22/T23 evidence-matrix/non-claim artifact; production rollout still BLOCKED**: added `backend/scripts/v17_t22_t23_external_writes_caller_coverage_readiness.py`, a safe read-only readiness matrix for the P0-8 T22/T23 gate. Default and `--execute` modes return `status=BLOCKED`, `read_only=true`, `mutation_allowed=false`, `network_or_provider_calls_executed=false`, `provider_calls_executed=false`, `benchmark_evidence_collected=false`, `approval_claimed=false`, and `production_rollout_approved=false`. The matrix inventories `NOT_RUN` proof cases with empty evidence arrays for external create/edit/delete/list/search write/read convergence, Developer API write/read paths, MCP REST/SSE write/read/list/search paths, chat/tool/agent caller coverage, durable dual-write/outbox or memory-write convergence plan, delete/review/import compatibility, no legacy unsafe fallback after memory writes, app/key/scope grant enforcement, Archive default-unavailable, response-shape compatibility, and rollback/disable behavior. It references existing route/adapter/test surfaces (`backend/routers/memories.py`, `backend/routers/developer.py`, `backend/routers/mcp.py`, `backend/routers/mcp_sse.py`, `backend/routers/tools.py`, `backend/routers/agent_tools.py`, `backend/database/memories.py`, legacy write guard, external app/key/scope authorization, memory product/developer/MCP/chat adapters, and existing caller/guard tests) and updates `cutover_evidence_readiness.py` so the T22/T23 gate points at this artifact and required proof scope. No production traffic, external writes, Firestore/Pinecone/cloud call, mutation, benchmark, telemetry sink integration, T22/T23 completion, final approval, or production approval is claimed; `production_rollout_approved=false`. | RED: `cd backend && pytest tests/unit/test_memory_t22_t23_external_writes_caller_coverage_readiness.py -q` → `4 failed in 0.07s` (missing runner/docs/cutover links). GREEN/static/readiness/regression/async: `cd backend && black --line-length 120 --skip-string-normalization scripts/v17_t22_t23_external_writes_caller_coverage_readiness.py scripts/cutover_evidence_readiness.py tests/unit/test_memory_t22_t23_external_writes_caller_coverage_readiness.py && pytest tests/unit/test_memory_t22_t23_external_writes_caller_coverage_readiness.py -q` → `1 file reformatted, 2 files left unchanged`, `4 passed in 0.05s`; readiness/default from repo root `python3 backend/scripts/v17_t22_t23_external_writes_caller_coverage_readiness.py` → JSON `status: "BLOCKED"`, `read_only: true`, `mutation_allowed: false`, `network_or_provider_calls_executed: false`, `provider_calls_executed: false`, `benchmark_evidence_collected: false`, `approval_claimed: false`, `production_rollout_approved: false`, all proof cases `NOT_RUN` with empty evidence arrays; `python3 backend/scripts/v17_t22_t23_external_writes_caller_coverage_readiness.py --execute` → exit 0, same blocked/read-only/no-provider/no-approval summary with `execute_requested: true`; cutover readiness `python3 backend/scripts/cutover_evidence_readiness.py` → `status: "BLOCKED"` and T22/T23 gate required command `python3 backend/scripts/v17_t22_t23_external_writes_caller_coverage_readiness.py`; focused tests `pytest tests/unit/test_memory_t22_t23_external_writes_caller_coverage_readiness.py -q && pytest tests/unit/test_memory_cutover_evidence_readiness.py -q` → `4 passed in 0.05s`, `4 passed in 0.04s`; full memory regression `pytest tests/unit/test_memory_*.py -q` → `332 passed, 3 warnings in 2.03s`; async scan from repo root `python3 backend/scripts/scan_async_blockers.py` → exit 0 with pre-existing findings only (`HIGH async helpers with blocking: 41`, `STRUCTURAL mixed await+sync DB: 10`). | Remaining P0-8: collect real T20/T21/T22/T23 evidence, implement/verify durable external write convergence, broaden caller regression across product/developer/MCP/chat/tool/agent paths, run real Pinecone/Firestore/IAM proof, benchmarks, central telemetry, rollback drill, and obtain explicit production approval before cutover. |
| 2026-06-19 | Oracle P1-1 rollout/control document parsing hardening | **Complete for first local P1-1 hardening slice; production rollout still BLOCKED**: hardened `backend/utils/memory/default_read_rollout.py` so rollout docs now require exact `uid` and `schema_version=1`; missing/mismatched uid fails closed with `uid_mismatch`, missing/unsupported schema fails closed with `unsupported_rollout_schema`, default-memory and Archive capability parsing use only canonical nested `grants.<consumer>.default_memory` / `grants.<consumer>.archive`, stale top-level aliases no longer override absent or explicit-false nested grants, and touched consumer aliases (`developer`, `chat`) no longer create ambiguous revocation precedence. Per-user rollout reads now use bounded `.get(timeout=2.0)` where supported and normalize Firestore/transport-style exceptions to fail-closed `rollout_read_failed`. No production traffic, Firestore/Pinecone/cloud/provider calls, benchmark, telemetry sink integration, or cutover approval is claimed; Oracle rollout verdict remains BLOCK / NO-GO. | RED: `cd backend && pytest tests/unit/test_default_read_rollout_decision.py -q` → `ImportError: cannot import name 'DEFAULT_READ_ROLLOUT_SCHEMA_VERSION' ... 1 error in 0.14s`. GREEN/parent regression: `pytest tests/unit/test_default_read_rollout_decision.py tests/unit/test_memory_chat_memory_adapter.py tests/unit/test_memory_developer_memory_adapter.py tests/unit/test_memory_mcp_memory_adapter.py tests/unit/test_memory_product_memory_router.py tests/unit/test_memory_non_active_route_admin_endpoint.py -q` → `83 passed in 0.39s`; `pytest tests/unit/test_memory_*.py -q` → `334 passed, 3 warnings in 1.98s`; `python3 backend/scripts/scan_async_blockers.py` → exit 0 with pre-existing `HIGH async helpers with blocking: 41`, `STRUCTURAL mixed await+sync DB: 10`. | Next P1 action: add schema migration/compat notes for existing rollout docs and continue P1-1/P1-3 with global gate/read-write convergence timeout semantics plus caller compatibility/error-shape coverage before any rollout. |
| 2026-06-19 | Oracle P1-1 rollout schema_version=1 migration/readiness contract | **Complete for local schema/readiness documentation slice; production rollout still BLOCKED**: added `docs/epics/memory_rollout_schema_migration.md` and safe-by-default `backend/scripts/rollout_schema_readiness.py`. The artifact makes `users/{uid}/memory_control/state` `schema_version=1` explicit, requires exact `uid`, inventories canonical nested `grants.<consumer>.default_memory` for `mcp`, `developer_api`, and `omi_chat`, documents optional canonical `grants.<consumer>.archive` as explicit Archive capability only, and lists top-level `*_default_memory_grant` aliases plus nested `developer`/`chat` aliases as rejected legacy compatibility shapes. Added `backend/tests/unit/test_rollout_schema_readiness.py` to prove checked-in examples parse with the shared rollout normalizer, rejected legacy shapes fail closed, and the migration note keeps canonical sections free of top-level alias examples. Registered the new test in `backend/test.sh`. Updated historical ticket prose so old memory docs no longer present top-level grant aliases as accepted examples; only the new migration note lists them as rejected legacy shapes. No production traffic, Firestore/Pinecone/cloud/provider calls, Firestore reads/writes, benchmark, telemetry sink integration, migration execution, or approval is claimed; `production_rollout_approved=false` and Oracle remains BLOCK / NO-GO. | RED: `cd backend && pytest tests/unit/test_rollout_schema_readiness.py -q` → `4 failed in 0.07s` (missing readiness runner and migration note). Interim GREEN blocked on docs: after adding script/docs, same command → `3 passed, 1 failed in 0.06s` (Oracle/ticket docs missing `rollout_schema_readiness.py`). Final GREEN/static: `black --line-length 120 --skip-string-normalization scripts/rollout_schema_readiness.py tests/unit/test_rollout_schema_readiness.py && pytest tests/unit/test_rollout_schema_readiness.py -q` → `1 file reformatted, 1 file left unchanged`, `4 passed in 0.05s`. Readiness: `python3 backend/scripts/rollout_schema_readiness.py --execute` → exit 0 with `status: "NOT_RUN"`, `read_only: true`, `mutation_allowed: false`, `network_or_provider_calls_executed: false`, `firestore_reads_executed: false`, `firestore_writes_executed: false`, `canonical_schema_version: 1`, and `production_rollout_approved: false`. Full memory regression: `cd backend && pytest tests/unit/test_memory_*.py -q` → `338 passed, 3 warnings in 2.05s`. Async scan: `python3 backend/scripts/scan_async_blockers.py` → exit 0 with pre-existing `HIGH async helpers with blocking: 41`, `STRUCTURAL mixed await+sync DB: 10`. Docs hygiene: no truncation marker literals or SUB chars under `docs/epics/memory*`. | Next P1 action: align global gate and read/write convergence timeout/error semantics, then run approved real migration inventory/proof before any production rollout stage. |
| 2026-06-19 | Oracle P1-1 global/write-convergence gate bounded read semantics | **Complete for narrow local P1-1 gate-read remediation slice; production rollout still BLOCKED**: aligned `read_global_read_gate(...)` and `read_write_convergence_gate(...)` with the per-user rollout read helper so Firestore document reads use bounded `.get(timeout=2.0)` where supported. Timeout, permission, deadline, and transport-style exceptions now fail closed with explicit low-cardinality reasons: `global_read_gate_read_failed` denies product memory reads, and `write_convergence_gate_read_failed` leaves write convergence not ready. Missing and malformed global/write-convergence gate documents keep their existing explicit fail-closed reasons. Archive remains default-unavailable, stale Short-term is not made default-visible, and exact per-user `uid`/`schema_version=1`/canonical-grant behavior is preserved. No production traffic, Firestore/Pinecone/cloud/provider calls, Firestore reads/writes, benchmark, telemetry sink integration, migration execution, or approval is claimed. | RED: `cd backend && pytest tests/unit/test_default_read_rollout_decision.py -q` → `4 failed, 13 passed in 0.12s` (global/write-convergence gate reads recorded timeout `None` instead of `2.0`). GREEN/focused: same command after implementation → `17 passed in 0.06s`; final formatted focused/regression chain `cd backend && black --line-length 120 --skip-string-normalization utils/memory/default_read_rollout.py tests/unit/test_default_read_rollout_decision.py && pytest tests/unit/test_default_read_rollout_decision.py -q && pytest tests/unit/test_memory_*.py -q` → `2 files left unchanged`, `17 passed in 0.05s`, `340 passed, 3 warnings in 2.03s`. Readiness: `python3 backend/scripts/rollout_schema_readiness.py --execute` → exit 0 with `status: "NOT_RUN"`, `read_only: true`, `mutation_allowed: false`, `network_or_provider_calls_executed: false`, `firestore_reads_executed: false`, `firestore_writes_executed: false`, `canonical_schema_version: 1`, and `production_rollout_approved: false`. Async scan: `python3 backend/scripts/scan_async_blockers.py` → exit 0 with pre-existing `HIGH async helpers with blocking: 41`, `STRUCTURAL mixed await+sync DB: 10`. Docs hygiene: no truncation marker literals or SUB chars under `docs/epics/memory*`. | Next P1 action: run an approved read-only production/staging inventory for existing rollout/global/convergence documents and continue P1-3 caller/API compatibility contract hardening. |
| 2026-06-19 | Oracle P1-3 Developer API category/response-shape first hardening slice | **Complete for first narrow local Developer API compatibility hardening slice; production rollout still BLOCKED**: `GET /v1/dev/user/memories` no longer forces category-filtered requests into explicit legacy-safe fallback solely because `categories` is present. Filtered and unfiltered Developer default-list calls now share app/key/scope grant authorization, server-owned rollout reads, and `search_memory_default_developer_memories(...)`; the adapter accepts normalized category filters, filters memory default-readable results locally, and still touches legacy only if a shared rollout decision explicitly returns `USE_LEGACY_SAFE`. Developer memory formatting now propagates authoritative source visibility (`visibility_source="memory_item.visibility"`) and marks compatibility-derived fields instead of silently fabricating them: `category=other`, `reviewed=False`, `edited=False`, and `manually_added=False` each carry explicit source markers for missing memory/source fields. Archive remains default-unavailable through Developer policy (`archive_capability=False`), stale Short-term remains filtered by shared default visibility policy, and malformed/no-grant/disabled rollout states still fail closed without unsafe legacy reads. No production traffic, Firestore/Pinecone/cloud/provider calls, Firestore reads/writes, benchmark, telemetry sink integration, or approval is claimed. | RED: `cd backend && pytest tests/unit/test_memory_developer_memory_adapter.py -q` → `3 failed, 20 passed in 0.15s` (`developer_category_legacy_safe_fallback_explicit` still present, `categories` unsupported by adapter, memory Developer formatting returned fabricated private visibility). GREEN/format/focused/regression: `cd backend && black --line-length 120 --skip-string-normalization utils/memory/v17_developer_memory_adapter.py utils/memory/memory_read_api.py routers/developer.py tests/unit/test_memory_developer_memory_adapter.py && pytest tests/unit/test_memory_developer_memory_adapter.py -q && pytest tests/unit/test_memory_*.py -q` → `4 files left unchanged`, `23 passed in 0.08s`, `352 passed, 3 warnings in 2.06s`. P1-3 readiness from repo root remains safe/read-only and blocked: `python3 backend/scripts/p1_3_caller_api_compatibility_readiness.py --execute` → status `BLOCKED`, `read_only=true`, `mutation_allowed=false`, `network_or_provider_calls_executed=false`, `provider_calls_executed=false`, `firestore_reads_executed=false`, `firestore_writes_executed=false`, `benchmark_evidence_collected=false`, `production_rollout_approved=false`, `approval_claimed=false`, with 13 surfaces and 11 behavior cases. Async scan: `python3 backend/scripts/scan_async_blockers.py` from repo root exited 0 with pre-existing `HIGH async helpers with blocking: 41`, `STRUCTURAL mixed await+sync DB: 10`. Docs hygiene: no truncation-marker literals or SUB chars under `docs/epics/memory*`. |
| 2026-06-19 | Oracle P1-3 MCP category/review/manual response-shape source hardening slice | **Complete for narrow local MCP response-shape/filter hardening; production rollout still BLOCKED**: memory MCP list/search formatting now exposes explicit compatibility-derived source semantics instead of silently fabricating unsupported legacy fields. MCP memory default results keep `category=other`, `reviewed=False`, and `manually_added=False` for legacy-shape compatibility but add `category_source="mcp_memory_compatibility_default_no_source_category"`, `reviewed_source="mcp_memory_compatibility_default_no_review_state"`, and `manually_added_source="mcp_memory_compatibility_default_no_manual_state"`. MCP REST `CleanerMemory` exposes these additive fields plus memory/default policy metadata so REST does not strip the source markers, while SSE tool responses already return the adapter dictionaries. `list_default_mcp_memories(...)` now accepts category/review/manual filters and applies them only to those explicit compatibility fields; REST and SSE get/list paths pass normalized filters into the shared memory list adapter. Archive remains default-unavailable, stale Short-term remains filtered by the shared default-memory visibility policy, and malformed/no-grant/disabled rollout states still fail closed without unsafe legacy reads unless the shared decision explicitly returns `USE_LEGACY_SAFE`. No production traffic, Firestore/Pinecone/cloud/provider calls, Firestore reads/writes, benchmark, telemetry sink integration, or approval is claimed. | RED: `cd backend && pytest tests/unit/test_memory_mcp_memory_adapter.py -q` → `3 failed, 21 passed in 0.17s` (`category_source` missing, `list_default_mcp_memories()` rejected `categories`, and REST `CleanerMemory` did not expose source fields). GREEN/format/focused/regression: `cd backend && black --line-length 120 --skip-string-normalization utils/mcp_memories.py routers/mcp.py routers/mcp_sse.py tests/unit/test_memory_mcp_memory_adapter.py && pytest tests/unit/test_memory_mcp_memory_adapter.py -q && pytest tests/unit/test_memory_*.py -q` → `1 file reformatted, 3 files left unchanged`, `24 passed in 0.13s`, `355 passed, 3 warnings in 2.06s`. P1-3 readiness from repo root remains safe/read-only and blocked: `python3 backend/scripts/p1_3_caller_api_compatibility_readiness.py --execute` → status `BLOCKED`, `read_only=true`, `mutation_allowed=false`, `network_or_provider_calls_executed=false`, `provider_calls_executed=false`, `firestore_reads_executed=false`, `firestore_writes_executed=false`, `benchmark_evidence_collected=false`, `production_rollout_approved=false`, `approval_claimed=false`, with 13 surfaces and 11 behavior cases. Async scan: `python3 backend/scripts/scan_async_blockers.py` from repo root exited 0 with pre-existing `HIGH async helpers with blocking: 41`, `STRUCTURAL mixed await+sync DB: 10`. Docs hygiene: no truncation-marker literals or SUB chars under `docs/epics/memory*`. |
| 2026-06-19 | Oracle P1-5/P1-3 tools REST, agent execute-tool, and chat get/list parity hardening | **Complete for narrow local runtime hardening; production rollout still BLOCKED**: extended the fail-closed memory memory evidence boundary guard to `backend/routers/tools.py` memory REST endpoints and `backend/routers/agent_tools.py` `POST /v1/agent/execute-tool` results, so memory-like memory output from tools REST or agent execute wrappers is preserved only with quoted-evidence/source/policy markers and otherwise collapses to `No memories available for this request.`. Added `list_default_chat_memories_decision_text(...)` and wired `get_memories_tool` through the same explicit Omi chat rollout decision family as `search_memories_tool`: disabled/malformed/no-grant states deny without legacy fallback, enabled-empty returns `No memory default memories found.`, and enabled results are quoted memory default evidence with `source_marker=memory_default_memory`, content caps, untrusted-evidence boundary, and `archive_default_visible=False`. Legacy fallback remains available only through explicit `USE_LEGACY_SAFE`; no stale Short-term or Archive default visibility was introduced. No production traffic, Firestore/Pinecone/cloud/provider calls, Firestore reads/writes beyond unit fakes, benchmark, telemetry sink integration, or approval is claimed. | RED: `cd backend && pytest tests/unit/test_memory_chat_memory_adapter.py tests/unit/test_memory_chat_memory_tool_caller.py -q` → collection error `ImportError: cannot import name 'list_default_chat_memories_decision_text' from 'utils.memory.v17_chat_memory_adapter'` (`1 error in 0.19s`). GREEN/format/focused/regression: `cd backend && black --line-length 120 --skip-string-normalization utils/memory/v17_chat_memory_adapter.py utils/retrieval/tools/memory_tools.py routers/tools.py routers/agent_tools.py tests/unit/test_memory_chat_memory_adapter.py tests/unit/test_memory_chat_memory_tool_caller.py && pytest tests/unit/test_memory_chat_memory_adapter.py tests/unit/test_memory_chat_memory_tool_caller.py -q && pytest tests/unit/test_memory_*.py -q` → `6 files left unchanged`, `17 passed in 0.10s`, `368 passed, 3 warnings in 2.12s`. Readiness/non-claims: `python3 backend/scripts/p1_3_caller_api_compatibility_readiness.py --execute` → `status=BLOCKED`, `read_only=true`, `mutation_allowed=false`, `network_or_provider_calls_executed=false`, `provider_calls_executed=false`, `firestore_reads_executed=false`, `firestore_writes_executed=false`, `benchmark_evidence_collected=false`, `production_rollout_approved=false`, `approval_claimed=false`, with 13 surfaces and 11 behavior cases; `python3 backend/scripts/p1_3_v3_external_compatibility_readiness.py --execute` → `status=BLOCKED`, `read_only=true`, `mutation_allowed=false`, `network_or_provider_calls_executed=false`, `provider_calls_executed=false`, `firestore_reads_executed=false`, `firestore_writes_executed=false`, `benchmark_evidence_collected=false`, `production_rollout_approved=false`, `approval_claimed=false`, with 7 surfaces and 7 gaps. Async scan: `python3 backend/scripts/scan_async_blockers.py` exited 0 with pre-existing `HIGH async helpers with blocking: 41`, `STRUCTURAL mixed await+sync DB: 10`. | Next P1 action: continue P1-3/P1-5 with tools REST runtime memory read adapter parity (not only boundary guarding), route-level FastAPI response-model tests for tools/agent wrappers, and `/v3` compatibility decisions for disabled/no-grant/enabled-empty/cursor semantics before any production rollout. |
| 2026-06-19 | Oracle P1-5/P1-3 tools REST/agent FastAPI TestClient readiness gap | **Complete for safe readiness/proof gap artifact; production rollout still BLOCKED**: added `backend/scripts/v17_p1_5_tools_fastapi_testclient_readiness.py` and `backend/tests/unit/test_memory_p1_5_tools_fastapi_testclient_readiness.py`, registered the test in `backend/test.sh`, and pinned the exact remaining route-level FastAPI `TestClient` production-dependency proof gap for `backend/routers/tools.py` `GET /v1/tools/memories`, `backend/routers/tools.py` `POST /v1/tools/memories/search`, and `backend/routers/agent_tools.py` `POST /v1/agent/execute-tool`. FastAPI `TestClient` production-dependency proof remains BLOCKED/NOT_RUN in this environment because `fastapi` is not importable with the local Python used for verification; this artifact is intentionally read-only and does not execute app startup, network/provider/cloud calls, Firestore/Pinecone calls, mutations, benchmarks, telemetry, or approval. It inventories required future proof cases for response-model serialization, quoted-evidence boundary preservation, denied/no-grant fail-closed states, enabled-empty stability, prompt-injection payload preservation as quoted data, and Archive/stale Short-term default-unavailable behavior. Existing local non-TestClient proof remains linked to `backend/tests/unit/test_memory_tools_agent_route_response_shape.py` and `backend/tests/unit/test_memory_tools_rest_memory_runtime_adapter.py`; no runtime behavior changed and Oracle remains BLOCKED / NO-GO. | RED: `cd backend && pytest tests/unit/test_memory_p1_5_tools_fastapi_testclient_readiness.py -q` → `4 failed in 0.15s` (missing readiness runner, test.sh registration, and docs links). GREEN/format/focused/readiness/regression/async/docs hygiene: `cd backend && black --line-length 120 --skip-string-normalization scripts/v17_p1_5_tools_fastapi_testclient_readiness.py tests/unit/test_memory_p1_5_tools_fastapi_testclient_readiness.py && pytest tests/unit/test_memory_p1_5_tools_fastapi_testclient_readiness.py -q` → `2 files left unchanged`, `4 passed in 0.12s`; readiness from repo root `python3 backend/scripts/v17_p1_5_tools_fastapi_testclient_readiness.py --execute` → `BLOCKED NOT_RUN True False False False False False False False False False 3 6`; focused route/runtime regression `pytest tests/unit/test_memory_chat_memory_adapter.py tests/unit/test_memory_chat_memory_tool_caller.py tests/unit/test_memory_tools_agent_route_response_shape.py tests/unit/test_memory_tools_rest_memory_runtime_adapter.py tests/unit/test_memory_p1_5_tools_fastapi_testclient_readiness.py -q` → `29 passed, 1 warning in 0.30s`; full memory regression `pytest tests/unit/test_memory_*.py -q` → `380 passed, 3 warnings in 2.33s`; async scan `python3 backend/scripts/scan_async_blockers.py` exited 0 with pre-existing `HIGH async helpers with blocking: 41`, `STRUCTURAL mixed await+sync DB: 10`; docs hygiene found no literal truncation markers or SUB chars under `docs/epics/memory*`. |
| 2026-06-19 | Oracle P1-5/P1-3 tools FastAPI TestClient dependency blocker evidence | **Complete for blocker-evidence tightening; production rollout still BLOCKED**: attempted the next narrow FastAPI `TestClient` route proof for `GET /v1/tools/memories`, `POST /v1/tools/memories/search`, and `POST /v1/agent/execute-tool` with a RED route-proof test, but the local verification Python still cannot import `fastapi`. The repo dependency source pins `fastapi==0.121.0` and `httpx==0.28.0` in `backend/requirements.txt`; a bounded install attempt `python3 -m pip install --user 'fastapi==0.121.0'` failed with `externally-managed-environment` / PEP 668, so no `--break-system-packages` override, lockfile change, broad install, app startup, route execution, network/provider/cloud call, Firestore/Pinecone call, mutation, benchmark, telemetry sink integration, or approval was performed. Tightened `backend/scripts/v17_p1_5_tools_fastapi_testclient_readiness.py` and its unit test so the BLOCKED/NOT_RUN artifact now emits exact dependency evidence, attempted command, exit code, Python major/minor, and safe next dependency options. Oracle remains BLOCKED / NO-GO. | RED route proof attempt: `cd backend && pytest tests/unit/test_memory_tools_fastapi_testclient_route_proof.py -q` → `4 errors in 0.08s` (`ModuleNotFoundError: No module named 'fastapi'`); bounded install attempt: `python3 -m pip install --user 'fastapi==0.121.0'` → exit 1, `externally-managed-environment` / PEP 668; RED blocker-artifact test: `cd backend && pytest tests/unit/test_memory_p1_5_tools_fastapi_testclient_readiness.py -q` → `1 failed, 4 passed in 0.18s` (`KeyError: 'dependency_evidence'`). GREEN/format/focused/readiness/regression: `cd backend && black --line-length 120 --skip-string-normalization scripts/v17_p1_5_tools_fastapi_testclient_readiness.py tests/unit/test_memory_p1_5_tools_fastapi_testclient_readiness.py && pytest tests/unit/test_memory_p1_5_tools_fastapi_testclient_readiness.py -q` → `1 file reformatted, 1 file left unchanged`, `5 passed in 0.15s`; readiness from repo root summarized `BLOCKED NOT_RUN True False False False False False False False False False 3 6 fastapi==0.121.0 httpx==0.28.0 1 ModuleNotFoundError`; focused route/runtime regression `pytest tests/unit/test_memory_chat_memory_adapter.py tests/unit/test_memory_chat_memory_tool_caller.py tests/unit/test_memory_tools_agent_route_response_shape.py tests/unit/test_memory_tools_rest_memory_runtime_adapter.py tests/unit/test_memory_p1_5_tools_fastapi_testclient_readiness.py -q` → `30 passed, 1 warning in 0.31s`; full memory regression `pytest tests/unit/test_memory_*.py -q` → `381 passed, 3 warnings in 2.35s`; async scan `python3 backend/scripts/scan_async_blockers.py` exited 0 with pre-existing `HIGH async helpers with blocking: 41`, `STRUCTURAL mixed await+sync DB: 10`. | Next P1 action: run the existing route-proof RED test in a repo-managed virtualenv or CI/backend image with `backend/requirements.txt` installed, then promote the safe TestClient route proof into committed tests without production traffic or external calls. |

## Readiness evidence markers

Operational gate markers referenced by readiness unit tests: `docs/operational/memory_readiness_evidence_markers.md`. Oracle milestone review transcripts and the full implementation journal: `omi-ingestion-benchmark/docs/research-logs/pipeline-memory/`.

