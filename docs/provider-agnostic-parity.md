# Provider-independent app parity

Working acceptance checklist, inspected 2026-09-07 against the current v5 working tree. This records source-backed capability boundaries. It is not a production-readiness certificate or a live deployment report. Update evidence against the deployed revision before checking an acceptance item.

## Scope and decisions to preserve

- Keep one React Native product UI and lifecycle owner across mobile, desktop and web. Credentials and authenticated transport remain native; the PWA development proxy injects credentials outside browser code. Do not copy the legacy Flutter or Swift product tree. Authority: [AGENTS.md](../AGENTS.md).
- Keep ratified memory projections and completeness semantics. Ready renders are not editable legacy memory records; missing accepted-work or short-term-memory coverage must remain visible. Do not manufacture empty successful projections to satisfy screens. Authority: [ratified contract guide](../packages/contracts/ratified/README.md).
- Keep task account epochs, revision checks, idempotent operation identities, retained uncertain writes and authoritative refresh. The UI does not optimistically claim completion. Authority: [task contract](../packages/contracts/src/domain/tasks.ts), [mutation client](../react-native/src/taskMutationClient.ts), [task UI](../react-native/src/ui/TaskEditor.tsx).
- Keep explicit setup disclosure separate from native sign-in. The old auth-only completion flag is not setup consent. Device connection is optional; no-device users can use chat. Authority: [onboarding owner](../react-native/src/app/useOnboarding.ts).
- The Cloudflare Worker is an existing implemented provider, not a provider-independent implementation merely because its HTTP shell also mounts in Elysia. Its persistence and generation implementation uses D1, R2 and Durable Objects. Authority: [Worker HTTP core](../apps/backend-worker/src/http-core.ts).
- A local seeded QA composition is an integration harness, not a production account/grant source. A first deployed memory-read slice does not complete app parity. Authority: [local entry](../backends/example-platform/apps/service/bin/dev-server.ts), [production entry](../backends/example-platform/apps/service/bin/production-server.ts).

## Current composition boundaries

**App:** [AppOrchestrator](../react-native/src/app/AppOrchestrator.tsx) coordinates shared screens, setup, reads, recording and writes. Apple and Android native transports select the rewritten backend for capture/chat/conversations/memories/tasks/settings routes; legacy account and connector APIs still use the primary cloud path. See [native path policy](../react-native/src/v5BackendOrigin.ts), [Android policy](../react-native/android/app/src/main/java/com/rnruntime/OmiBackendTransport.java) and [PWA proxy policy](../react-native/src/local-proxy.ts). The example-platform development selector currently allows conversation/memory/task/settings/chat-history reads, completed recording transcript GET, and task operations on the PWA proxy and the Android/Apple native transports. It does not expose chat writes, generation SSE, attachments, capture ownership, or transcription POST. Chat copy, browser generation SSE, capture upload retries, library reads, and recording transcript reloads honor nested `retryable:false` on 503, including `development_backend_unsupported`. Capture ownership unavailable is not a transient upload retry. Nested non-retryable generation cancel, including example-platform `development_backend_unsupported` and unmounted 404, is `OMI_DEV_BACKEND_UNSUPPORTED` instead of retryable `OMI_HTTP_TRANSPORT`, so Stop copy says the door is unavailable rather than a transient blip. Transcript and main-chat history doors that cannot be retried omit Check again; grant denials still offer it. Home Retry / Try again is omitted when every failed library door is nested non-retryable. The macOS Home/Conversations/Tasks banner for that door uses mapped unavailable copy instead of "Some of your history isn't loaded yet." Retryable library failures keep that delay copy and Try again. Settings Retry is omitted for nested non-retryable Settings 503s. Apps Retry is omitted for nested non-retryable catalogue 503s, including example-platform `development_backend_unsupported`; omitted 503 retryable still offers Retry. Library Load more omits Try again and the load control when a later page is nested non-retryable. Omitted 503 retryable still means retry. An empty Devices list does not show Apple CoreBluetooth wire tokens such as poweredOn; Bluetooth on with no devices says No Omi device was discovered, and off or permission-needed states use the same human labels as the Devices header. Nested non-retryable Apps enable and Settings privacy writes omit Install / Opt in / Update instead of keeping a disabled control that still names the action. Empty-list Bluetooth hints do not interpolate Android GATT/scan integers or Apple localizedDescription. Rewind omits Start capture when ScreenCaptureKit is unavailable instead of keeping a disabled Start control; status copy stays. An empty Devices list does not keep Scanning for Omi devices after the scan has finished; Apple scan timeout matches Android with No Omi devices found. Compact Home idle capture says Not capturing instead of Paused / Capture is paused. Compact Home live capture without a transcript does not say Listening for speech, and says Live transcript is not available instead of a blank speech line. Compact Home no longer shows a raised microphone that looks like it can start capture. Compact Home Mind Map says View All instead of Expand; that control opens Memories and the preview stays collapsed. Compact Home no longer says Omi disconnected when Bluetooth is on and no device is connected; that header says Omi not connected.

**Worker:** [registered routes](../apps/backend-worker/src/http-core.ts) implement chat admission/history/SSE/cancellation, attachments, indexed recording upload/transcription, conversation reads and service settings. Canonical memories and task mutations require an optional authenticated service binding. Local D1 tasks can be read without that binding; that is not proof of canonical production writes. Unbound D1 conversation and local task handlers return retryable 503 instead of an empty complete page. The unused complete-empty `emptyPage` helper is gone.

**Local portable service:** [app-facing composition](../backends/example-platform/apps/service/app-facing.ts) mounts memories, conversations, tasks, settings, chat, listen, and additional development routes. [dev-server](../backends/example-platform/apps/service/bin/dev-server.ts) builds SQLite stores with a development principal and seeded state. Existing route logic is reusable, but its principal/control/storage composition cannot be promoted unchanged to production.

**Production entry:** [production-server](../backends/example-platform/apps/service/bin/production-server.ts), built by the [Dockerfile](../backends/example-platform/Dockerfile), composes Firebase authorization with PostgreSQL memory reads, task operations, durable indexed device audio uploads and prerecorded transcription. It explicitly returns unavailable for MCP, supplies unavailable accepted-work/STM coverage, persists grounded render responses and limits concurrent non-health requests to two with a 25-second deadline, extended to 135 seconds for transcription requests. It does not mount the full app-facing route set. The [GCP configuration](../backends/example-platform/deploy/gcp/dev/main.tf) targets a development project; configuration existing in the tree does not prove successful rollout, grants or authenticated app access.

## Capability matrix

| Capability / app owner                                                          | Existing implementation and wire                                                                                                                                                                                                                                                                                                                                                                                                                             | Missing implementation or integration                                                                                                                                                                                                                                                                                                                                                                                                                   | Required acceptance evidence                                                                                                                                                                                                                                                                                          |
| ------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Authentication, setup and account binding — native auth + backend authorization | Native Google/Firebase flow, PKCE, secure session storage, refresh/cancel/sign-out; shared RN setup revision. Production memory authorization composes verified Firebase identity → application grant → account control. [Authorization composition](../backends/example-platform/apps/service/composition/firebase-memory-authorization.ts), [Postgres source](../backends/example-platform/drivers/postgres/firebase-application-authorization-source.ts). | A verified token alone does not create an application grant or account binding. Production onboarding/provisioning must establish the existing authority records and lifecycle, without treating a staging bearer or user-supplied account ID as a grant. Installed mobile builds need explicit intended backend configuration. PWA production sign-in is not implemented by the local proxy.                                                           | Fresh and existing users complete real native sign-in; correct account/grant admits access; missing, revoked, wrong-project and wrong-account grants deny correctly; refresh, cancel, sign-out and account changes retire old work.                                                                                   |
| Chat and SSE — `chatClient`, native generation streams                          | `GET/POST /v1/chat-messages`, `GET /v1/chat-generations/:id/events`, cancellation. Worker persists generation events; local portable service has [chat routes](../backends/example-platform/apps/service/routes/chat-messages.ts). Production entry now mounts `GET /v1/chat-messages` under the exact `chat.read` grant; missing grants are 403, granted empty history is an honest empty page, and assistant rows without a unique terminal event are 503. Worker history GET uses that same unique done/cancelled terminal as the assistant outcome authority and returns retryable 503 instead of silently completing an orphan or payload-null assistant row. Unmounted writes, SSE, cancellation and attachments return nested `{error:{code:"not_found"}}` without admission. Nested `not_found` / `development_backend_unsupported` write doors disable desktop Ask, mobile Ask, and composer Send instead of leaving a live CTA. Composer uses that same unusable-send placeholder and a non-editable input. Compact Home unavailable Send uses the muted composer send treatment instead of a live white send control. Compact Home empty Send is disabled and muted the same way Composer already disables an empty draft; the Ask field stays editable and the control stays labeled Send to Omi. Desktop unavailable Ask uses muted ink and 0.35 opacity instead of live Ask ink. Search stays editable. Compact Home and Composer unavailable Ask fields use that same muted 0.35 opacity instead of a live typeable input. Nested non-retryable cancellation, including example-platform `development_backend_unsupported` and unmounted 404, maps to `OMI_DEV_BACKEND_UNSUPPORTED` instead of retryable `OMI_HTTP_TRANSPORT`, so Stop says the door is unavailable rather than a transient blip. Native `OMI_DEV_BACKEND_UNSUPPORTED` history loads use that same unavailable copy and omit Check again instead of claiming a connection blip. Native `OMI_DEV_BACKEND_UNSUPPORTED` send throws latch Ask and use unavailable copy instead of claiming a connection blip; nested HTTP 404/unsupported already did. Transport send throws still use connection copy. An empty first page with a remaining older cursor offers Load older instead of claiming the chat is empty. Later-page failures keep already loaded messages instead of replacing them with a full-page error; nested non-retryable later pages omit Load older and still do not claim an empty chat. An empty composer with a later-history error stays on the history stage instead of the resting "I'm ready." / "Nothing's waiting on you." stage. Chat message timestamps keep Today as time-only and date Yesterday and older days with the same grouping as compact Daily Recaps and macOS Home, including conversation-detail history. A zero createdAt says Time unavailable instead of a blank clock or 1970. Switching Old/New reloads chat history and saved-data reads and drops a stale unusable-send latch from the previous plane; a failed plane persist does not reload. [Chat deployed read](../backends/example-platform/docs/memory-productionization/chat-deployed.md). | Production entry does not compose chat admission, POST success, SSE, cancellation, attachments, or a source-owned entitlement producer. Worker Firebase accounts still use a staging lifetime admission limit, not a production subscription-period authority. Migration 55 is not yet applied to DEV. | Actual deployed app sends a prompt, observes incremental and terminal events, reloads history, reconnects with event cursor, cancels, retries an uncertain admission exactly once, and shows provider failure honestly. Exercise entitlement exhaustion and a second isolated account.                                |
| Recording/transcription — `useNativeDevices`, `deviceSessionClient`             | Worker `POST /v1/device-sessions`, indexed `/audio`, `/complete`, and `GET /:id/transcript`; durable transcription jobs feed server-created conversation records. Worker `GET /v1/device-sessions/ownership` stays 503 nested `{error:{code:"capture_ownership_unavailable",retryable:false,action:"none"}}` without inventing a receipt. Native ownership preflight keeps that capability code, and nested `{error:{retryable:false}}` on 408/429/5xx is permanent instead of retryable `OMI_HTTP_TRANSPORT`. Native transcript GET throws of `OMI_DEV_BACKEND_UNSUPPORTED` or nested `retryable:false` omit Check again and use unavailable copy; transport throws still offer Check again. Nested non-retryable HTTP 503 transcript reads already did. Nested non-retryable capture opens, `OMI_DEV_BACKEND_UNSUPPORTED`, and `capture_ownership_unavailable` use unavailable copy instead of asking to reconnect; ownership preflight that is `OMI_CAPTURE_OWNERSHIP_UNAVAILABLE` or native "unavailable from this backend" no longer asks to reopen the app. Transport ownership failures still ask to reopen. DeviceSession Scan when Bluetooth is off stays a Scan control because scanning is a real action once the radio is on; compact Home no longer keeps the live mint Scan pill while scan is unavailable and uses muted 0.35 opacity instead. Remembered Reconnect uses that same muted treatment when Bluetooth cannot scan; Forget stays live because it only clears the local shortcut. A leftover disconnected device uses that same muted Connect treatment while Bluetooth cannot scan; Disconnect and Cancel stay live. Compact Home no longer says Omi disconnected when Bluetooth is on and no device is connected; that header says Omi not connected instead of claiming a dropped session. Portable local service instead has [WebSocket `/v4/listen`](../backends/example-platform/apps/service/routes/listen.ts).                                                                                                                                                                   | The production entry stores indexed audio chunks and upload completion in PostgreSQL using the canonical Listen authority. Transcription processing and transcript reads now use persisted claims, saved provider results and canonical batched Listen publication. Retries resume saved results without another provider call. Conversation listing is now mounted; downstream formation workers remain unwired; upload completion is not transcript completion. A configured capture-ownership signing key is still required before mutations; without it, admitted ownership GET and capture writes are nested non-retryable 503 `{error:{code:"capture_ownership_unavailable",retryable:false,action:"none"}}` without `retry-after` and without inventing a receipt. Identity-verification and authorization-source 503s stay retryable with `retry-after: 1`. Phone-microphone recording is not implemented by the wearable path; encrypted journal recovery is implemented with physical offline/restart verification still open.                                                                 | Physical Omi audio survives lost acknowledgements and reconnects without duplicate bytes; completion leads to an actual stored transcript in the mobile conversation detail; queued/running jobs can resume; terminal failures are truthful and refresh without restarting paid work; wrong-account reads fail. Test real background/foreground behavior and process death separately. |
| Conversations — shared `ConversationsPage`                                      | Worker projects namespaced `chat:` and `recording:` IDs, bounded metadata and transcript detail. `GET /v1/chat-messages` defaults to the same main-session grouping as `chat:chat-main` and accepts optional `chatSessionId` for a named `chat:` item. Home and `chat:chat-main` omit that query. Local portable composition has conversation projection routes. Mobile reuses the full page rather than only recap cards. Conversation detail loads session-scoped history for every `chat:` id instead of mixing main history into another session. Empty titles on processing Listen/recording rows use the same visible fallback as desktop instead of a blank row. Empty summaries use the same class of visible fallback instead of a blank subtitle or detail gap. Listen/microphone conversation detail states that a full transcript is not available yet instead of presenting a blank gap as the whole record. Search indexes those visible title/summary fallbacks. Wide Home search rows use those same visible title and summary fallbacks instead of blank gaps. Home Daily Recaps use the same title fallback and empty-library completeness copy instead of blank cards or "No recaps yet" on incomplete conversation pages. Compact Home Daily Recaps open the matching conversation detail instead of remaining display-only; View All still opens the conversation list. Compact Home Daily Recaps use the same Today/Yesterday/date grouping as Conversations instead of weekday-only labels. A zero createdAt or startedAt groups as Date unavailable instead of 1970. macOS Home and Conversations rows keep Today as time-only and date Yesterday and older days with that same grouping instead of a clock time that could belong to any day. A missing or zero timestamp says Time unavailable instead of omitting the clock. Conversation list and detail Started/Finished timestamps use that same Today/Yesterday/date clock instead of month and day without a year. Conversation list and detail durations under 60 seconds say "< 1 min" instead of "0 min". A zero startedAt or finishedAt says Duration unavailable instead of a multi-year duration or "< 1 min". Conversation detail Captured (device time) uses that same Today/Yesterday/date clock as Started/Finished instead of a locale datetime, and a zero capture timestamp says Time unavailable instead of 1970. Conversation detail Status uses In progress / Processing / Merging / Completed / Failed instead of snake_case wire tokens such as in_progress, and an empty status says Status unavailable. Chat history timestamps on Home and conversation detail use that same Today/Yesterday/date clock instead of time-only for older messages, and a zero createdAt says Time unavailable instead of a blank clock or 1970. Conversation-detail chat history now uses that same clock instead of sender and text only. A cancelled conversation-detail assistant message says Response stopped instead of a completed blank Omi line. macOS Home Ask exchange uses that same Today/Yesterday/date clock instead of sender and text only, and a zero createdAt says Time unavailable. Compact Home no longer offers an Open calls control that substitutes the Conversations tab for a phone-calls producer. Nested non-retryable conversation, task, and memory Home errors use that mapped unavailable copy instead of "Couldn’t load recaps/tasks/mind map". Wide Home search OutcomeStatus uses that same mapped error instead of "Conversations are unavailable" / "Memories are unavailable". When conversation and memory doors both failed, that search parent line uses the mapped copy instead of a generic "Saved data is unavailable." claim above it, and task OutcomeStatus still shows. That parent also uses the mapped copy instead of "Showing saved data. Could not refresh." when tasks still have rows. The macOS library banner for nested non-retryable first-page failures uses that mapped copy instead of "Some of your history isn't loaded yet." Generic missing-content stages keep the existing Couldn’t load copy. Home search uses that completeness copy instead of claiming a complete miss over incomplete conversation, memory, or task pages. A Home search miss after a nested non-retryable later page uses search-miss copy instead of more-available. Wide Home search OutcomeStatus also omits more-available after that closed later conversation, memory, or task page. An unfiltered empty Home with remaining items still reports more-available. Conversations/Memories/Tasks empty-search titles use the same completeness-first copy instead of "No loaded X match" on incomplete pages. Rewind does not claim "No captures saved yet" while a continuation cursor remains. Rewind list timestamps use the same Today/Yesterday/date clock as conversation capture time instead of a locale datetime, and a zero capture timestamp says Time unavailable instead of 1970. Rewind list timestamps use the same Today/Yesterday/date clock as conversation capture time instead of a locale datetime, and a zero capture timestamp says Time unavailable instead of 1970. Later Rewind pages that fail with `OMI_REWIND_UNAVAILABLE` or `OMI_REWIND_AUTH` keep already loaded frames, show that unavailable copy, and omit Load more instead of advertising another page against a capability door. Generic later-page failures keep Load more. Desktop Conversations (`LibraryPage`) now exposes Load more conversations when more pages exist, matching mobile; nested non-retryable later pages keep loaded rows, show unavailable copy, and omit Load more, while generic later-page failures keep Load more. Nested non-retryable later pages also omit "More conversations are available." Nested non-retryable later-page empty searches use "No loaded conversations match." instead of that more-available copy. Nested non-retryable first-page conversation reads omit Refresh; retryable first-page failures still offer Refresh. Compact Home recap errors use that same Refresh omit/offer. Loaded compact Home recaps keep completeness and more-available copy instead of looking like a complete strip. Nested non-retryable later pages omit that more-available copy. macOS Home omits "More conversations are available." after a nested non-retryable later conversation page. A Home search miss after that closed later page uses search-miss copy instead of more-available. An unfiltered empty Home with remaining conversations still reports more-available instead of "Nothing captured yet." Conversation chat detail does not claim "No messages in this chat yet" while an older-history cursor remains. Conversation-detail and mobile Home Load older controls are labeled Load older messages. Conversation list rows show a star only when the backend marked the conversation starred, matching wide Home search; they do not show an empty star that looks like a toggle this connection cannot perform.                                                                                                                                                                                                                                    | Production entry now reads recorded and canonical finalized Listen sessions under the exact conversations.read grant. Persisted account revisions fence cursors when visibility or state changes. Envelope reads that also hold `chat.read` page granted `chat:` sessions with Listen rows by `updatedAt` descending then `id` ascending, matching Worker `readConversations` / `paginateConversations`, without dumping every chat onto page one or inventing empty `chat:chat-main`. A Listen-sequence cursor cannot continue on that union path. Production history GET defaults to main-session only and accepts one optional `chatSessionId` for a named `chat:` item; unknown extra keys stay 400. Metadata mutations remain missing. Conversation pages now select bounded PostgreSQL rows before transcript projection; signed cursor positions persist for 900 seconds with a 10,000-position account metadata ceiling.                                                                                                                                                                             | Persisted chat and recordings appear after a fresh app launch, account isolation holds, no source-ID collision occurs, transcript/detail works, and pagination/search scope is explicit. No duplicated or skipped records at cursor boundaries.                                                                       |
| Tasks — shared task client/UI + canonical task authority                        | App `GET /v1/tasks`, `POST /v1/tasks/ops`; complete/reopen/edit uses account epoch and revision, retained operation identity, failure retry/dismiss, authoritative refresh. Local SQLite app path is exercised; Worker delegates canonical reads/writes through the same service binding when configured. Mobile Home and the Tasks tab keep incomplete or degraded empty pages from claiming "Nothing's waiting on you." Desktop Home search does not claim "No tasks yet" when a query hides existing tasks. Combined Home search empty titles include task-page completeness so complete conversation/memory pages cannot claim "No results" over incomplete tasks. Wide Home search results include matching tasks instead of only conversation and memory rows. Wide Home search OutcomeStatus includes that same task page, including mapped task errors and more-available copy, and omits more-available after a nested non-retryable later task page. That task row still shows when conversation and memory doors both failed. The Tasks page empty-search title keeps completeness copy instead of "No loaded tasks match" on incomplete pages. Nested `{error:{retryable:false}}` task-write 503s and `OMI_DEV_BACKEND_UNSUPPORTED` throws are permanent and omit Retry, and they now say task editing is unavailable instead of "This change was not accepted."; write-ops `control_unavailable` 503s stay retryable. Home, Tasks, and desktop Tasks do not say "Task editing is unavailable for this connection" while the task read is loading or after that read failed; that copy stays only after a successful task page that this connection cannot edit. Compact Home and the Tasks page do not label a disabled read-only task control Open; incomplete read-only rows stay Task, completed read-only rows stay Completed, and Complete/Reopen stay only when writes are available. Tasks page Open task still opens the editor when writes and an edit handler are present. macOS Tasks uses that same Task/Completed read-only labeling instead of Complete/Reopen on a disabled control. Task due dates keep UTC day boundaries and include the year so last-year overdue dues in Today do not look like this year. Wide Home search task rows use that same calendar due copy instead of a raw epoch, and second-scale Worker/ratified dues use the same UTC day as millisecond canonical dues instead of 1970. A zero due timestamp says Date unavailable instead of 1970 and does not group into Today. Nested non-retryable later task pages omit Load more and also omit "More tasks are available."; generic later-page failures keep Load more and that more-available copy. Nested non-retryable later-page empty searches use "No loaded tasks match." instead of that more-available copy. Home tasks still report more-available without a Home pager because the Tasks page has that pager, except after a nested non-retryable later task page, when macOS Home also omits that more-available copy. A Home search miss after that closed later page uses "No tasks match this search." instead of more-available. An unfiltered empty Home with remaining tasks still reports more-available instead of "No tasks yet." Nested non-retryable first-page task reads omit Refresh; retryable first-page failures still offer Refresh. Compact Home and the compact Tasks tab use that same Refresh omit/offer on first-page task errors. Loaded compact Home tasks and the compact Tasks tab keep more-available copy instead of looking complete. Nested non-retryable later pages omit that more-available copy.                                                                                                                                                    | Production entry now composes verified Firebase task grants, persisted control/epoch fences and transactional task/receipt storage. Remaining limits include a 10,000-distinct-record lifetime ceiling including tombstones and an undeployed straggler retention scheduler. Real account and grant issuance remains required. Task generation from real recordings also needs its actual producer, not a seeded task list.                                                                                                                                                                                                          | Signed-in app edits a real task, restarts, sees saved state; duplicate op is acknowledged once; stale revision/epoch blocks writes; conflict refresh preserves draft; lost acknowledgement retries the same op; another account cannot read/write it.                                                                 |
| Memories — ratified read adapter + Firebase/Postgres reader                     | App `GET /v1/memories`; production entry composes a real authorized reader and HTTP render-model port. Worker [memory adapter](../apps/backend-worker/src/memory-service.ts) forwards original Firebase credentials via [service binding](../apps/backend-worker/src/canonical-service.ts), never staging credentials. Empty authorized pages with incomplete or degraded coverage show that completeness copy instead of "No memories yet." Production unavailable accepted-work/STM coverage maps to degraded / `projection_unavailable`. Home search uses that same completeness copy instead of "No results" / "Nothing captured matches this search." Library Memories empty-search titles keep that completeness copy instead of "No loaded memories match." Later-page nested `{error:{retryable:false}}` 503s show unavailable copy and omit Load more instead of "More memories could not be loaded." Generic later-page failures keep that load-failure copy and Load more. Desktop Home now exposes Load more memories when more memories exist, matching that later-page retryability: nested non-retryable later pages keep loaded rows, omit Load more, and omit "More memories are available."; generic later-page failures keep Load more. Nested non-retryable later-page empty searches use "No loaded memories match." instead of that more-available copy. A Home search miss after that closed later page uses search-miss copy instead of more-available. An unfiltered empty Home with remaining memories still reports more-available instead of "Nothing captured yet." Nested non-retryable first-page memory reads omit Refresh; retryable first-page failures still offer Refresh. Compact Home mind-map errors use that same Refresh omit/offer. Loaded compact Home mind map keeps completeness and more-available copy instead of looking like a complete map. Nested non-retryable later pages omit that more-available copy. The preview stays collapsed when memories exist. Compact Home uses View All for that Memories door instead of Expand. Compact Home empty mind map uses that same empty-library completeness copy instead of decorative nodes that look populated. That visible label stays memories-only in the mixed Conversations & memories list, including when conversation pages also have more; Conversations keeps its own pager. Empty memory bodies use visible fallback copy instead of a blank card. Old `/v3/memories` namespaced entity prefixes stay out of visible memory text the same way canonical `parseMemoryText` already does, without inventing provenance digests. Wide Home search and macOS memory rows use that same visible text. Search indexes that visible copy. Wide Home search and Home currents memory rows use the same citation-count copy as the Memories page instead of claiming source citations when the citation list is empty. The Memories page treats a zero timestamp as Date unavailable instead of 1970, matching null timestamps. The Memories page Load more is labeled Load more memories.                                                                                                                                       | Production data ingestion, ownership/grants, render model configuration and actual accepted-work/STM coverage must be present. The current entry intentionally declares coverage unavailable. Memory-read deployment does not establish the formation/write pipeline.                                                                                                                                                                                   | Authorized non-seeded source data yields grounded renders with valid citations/provenance and honest completeness. Missing coverage stays incomplete or degraded and must not read as an empty complete library, cross-account reads fail, revocation works, and the mobile page consumes the deployed response without a legacy-memory mapping.                                 |
| Settings/account/privacy — `SettingsPage`, `desktopCloudClient`                 | Native account/subscription/recording-storage/training/private-sync/webhook reads and supported privacy mutations use existing primary-cloud APIs. Web `GET /v1/settings` preserves nullable identity and shows chat requests or transcription seconds with their actual units; policy links and native OS permissions are real actions. Settings is a mobile bottom destination. Missing mobile `settingsContent` reports unavailable instead of a blank Settings stage. Production entry now mounts `GET /v1/settings`: signed-out is `{identity:null,entitlement:null}`, invalid credentials are 401, and a verified Firebase identity without an owner-backed producer is nested non-retryable 503 `{error:{code:"service_unavailable",retryable:false,action:"none"}}` without `retry-after`. Browser Settings shows typed unavailable copy for that 503 instead of a generic load failure or an invented profile, and omits Retry. A Firebase verification outage stays retryable 503 with `retry-after: 60`. Worker `GET /v1/settings` keeps D1 admission usage and the Worker chat-admission limit, and does not project `STAGING_DISPLAY_NAME` / `STAGING_EMAIL` / `STAGING_PLAN_LABEL` as a profile or billing plan; empty identity and plan strings render as unavailable, and `upgradeAvailable` stays false. Nested non-retryable `/v1/users/*` reads and writes, including example-platform `development_backend_unsupported`, say the account setting is unavailable instead of "This saved data is not available" or "Settings change could not be saved. Try again." Desktop Settings Account does not claim "Signed in to Omi" when that profile slice is unavailable, and does not claim it when a successful profile read has no email; that case uses "Email not set on this account." matching mobile Settings. A successful profile also shows Account id and Name, using "Name not set on this account." when name is null. Desktop Settings Current plan shows already-loaded transcription seconds with the same used/limit units as mobile Settings Plan when both values are present; it does not invent 0 / 0 when those fields are null. Desktop Settings Account shows already-loaded company, job, and data protection when those profile fields are present; it does not invent empty rows when they are null. Mobile Settings Plan and desktop Settings Current plan humanize plan and status tokens instead of showing plus · active, and data protection humanizes standard instead of the raw enum. Empty tokens say Plan unavailable or Data protection unavailable. Desktop Settings Audio Recording shows Off / Always / Meetings instead of raw preference tokens; stored values stay off / always / meetings. Mobile and desktop Settings Developer webhook rows use Conversation Events / Real-time Transcript / Audio Bytes / Day Summary instead of API keys such as memory_created; unknown types stay visible as humanized copy, and an empty type says Webhook unavailable. Desktop Settings Data Retention shows Forever instead of 0 because native capture treats 0 as no expiry cutoff; stored rewindRetentionDays stays 0, and 7 / 14 / 30 stay numeric. Mobile Apps and the macOS Apps gallery humanize category tokens such as productivity instead of showing the raw enum; empty categories stay omitted. Desktop Alerts does not claim recording storage or private cloud sync unavailable while the account snapshot is still loading, and a page-level `loadAccountSettings` failure surfaces the mapped error instead of leaving Account on "Loading account…". Desktop Alerts now shows the already-loaded training opt-in slice instead of omitting that closed `/v1/users/training-data-opt-in` door. Nested non-retryable Opt in, recording-storage, and private-cloud-sync writes omit Try again and latch those controls independently; retryable 503s keep them live. Desktop AI & Automation now shows the already-loaded `/v1/users/developer/webhooks/status` slice instead of omitting that closed developer door. An empty webhook catalogue stays empty instead of unavailable. Local preference failures still say Try again. [Settings deployed read](../backends/example-platform/docs/memory-productionization/settings-deployed.md).                                                                                                                                                                | Worker lacks legacy `/v1/users/*` settings APIs. Production identity, entitlements and privacy mutation authorities must still be composed; do not present staging labels as verified profile/billing. Name/language editing, notification preferences and storage controls need real persistence before adding forms. | Mobile Settings remains selected in bottom navigation; authenticated profile/usage reflect the actual user; a supported privacy change survives reload and affects the authoritative policy; unavailable settings show retryable user-facing copy without request IDs.                                                |
| Connectors/apps — `ConnectorsPage`                                              | `/v1/apps`, `/v1/apps/enabled`, profile, enable/disable calls exist in [cloud client](../react-native/src/desktopCloudClient.ts); native transport can use primary cloud APIs. Apps tab is retained. Missing mobile `appsContent` reports unavailable instead of "No apps connected yet." Nested `{error:{retryable:false}}` catalogue 503s, including example-platform `development_backend_unsupported`, show Apps-unavailable copy and omit Retry; omitted 503 retryable still offers Retry. Desktop Apps gallery uses that same unavailable copy instead of "Apps could not be loaded" for nested non-retryable catalogue 503s. Browser Apps already omits Retry because that connection has no catalogue producer. My Apps no longer says owned apps are unavailable until the account profile loads after a nested non-retryable `/v1/users/profile` read; it uses account-setting unavailable copy instead. Nested non-retryable `/v1/apps/enabled` reads do not treat catalogue `enabled` bits as installed apps; Installed shows Apps-unavailable copy instead of those catalogue rows. Explore and the macOS gallery do not claim "Not installed" / "Not connected" over that closed door. Explore omits Install and Remove while `enabledIds` is unknown; nested non-retryable `/v1/apps/enable` POSTs already latch that Install control and show Apps-unavailable copy. A successful empty enabled list still reports Not installed / Not connected and still offers Install. The macOS Apps gallery surfaces that same enabled-list error instead of claiming "No apps are available" or showing Installed tiles.                                                                                                                                                                                                                                                         | Worker and production portable entry do not expose connector catalog/ownership/enablement APIs. Production browser routing/auth is missing. A connector toggle is not proof that its external authorization or ingestion works.                                                                                                                                                                                                                         | Real catalog loads, enabled state survives reload, ownership is enforced, external authorization cancellation/errors are handled, and app data arrives through an authorized producer.                                                                                                                                |
| Attachments — native transport + backend attachment authority                   | Worker upload admission/completion, checksums, ownership and chat attachment admission exist in [attachments](../apps/backend-worker/src/attachments.ts). Local portable service has [attachment routes](../backends/example-platform/apps/service/routes/chat-attachments.ts).                                                                                                                                                                              | Production portable entry does not compose attachment storage/signing/completion. End-user native picker/upload and signed object-store integration need their own evidence; backend tests alone do not establish them.                                                                                                                                                                                                                                 | Attach, upload, complete and send through the actual app; reject mismatched checksum/size/owner, expire access and recover uncertain completion. Do not expose provider credentials in JS or public object URLs.                                                                                                      |
| Offline/process recovery — native storage owner                                 | Capture uses an encrypted native recording journal, with bounded fsynced entries, native owner receipts and indexed batch acknowledgement before replay cleanup. [Journal implementation and limits](native-recording-journal.md) describe the account/origin/login partition and recovery contract; the separate [sync outbox](../packages/sync/src/outbox.ts) is not the recording journal.                                                                                                                                                                                                                                                                                                                 | Physical offline/restart replay and suspended background packet delivery remain unverified. Cached ownership is permitted only after classified offline transport failures; every replay requires fresh matching authority. Process recovery protects already persisted packets and does not imply continuous recording after OS termination.                                                                                                                                                                                                                          | Capture while offline, terminate process, relaunch/reconnect and upload once; sign-out/account-switch cannot replay old account audio; corrupt/partial journal entries fail visibly and storage limits are enforced.                                                                                                  |

Settings authority boundary: the canonical [settings projection](../backends/example-platform/apps/service/control/settings-projection.ts) requires identity and shares its entitlement projection with enforcement. Its implementations are [local SQLite](../backends/example-platform/drivers/sqlite/service-stores/settings-projection.ts) and in-memory; [app-facing producers](../backends/example-platform/apps/service/app-facing.ts) seed QA identity and optional unmetered entitlement. PostgreSQL account/grant records do not supply profile, billing or consumed usage, and [verified Firebase identity](../backends/example-platform/apps/service/auth/firebase-identity.ts) supplies only project, UID, authentication strength and expiry. Production now mounts `GET /v1/settings` and returns nested non-retryable 503 after a verified identity until that owner-backed producer exists; it does not project token claims as a profile or advertise Retry-After for that door. Native `/v1/users/profile` and `/v1/users/me/subscription` continue through the primary cloud [client](../react-native/src/desktopCloudClient.ts) and [route policy](../react-native/src/v5BackendOrigin.ts); they are not portable-backend implementations. Web service settings read the existing local projection, preserve absent identity/entitlement, and label chat requests and transcription seconds separately. Unknown allowance units remain unavailable; this local read does not create a deployed producer.

Chat identity boundary: the PostgreSQL [memory context adapter](../backends/example-platform/drivers/postgres/firebase-chat-generation-context-source.ts) now produces the trusted context packet consumed by the current supervisor, using the authenticated account-bound memory read. This does not authorize chat generation. Production now persists and reads chat history under `chat.read`; write, SSE, cancellation and attachment doors return nested 404 without admission. The unmounted [gateway source](../backends/example-platform/apps/service/chat/generation-source.ts) still sends the opaque context account ID as `x-omi-user-uid` and defaults its service caller to `platform`; neither establishes the verified Firebase UID or a gateway-allowlisted caller. Production chat writes must compose the actual verified Firebase identity, account binding, explicit caller configuration and a source-owned entitlement producer before mounting admission. The local QA principal and memory-read grant cannot substitute for `chat.read` or `chat.write`.

Chat entitlement integration prerequisite: the legacy origin owns `backend/utils/subscription.py:get_chat_quota_snapshot`, catalog revision 2 in `backend/config/plan_catalog.json`, and UTC monthly consumption in `backend/database/user_usage.py:get_monthly_chat_usage`. The `/v1/users/me/usage-quota` response is a read snapshot, not an atomic reservation; the gateway's `gateway/jit_budget.py` reservation is restricted to JIT QA run budgets and cannot authorize subscriber chat. The required source-owned admission operation must bind the verified Firebase project/UID, canonical account and epoch, client-message ID and payload hash to one durable reservation. Exact replay must return its existing receipt without consuming again; changed payload must conflict; missing source state must be unavailable; exhaustion must remain an entitlement refusal. The receipt needs the source catalog/subscription revision, UTC usage period and billing unit, and must be persisted with PostgreSQL message admission and its first generation event. Completion must settle that same reservation once from gateway-owned provider-attempt accounting; interrupted or unknown-cost attempts must not be silently released. A same-message retry after an uncertain response must reconcile the reservation rather than allocate another. These are integration requirements, not an implemented public endpoint: the legacy source producer and its authenticated transport do not yet exist. PostgreSQL `chat.read` now exists as an explicit application grant for history GET; `chat.write` still requires that same lifecycle and is not conferred by Firebase identity or a memory grant. The current token-only `ChatGenerationUsage` shape also lacks the cost and settlement evidence needed for monetary plans.

## Acceptance checklist for subsequent work

- [ ] **Production authority:** provision and verify real Firebase credential binding, application grant, account control and revocation for an intended account; no seeded principal or staging-token substitution.
- [ ] **Full deployed composition:** mount every required app capability above with production stores, model/storage ports and bounded shutdown/readiness behavior. Keep unsupported routes explicit until implemented.
- [ ] **Provider independence:** run the same app contract scenarios against Worker and portable production composition. Where the app wire differs (`/v1/device-sessions` versus `/v4/listen`), implement the accepted boundary rather than silently switching semantics.
- [ ] **Real mobile path:** fresh setup → sign-in → optional device → recording/transcript → conversation → task mutation → Settings/Apps, with reload and account isolation. Simulator rendering alone does not close physical-device checks.
- [ ] **Failure and recovery:** credential expiration/revocation, offline/process death, interrupted uploads, uncertain writes, SSE reconnect/cancel, stale task revision/epoch and unavailable projections produce the intended recoverable state.
- [ ] **Production release evidence:** record deployed revision/configuration, account-scoped end-to-end results, migration/rollback readiness and provider-specific resource verification. Health/ready 200 and a container image are prerequisites, not full parity.

Existing local evidence and limitations live in [production-readiness.md](production-readiness.md). This checklist adds no new domain authority, seeded production data, or declaration that all capabilities are complete.

Browser parity remains open: current browser setup continues into Omi, and Apps explains that it is not available for this browser connection yet instead of offering an unusable sign-in. These capability boundaries do not complete browser authentication, connector authorization or app installation. The browser still needs real identity/session and app-service composition before those actions can be enabled.
