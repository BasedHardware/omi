// Plan & Usage settings tab — scaffold registered ahead of implementation so the
// tab rail and panel wiring land in one place. Content is built out in the
// settings-parity workstream (Mac reference: SettingsContentView+AccountBilling.swift).
export function PlanUsageTab(): React.JSX.Element {
  return <></>
}
