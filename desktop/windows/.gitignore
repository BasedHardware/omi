node_modules
dist
out
.DS_Store
.eslintcache
*.log*

# Secrets — never commit
.env
.env.local
.env.*.local

# Build / installer artifacts
build/
release/
dist/
*.exe
*.msi
*.dmg
*.AppImage

# Generated audio test fixtures (pnpm fixtures:audio) — never commit binaries
test/fixtures/audio/

# Staged VAD runtime assets (pnpm vad:assets) — copied from node_modules, never commit binaries
src/renderer/public/vad/

# TypeScript incremental-build cache
*.tsbuildinfo

# Perf benchmark artifacts + tooling (dev-only; keep out of the public repo)
.bench/
scripts/bench/
src/main/bench/

# HuggingFace / model cache
.cache/
models/

# OS
Thumbs.db
desktop.ini

#Personal files
.agents/
.vscode/
# docs/ holds personal notes, EXCEPT explicitly re-included design docs
docs/*
!docs/conversation-sync.md
!docs/bar-gotchas.md
!docs/multi-worktree-dev.md
!docs/mac-parity-audit
skills-lock.json

# Visual brainstorming companion
.superpowers/

# .NET build artifacts (win-ocr-helper)
src/main/ocr/win-ocr-helper/bin/
src/main/ocr/win-ocr-helper/obj/
resources/win-ocr-helper/*.pdb

# .NET build artifacts (win-automation-helper)
src/main/automation/helper/bin/
src/main/automation/helper/obj/
resources/win-automation-helper/*.pdb

# .NET build artifacts (win-audio-helper)
src/main/audio/helper/bin/
src/main/audio/helper/obj/
resources/win-audio-helper/*.pdb

# Orb harness artifacts (contact sheets, pixel dumps) — generated
.orb-out/
orb-harness/dist/
