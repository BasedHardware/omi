{
  "change": "The Brain Map now draws every connection instead of a few dozen, brighter inside a neighbourhood than between two, and fits the map to a square rather than stretching it across the window"
}
