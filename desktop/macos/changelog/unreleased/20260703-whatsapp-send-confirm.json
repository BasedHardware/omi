{
  "change": "WhatsApp replies now send fully automatically and safely — Omi verifies the right chat is open before sending and confirms against WhatsApp's own record that the message actually landed in that chat, so it never sends to the wrong person or marks an unsent reply as sent"
}
