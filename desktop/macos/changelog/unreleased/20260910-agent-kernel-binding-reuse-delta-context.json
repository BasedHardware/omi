{
  "change": "Follow-up chat messages no longer re-send the whole conversation context on every turn: the app now keeps its agent session between turns and sends only what changed. This makes follow-ups noticeably faster on local models and cheaper on cloud ones."
}
