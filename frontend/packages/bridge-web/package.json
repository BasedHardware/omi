{
  "name": "@omi-core/bridge-web",
  "version": "0.0.0",
  "private": true,
  "description": "Web-tier bridge bindings: IndexedDB-backed DurableLog/DurableKv StorageBridge (best-effort durability per ADR-008 \u00a73 \u2014 native shells bind real stores instead), and the privileged-HTTP HttpClient binding that keeps tokens and base URLs shell-side.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "echo bridge-web:browser-only-covered-by-shell-e2e",
    "lint": "echo bridge-web:no-lint-yet"
  },
  "dependencies": {
    "@omi-core/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
