{
  "name": "omi-core",
  "private": true,
  "version": "0.0.0",
  "description": "Omi shared product core \u2014 contracts, codegen, packages, shells (WS-002)",
  "engines": {
    "node": ">=22.19.0"
  },
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "codegen": "node scripts/gen-bridge-swift.mjs && node scripts/gen-bridge-dart.mjs",
    "codegen:check": "node scripts/gen-bridge-swift.mjs --check && node scripts/gen-bridge-dart.mjs --check",
    "verify": "pnpm -r build && pnpm -r test && node scripts/check-isolation.mjs && node scripts/gen-barrels.mjs --check && node scripts/check-structure.mjs && node scripts/gen-bridge-swift.mjs --check && node scripts/gen-bridge-dart.mjs --check"
  }
}
