{
  "name": "@omi-core/shell-macos",
  "version": "0.0.0",
  "private": true,
  "description": "macOS AppKit glass shell: fixed-port loopback origin, transparent WKWebView island composition, privileged HTTP bridge with shell-side credential custody.",
  "type": "module",
  "scripts": {
    "build": "echo \"shell-macos: Swift build is scripts/build-shell.sh (needs a macOS toolchain); not part of pnpm -r build\"",
    "test": "node --test tests/*.test.mjs"
  }
}
