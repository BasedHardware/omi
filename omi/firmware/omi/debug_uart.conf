# Debug overlay: log over UART console (P0.03 TX / P0.02 RX @115200) instead of RTT.
# prj.conf already enables CONFIG_LOG + CONFIG_UART_CONSOLE.
CONFIG_OMI_VAD_HOLD_MS=10000
CONFIG_OMI_AAD_SETTLE_MS=800
CONFIG_OMI_DEBUG_FAKE_RTC=y
