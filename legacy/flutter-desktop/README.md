# Flutter Desktop (Archived)

This directory contains the archived Flutter desktop (macOS) app code. It has been replaced by the native Swift macOS app at `desktop/`.

This code is kept for historical reference only and is no longer maintained or built.
